# ═══════════════════════════════════════════════════════════════════════════
# origin.olang VM — x86_64 Linux (no libc)
# Author: Lyra (session 2pN6F)
# ═══════════════════════════════════════════════════════════════════════════
#
# Register conventions:
#   r12 = bytecode base pointer (immutable during execution)
#   r13 = program counter (offset into bytecode)
#   r14 = VM stack pointer (NOT CPU stack)
#   r15 = heap pointer (bump allocator)
#   rbx = bytecode size (bounds checking)
#   rbp = preserved (call frame base)
#   rsp = CPU stack (for CALL/RET, syscalls)
#
# VM stack layout: each entry = 16 bytes
#   [ptr:8][len:8] — points to chain/string data on heap
#   For f64: ptr stores raw f64 bits, len = 0xFFFFFFFF_FFFFFFFF (marker)
#
# Bytecode format: see crates/olang/src/exec/ir.rs
# ═══════════════════════════════════════════════════════════════════════════

.section .text
.globl _start

# ── Constants ────────────────────────────────────────────────────────────
.equ STACK_SIZE,    0x100000        # 1 MB VM stack
.equ HEAP_SIZE,     0x40000000      # 1 GB heap arena
.equ HEADER_SIZE,   32
.equ ELF_HEADER_SIZE, 120           # ELF64 header + 1 program header
.equ F64_MARKER,    -1              # len = 0xFFFFFFFFFFFFFFFF means f64
.equ ARRAY_MARKER,  -3              # len = 0xFFFFFFFFFFFFFFFD means array
.equ DICT_MARKER,   -4              # len = 0xFFFFFFFFFFFFFFFC means dict
# Note: CLOSURE_MARKER = -2 is defined near cg_closure

# Array heap layout: [count:8][elem_0_ptr:8][elem_0_len:8]...
# When array is relocated by push: old location gets [FORWARD_MARKER:8][new_base:8]
# Dict heap layout:  [count:8][entry: key_ptr:8 key_len:8 val_ptr:8 val_len:8]...
.equ ARRAY_INIT_CAP, 512
.equ ARRAY_FORWARD_MARKER, -2      # sentinel: array relocated

# ── Register frame constants ──
.equ REG_MAX_SLOTS,  64             # max locals per function
.equ REG_SLOT_SIZE,  16             # [ptr:8][len:8] per slot
.equ REG_FRAME_SIZE, 1024           # REG_MAX_SLOTS * REG_SLOT_SIZE
.equ REG_STACK_SIZE, 0x400000       # 4MB register stack (4096 frames)

# ── Syscall numbers ─────────────────────────────────────────────────────
.equ SYS_READ,      0
.equ SYS_WRITE,     1
.equ SYS_OPEN,      2
.equ SYS_CLOSE,     3
.equ SYS_MMAP,      9
.equ SYS_MUNMAP,    11
.equ SYS_NANOSLEEP, 35
.equ SYS_CLOCK_GETTIME, 228
.equ SYS_SOCKET,    41
.equ SYS_CONNECT,   42
.equ SYS_SENDTO,    44
.equ SYS_RECVFROM,  45
.equ SYS_GETDENTS64, 217
.equ SYS_EXIT,      60
.equ AF_INET,       2
.equ SOCK_STREAM,   1
.equ SOCK_DGRAM,    2
.equ CLOCK_MONOTONIC, 1

# ── File open flags ─────────────────────────────────────────────────────
.equ O_RDONLY,       0
.equ PROT_RW,       3               # PROT_READ | PROT_WRITE
.equ MAP_PRIV_ANON, 0x22            # MAP_PRIVATE | MAP_ANONYMOUS

# ═══════════════════════════════════════════════════════════════════════════
# Task 1: Entry point + memory setup
# ═══════════════════════════════════════════════════════════════════════════

_start:
    # ── 1. Open bytecode file ────────────────────────────────────────
    # Check argc: if argv[1] exists, check for --eval or open that file
    # Otherwise fall back to /proc/self/exe
    # Stack at _start: [argc][argv[0]][argv[1]]...
    mov     (%rsp), %rax                # argc
    cmp     $2, %rax
    jb      .open_self                  # no args → open self

    # Check if argv[1] == "--eval" (bytes: 2d 2d 65 76 61 6c 00)
    mov     16(%rsp), %rdi              # argv[1]
    # Compare first 4 bytes: "--ev" = 0x76652d2d (little-endian)
    cmpl    $0x76652d2d, (%rdi)         # "--ev"
    jne     .not_eval
    # Compare next 2 bytes: "al" = 0x6c61
    movzwl  4(%rdi), %eax
    cmp     $0x6c61, %eax               # "al" little-endian
    jne     .not_eval
    # Check null terminator
    cmpb    $0, 6(%rdi)
    jne     .not_eval

    # --eval mode detected: set flag, use argv[2] or /proc/self/exe
    movb    $1, eval_mode(%rip)
    cmpq    $3, (%rsp)                  # argc >= 3? (argv[2] = file)
    jb      .open_self
    mov     24(%rsp), %rdi              # argv[2]
    jmp     .do_open

.not_eval:
    # Check if argv[1] == "--mcp" (bytes: 2d 2d 6d 63 70 00)
    mov     16(%rsp), %rdi
    cmpl    $0x636d2d2d, (%rdi)         # "--mc" little-endian
    jne     .not_mcp
    movzwl  4(%rdi), %eax
    cmp     $0x0070, %eax               # "p\0"
    jne     .not_mcp
    movb    $2, eval_mode(%rip)         # mode 2 = MCP
    jmp     .open_self
.not_mcp:
    mov     16(%rsp), %rdi              # argv[1] = filename
    jmp     .do_open
.open_self:
    lea     proc_self_exe(%rip), %rdi
.do_open:
    mov     $O_RDONLY, %esi
    xor     %edx, %edx
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      err_open                    # negative = error
    mov     %rax, %rbp                  # rbp = fd (temporarily)

    # Read header (32 bytes) onto CPU stack
    sub     $HEADER_SIZE, %rsp
    mov     %rbp, %rdi                  # fd
    mov     %rsp, %rsi                  # buf = stack
    mov     $HEADER_SIZE, %edx          # count
    mov     $SYS_READ, %eax
    syscall
    cmp     $HEADER_SIZE, %rax
    jne     err_header

    # Check magic: if ELF (7f 45 4c 46), find origin header via trailer
    cmpl    $0x464c457f, (%rsp)         # "\x7fELF" in little-endian
    jne     .header_ok                  # not ELF → already origin header

    # ELF detected → read 8-byte trailer from end of file
    # lseek(fd, -8, SEEK_END) to find header_offset
    mov     %rbp, %rdi                  # fd
    mov     $-8, %rsi                   # offset = -8
    mov     $2, %edx                    # SEEK_END = 2
    mov     $8, %eax                    # sys_lseek
    syscall

    # Read 8 bytes (header_offset u64 LE) into stack buffer
    mov     %rbp, %rdi                  # fd
    mov     %rsp, %rsi                  # reuse stack buf
    mov     $8, %edx                    # count = 8
    mov     $SYS_READ, %eax
    syscall

    # Get header_offset from buffer
    mov     (%rsp), %rsi               # header_offset (u64 LE)

    # lseek(fd, header_offset, SEEK_SET)
    mov     %rbp, %rdi                  # fd
    xor     %edx, %edx                  # SEEK_SET = 0
    mov     $8, %eax                    # sys_lseek
    syscall

    # Re-read 32 bytes origin header
    mov     %rbp, %rdi                  # fd
    mov     %rsp, %rsi                  # buf = stack (overwrite)
    mov     $HEADER_SIZE, %edx          # count
    mov     $SYS_READ, %eax
    syscall
    cmp     $HEADER_SIZE, %rax
    jne     err_header

.header_ok:
    # Parse header fields
    # offset  4: version (1 byte)
    # offset  5: arch (1 byte) — should be 0x01 for x86_64
    # offset  6: vm_offset (4 bytes LE)
    # offset 10: vm_size (4 bytes LE)
    # offset 14: bc_offset (4 bytes LE)
    # offset 18: bc_size (4 bytes LE)
    # offset 22: kn_offset (4 bytes LE)
    # offset 26: kn_size (4 bytes LE)
    # offset 30: flags (2 bytes LE)

    movl    14(%rsp), %eax              # bc_offset
    push    %rax                        # save bc_offset on CPU stack
    movl    18(%rsp), %eax              # bc_size (now at 18+8 because of push)
    # Fix: header is still on stack, bc_size at original offset 18
    # After push, stack is: [bc_offset][header...]
    # So bc_size is at 8+18(%rsp) = 26(%rsp)
    pop     %rax                        # get bc_offset back
    push    %rax                        # re-push it
    movl    26(%rsp), %ebx              # bc_size from header (offset 18 + 8 for pushed bc_offset)
    # Actually let's re-read: header starts at 8(%rsp) now
    # bc_offset is at 0(%rsp), header at 8(%rsp)
    # bc_size field = header+18 = 8+18(%rsp) = 26(%rsp)
    # This is getting complex. Simpler approach:
    pop     %rax                        # bc_offset → rax
    movl    18(%rsp), %ebx              # bc_size from header (stack still has header)
    # Read flags (offset 30 in header) before freeing
    movzwl  30(%rsp), %ecx             # flags (u16 LE)
    lea     bc_format(%rip), %rdx
    test    $1, %ecx                    # bit 0 = codegen format?
    jz      .fmt_irrs
    movb    $1, (%rdx)
    jmp     .fmt_done
.fmt_irrs:
    movb    $0, (%rdx)
.fmt_done:
    add     $HEADER_SIZE, %rsp          # free header from stack

    # Save bc_offset on stack for later (mmap clobbers r8)
    push    %rax                        # save bc_offset

    # ── 2. mmap the bytecode section ─────────────────────────────────
    # We'll read the whole bytecode into heap memory
    # First, allocate VM stack
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi                  # addr = NULL
    mov     $STACK_SIZE, %esi           # length = 1 MB
    mov     $PROT_RW, %edx             # PROT_READ | PROT_WRITE
    mov     $MAP_PRIV_ANON, %r10d      # MAP_PRIVATE | MAP_ANONYMOUS
    mov     $-1, %r8d                   # fd = -1
    xor     %r9d, %r9d                  # offset = 0
    syscall
    test    %rax, %rax
    js      err_mmap
    mov     %rax, %r14                  # r14 = VM stack base
    add     $STACK_SIZE, %r14           # stack grows downward
    # Save stack top for debug
    mov     %r14, (%rax)

    # Allocate heap
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi
    mov     $HEAP_SIZE, %esi            # 16 MB
    mov     $PROT_RW, %edx
    mov     $MAP_PRIV_ANON, %r10d
    mov     $-1, %r8d
    xor     %r9d, %r9d
    syscall
    test    %rax, %rax
    js      err_mmap
    mov     %rax, %r15                  # r15 = heap base (bump allocator)

    # ── 3. Read bytecode from file into heap ─────────────────────────
    # Recover bc_offset from stack
    pop     %rsi                        # bc_offset
    # lseek to bc_offset
    mov     %rbp, %rdi                  # fd
    xor     %edx, %edx                  # SEEK_SET = 0
    mov     $8, %eax                    # sys_lseek
    syscall

    # Read bytecode into start of heap
    mov     %rbp, %rdi                  # fd
    mov     %r15, %rsi                  # buf = heap base
    mov     %rbx, %rdx                  # count = bc_size
    mov     $SYS_READ, %eax
    syscall
    cmp     %rbx, %rax
    jne     err_read_bc

    # Setup VM registers
    mov     %r15, %r12                  # r12 = bytecode base
    xor     %r13d, %r13d               # r13 = PC = 0
    add     %rbx, %r15                  # bump heap past bytecode
    # Save boot bytecode base + size for nested calls
    lea     boot_bc_base(%rip), %rax
    mov     %r12, (%rax)
    lea     boot_bc_size(%rip), %rax
    mov     %rbx, (%rax)

    # Close file
    mov     %rbp, %rdi
    mov     $SYS_CLOSE, %eax
    syscall

    # ── 4. Initialize variable table ─────────────────────────────────
    call    var_table_init

    # ── 5. Enter VM loop ─────────────────────────────────────────────
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Task 2: VM dispatch loop
# ═══════════════════════════════════════════════════════════════════════════

vm_loop:
    # Bounds check
    cmp     %rbx, %r13
    jae     op_halt                     # PC >= bc_size → done

    # Fetch opcode
    movzbl  (%r12, %r13), %eax         # al = bytecode[PC]
    inc     %r13                        # PC++

    # Check format flag (set during header parse)
    lea     bc_format(%rip), %rcx
    cmpb    $1, (%rcx)
    je      dispatch_codegen

    # ── ir.rs format dispatch ──
    # Range 0x00-0x0F: basic ops
    cmp     $0x10, %al
    jb      dispatch_basic

    # Range 0x10-0x14: relation ops
    cmp     $0x15, %al
    jb      dispatch_relation

    # Range 0x20-0x22: control flow
    cmp     $0x20, %al
    jb      op_unknown
    cmp     $0x23, %al
    jb      dispatch_control

    # Range 0x30-0x39: data ops
    cmp     $0x30, %al
    jb      op_unknown
    cmp     $0x3A, %al
    jb      dispatch_data

    # 0x3A: CallBuiltin(u8) — skip builtin_id byte, treat as no-op
    # ASM VM delegates to boot bytecode functions instead.
    # Format: [0x3A][builtin_id:1] = 2 bytes
    cmp     $0x3A, %al
    jne     .Lnot_call_builtin
    inc     %r13                        # skip builtin_id byte
    jmp     vm_loop
.Lnot_call_builtin:

    # Range 0x40-0x42: device ops (stub)
    cmp     $0x40, %al
    jb      op_unknown
    cmp     $0x43, %al
    jb      op_nop

    # Range 0x50-0x53: file ops
    cmp     $0x50, %al
    jb      op_unknown
    cmp     $0x54, %al
    jb      dispatch_file

    # Everything else
    jmp     op_unknown

# ── codegen.ol format dispatch (tags 0x01-0x24) ─────────────────────────
dispatch_codegen:
    # 0x3A = CallBuiltin(id) — dispatch to builtin by ID
    cmp     $0x3A, %al
    jne     .cg_not_builtin
    movzbl  (%r12, %r13), %eax         # read builtin ID
    inc     %r13                        # skip ID byte
    # Dispatch by builtin ID (matches BID_* constants from ir.rs)
    cmp     $6, %al                     # BID_HYP_ADD
    je      .call_add
    cmp     $7, %al                     # BID_HYP_SUB
    je      .call_sub
    cmp     $8, %al                     # BID_HYP_MUL
    je      .call_mul
    cmp     $9, %al                     # BID_HYP_DIV
    je      .call_div
    cmp     $0, %al                     # BID_EQ
    je      .bid_eq
    cmp     $1, %al                     # BID_CMP_LT
    je      .bid_lt
    cmp     $2, %al                     # BID_CMP_GT
    je      .bid_gt
    cmp     $3, %al                     # BID_CMP_LE
    je      .bid_le
    cmp     $4, %al                     # BID_CMP_GE
    je      .bid_ge
    cmp     $5, %al                     # BID_CMP_NE
    je      .bid_ne
    cmp     $10, %al                    # BID_LOGIC_NOT
    je      .bid_logic_not
    cmp     $11, %al                    # BID_ASSERT_TRUTH
    je      .bid_eq
    # All others: pop 2 args, push empty result
    # (most builtins are binary ops: pop 2, push 1)
    add     $16, %r14                   # pop arg 2
    # Replace top (arg 1) with empty
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.bid_eq:
    xor     %ecx, %ecx
    jmp     .call_eq
.bid_lt:
    xor     %ecx, %ecx
    jmp     .call_lt
.bid_gt:
    xor     %ecx, %ecx
    jmp     .call_gt
.bid_le:
    xor     %ecx, %ecx
    jmp     .call_le
.bid_ge:
    xor     %ecx, %ecx
    jmp     .call_ge
.bid_ne:
    xor     %ecx, %ecx
    jmp     .call_ne
.bid_logic_not:
    # Invert: empty → 1.0, non-empty → empty
    mov     8(%r14), %rax
    test    %rax, %rax
    jz      .bid_not_was_empty
    # Was non-empty → make empty
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.bid_not_was_empty:
    # Was empty → make 1.0
    mov     $0x3FF0000000000000, %rax   # 1.0 as f64
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_is_keyword:
    # __str_is_keyword: pop string, check against keyword list
    # For now: push false (not a keyword) — stub
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.cg_not_builtin:
    lea     codegen_table(%rip), %rcx
    movzbl  %al, %eax
    cmp     $0x2A, %al
    jae     op_unknown
    movslq  (%rcx, %rax, 4), %rdx
    add     %rcx, %rdx
    jmp     *%rdx

.section .rodata
.align 4
codegen_table:
    .long op_unknown    - codegen_table # 0x00 (unused)
    .long cg_push       - codegen_table # 0x01 Push (name string, u16 len)
    .long op_load       - codegen_table # 0x02 Load
    .long op_lca        - codegen_table # 0x03 Lca
    .long op_edge       - codegen_table # 0x04 Edge
    .long op_query      - codegen_table # 0x05 Query
    .long op_emit       - codegen_table # 0x06 Emit
    .long op_call       - codegen_table # 0x07 Call
    .long op_ret        - codegen_table # 0x08 Ret
    .long op_jmp        - codegen_table # 0x09 Jmp
    .long op_jz         - codegen_table # 0x0A Jz
    .long op_dup        - codegen_table # 0x0B Dup
    .long op_pop        - codegen_table # 0x0C Pop
    .long op_swap       - codegen_table # 0x0D Swap
    .long op_loop       - codegen_table # 0x0E Loop
    .long op_halt       - codegen_table # 0x0F Halt
    .long op_nop        - codegen_table # 0x10 Dream
    .long op_nop        - codegen_table # 0x11 Stats
    .long op_nop        - codegen_table # 0x12 Nop
    .long op_store      - codegen_table # 0x13 Store
    .long op_load_local - codegen_table # 0x14 LoadLocal
    .long op_push_num   - codegen_table # 0x15 PushNum
    .long op_nop        - codegen_table # 0x16 Fuse
    .long op_nop        - codegen_table # 0x17 ScopeBegin
    .long op_nop        - codegen_table # 0x18 ScopeEnd
    .long cg_push_mol   - codegen_table # 0x19 PushMol (codegen: 2 bytes u16 LE)
    .long op_try_begin  - codegen_table # 0x1A TryBegin
    .long op_catch_end  - codegen_table # 0x1B CatchEnd
    .long op_store_upd  - codegen_table # 0x1C StoreUpdate
    .long op_nop        - codegen_table # 0x1D Trace
    .long op_nop        - codegen_table # 0x1E Inspect
    .long op_nop        - codegen_table # 0x1F Assert
    .long op_nop        - codegen_table # 0x20 TypeOf
    .long op_nop        - codegen_table # 0x21 Why
    .long op_nop        - codegen_table # 0x22 Explain
    .long op_nop        - codegen_table # 0x23 Ffi (stub)
    .long cg_call_closure - codegen_table # 0x24 CallClosure
    .long cg_closure      - codegen_table # 0x25 Closure
    .long op_load_reg     - codegen_table # 0x26 LoadReg(u8 slot)
    .long op_store_reg    - codegen_table # 0x27 StoreReg(u8 slot)
    .long op_enter_frame  - codegen_table # 0x28 EnterFrame(u8 num_slots)
    .long op_leave_frame  - codegen_table # 0x29 LeaveFrame
.section .text

# codegen Push: [chain_len:2 u16 LE][chain_bytes:N] → push raw bytes as chain
cg_push:
    # Read u16 molecule count
    movzwl  (%r12, %r13), %ecx         # u16 LE mol_count
    add     $2, %r13

    # Empty chain (mol_count == 0): just push empty onto stack
    test    %ecx, %ecx
    jnz     .cg_push_nonempty
    sub     $16, %r14
    movq    $0, (%r14)                  # ptr = 0
    movq    $0, 8(%r14)                 # len = 0
    jmp     vm_loop

.cg_push_nonempty:
    # Byte count = mol_count * 2 (each molecule = u16)
    mov     %ecx, %edx
    shl     $1, %edx                    # byte_count = mol_count * 2

    # Copy molecule bytes to heap
    mov     %r15, %rdi                  # dest = heap
    lea     (%r12, %r13), %rsi         # src = bytecode
    push    %rcx
    push    %rdx
    call    memcpy_small
    pop     %rdx
    pop     %rcx

    # Push (ptr, mol_count) onto VM stack
    sub     $16, %r14
    mov     %r15, (%r14)                # ptr to molecule data
    mov     %rcx, 8(%r14)               # mol_count (NOT byte count)
    add     %rdx, %r15                  # bump heap by byte_count
    add     %rdx, %r13                  # PC += byte_count
    jmp     vm_loop

# codegen Closure: [param_count:1][body_len:4]
# Creates a closure marker on VM stack, jumps over body.
# Marker: [ptr=body_pc as f64 bits][len=0xFFFFFFFFFFFFFFFE] (closure tag)
.equ CLOSURE_MARKER, -2

cg_closure:
    movzbl  (%r12, %r13), %ecx         # param_count
    inc     %r13
    movl    (%r12, %r13), %eax          # body_len (u32 LE)
    add     $4, %r13

    # Push closure marker: body_pc in ptr, param_count in len high bits
    # Encode: ptr = body_pc (current PC = start of body), len = CLOSURE_MARKER
    sub     $16, %r14
    # Store body_pc: if in boot context → relative PC, if in eval → absolute address
    # This allows op_call to distinguish boot vs eval closures
    lea     boot_bc_base(%rip), %rdx
    cmp     (%rdx), %r12
    je      .cg_closure_boot
    # Eval context: store absolute address (r12 + r13) with high bit set as flag
    lea     (%r12, %r13), %rdx
    bts     $63, %rdx                   # set bit 63 as "eval closure" flag
    mov     %rdx, (%r14)
    jmp     .cg_closure_done
.cg_closure_boot:
    mov     %r13, (%r14)                # boot: relative body_pc
.cg_closure_done:
    # Store closure marker with param_count encoded
    movq    $CLOSURE_MARKER, 8(%r14)    # len = closure tag
    # Save param_count in the byte after the marker
    # Actually, encode param_count in the closure marker itself:
    # We use a trick: len = 0xFFFFFFFFFFFFFFFE | (param_count << 48)
    # Simpler: just store param_count alongside. Use top byte of len.
    shl     $48, %rcx                   # param_count << 48
    orq     $CLOSURE_MARKER, %rcx       # combine with marker
    mov     %rcx, 8(%r14)              # store combined

    # Jump over body
    add     %eax, %r13d                 # PC += body_len
    jmp     vm_loop

# ── Register frame opcodes (Julia/Lua-style) ──────────────────────────

# EnterFrame(u8 num_slots): allocate register frame with N slots
# Format: [0x28][num_slots:1]
# Saves old reg_cur_frame + old reg_frame_ptr on CPU stack.
op_enter_frame:
    movzbl  (%r12, %r13), %eax         # num_slots
    inc     %r13
    # Save old state on CPU stack
    lea     reg_cur_frame(%rip), %rcx
    push    (%rcx)                      # save old cur_frame
    lea     reg_frame_ptr(%rip), %rdx
    push    (%rdx)                      # save old frame_ptr
    # New frame base = current frame_ptr
    mov     (%rdx), %rdi               # new frame base
    mov     %rdi, (%rcx)               # reg_cur_frame = new base
    # Bump frame_ptr past this frame
    movzbl  %al, %eax
    shl     $4, %eax                   # bytes = num_slots * 16
    add     %rax, %rdi
    mov     %rdi, (%rdx)               # frame_ptr += N*16
    # Zero the frame (fast: use rep stosq)
    mov     (%rcx), %rdi               # frame base
    shr     $3, %eax                   # qword count = bytes / 8
    mov     %eax, %ecx
    xor     %eax, %eax
    rep     stosq
    jmp     vm_loop

# LeaveFrame: deallocate current register frame
# Format: [0x29]
op_leave_frame:
    pop     %rax                        # restore old frame_ptr
    lea     reg_frame_ptr(%rip), %rcx
    mov     %rax, (%rcx)
    pop     %rax                        # restore old cur_frame
    lea     reg_cur_frame(%rip), %rcx
    mov     %rax, (%rcx)
    jmp     vm_loop

# LoadReg(u8 slot): push register value onto VM stack
# Format: [0x26][slot:1]
op_load_reg:
    movzbl  (%r12, %r13), %eax         # slot
    inc     %r13
    lea     reg_cur_frame(%rip), %rcx
    mov     (%rcx), %rcx               # frame base
    shl     $4, %eax                   # byte offset = slot * 16
    sub     $16, %r14                  # push onto VM stack
    mov     (%rcx, %rax), %rdx         # val_ptr
    mov     %rdx, (%r14)
    mov     8(%rcx, %rax), %rdx        # val_len
    mov     %rdx, 8(%r14)
    jmp     vm_loop

# StoreReg(u8 slot): pop VM stack into register
# Format: [0x27][slot:1]
op_store_reg:
    movzbl  (%r12, %r13), %eax         # slot
    inc     %r13
    lea     reg_cur_frame(%rip), %rcx
    mov     (%rcx), %rcx               # frame base
    shl     $4, %eax                   # byte offset = slot * 16
    mov     (%r14), %rdx               # val_ptr from VM stack
    mov     %rdx, (%rcx, %rax)
    mov     8(%r14), %rdx              # val_len from VM stack
    mov     %rdx, 8(%rcx, %rax)
    add     $16, %r14                  # pop VM stack
    jmp     vm_loop

# codegen CallClosure: [name_len:1][name:N][arity:1]
# Pops closure marker + args from stack, jumps to body PC.
# Uses CPU stack for return address.
cg_call_closure:
    movzbl  (%r12, %r13), %ecx         # name_len
    inc     %r13
    # Hash the name to find the closure in var table
    lea     (%r12, %r13), %rdi         # name ptr
    mov     %ecx, %edx                  # name len
    push    %rcx
    call    hash_name                   # → rax = name hash
    pop     %rcx
    add     %rcx, %r13                  # PC += name_len
    movzbl  (%r12, %r13), %ecx         # arity
    inc     %r13                        # PC past arity byte

    # Look up variable by hash → get closure marker
    push    %rcx                        # save arity
    call    var_load_hash               # rax=ptr, rdx=len
    pop     %rcx                        # restore arity

    # Check if it's a closure marker (len has CLOSURE_MARKER bits)
    mov     %rdx, %rsi
    and     $0xFFFF, %esi               # low 16 bits
    cmp     $CLOSURE_MARKER, %si        # == 0xFFFE?
    jne     .cc_not_closure

    # It's a closure — rax = body_pc, rdx has param_count in high bits
    # Extract body_pc
    mov     %rax, %rdi                  # body_pc

    # Save return context: PC + bytecode base
    push    %r12                        # save current r12
    push    %r13                        # save return PC

    # Check bit 63: eval closure (absolute address) vs boot closure (relative PC)
    bt      $63, %rdi
    jc      .cc_eval_closure

    # Boot closure: switch r12 to boot bytecode base, PC = relative body_pc
    lea     boot_bc_base(%rip), %rax
    mov     (%rax), %r12
    mov     %edi, %r13d                 # PC = body_pc (relative)
    jmp     vm_loop

.cc_eval_closure:
    # Eval closure: body_pc is absolute address with bit 63 set
    btr     $63, %rdi                   # clear bit 63 → absolute address
    # Use saved eval_bc_base as r12, compute body offset for r13
    lea     eval_bc_base(%rip), %rax
    mov     (%rax), %r12               # r12 = eval buffer base
    sub     %r12, %rdi                 # offset = absolute - base
    mov     %edi, %r13d               # PC = body offset within eval buffer
    jmp     vm_loop

.cc_not_closure:
    # Not a closure — push empty (like function not found)
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ── Dispatch sub-tables ──────────────────────────────────────────────────

dispatch_basic:
    lea     basic_table(%rip), %rcx
    movzbl  %al, %eax
    movslq  (%rcx, %rax, 4), %rdx
    add     %rcx, %rdx
    jmp     *%rdx

.section .rodata
.align 4
basic_table:
    .long op_nop        - basic_table   # 0x00 NOP
    .long op_lca        - basic_table   # 0x01 LCA
    .long op_emit       - basic_table   # 0x02 EMIT
    .long op_ret        - basic_table   # 0x03 RET
    .long op_dup        - basic_table   # 0x04 DUP
    .long op_pop        - basic_table   # 0x05 POP
    .long op_swap       - basic_table   # 0x06 SWAP
    .long op_halt       - basic_table   # 0x07 HALT
    .long op_nop        - basic_table   # 0x08 DREAM (stub)
    .long op_nop        - basic_table   # 0x09 STATS (stub)
    .long op_nop        - basic_table   # 0x0A FUSE (stub)
    .long op_nop        - basic_table   # 0x0B TRACE (stub)
    .long op_nop        - basic_table   # 0x0C INSPECT (stub)
    .long op_nop        - basic_table   # 0x0D ASSERT (stub)
    .long op_nop        - basic_table   # 0x0E TYPEOF (stub)
    .long op_nop        - basic_table   # 0x0F WHY (stub)
.section .text

dispatch_relation:
    sub     $0x10, %al
    cmp     $0x00, %al
    je      op_edge
    cmp     $0x01, %al
    je      op_query
    jmp     op_nop                      # EXPLAIN, SCOPE_BEGIN, SCOPE_END = nop

dispatch_control:
    sub     $0x20, %al
    cmp     $0x00, %al
    je      op_loop
    cmp     $0x01, %al
    je      op_jmp
    cmp     $0x02, %al
    je      op_jz
    jmp     op_unknown

dispatch_data:
    sub     $0x30, %al
    lea     data_table(%rip), %rcx
    movzbl  %al, %eax
    cmp     $0x0A, %al
    jae     op_unknown
    movslq  (%rcx, %rax, 4), %rdx
    add     %rcx, %rdx
    jmp     *%rdx

.section .rodata
.align 4
data_table:
    .long op_push       - data_table    # 0x30 PUSH
    .long op_load       - data_table    # 0x31 LOAD
    .long op_call       - data_table    # 0x32 CALL
    .long op_store      - data_table    # 0x33 STORE
    .long op_load_local - data_table    # 0x34 LOAD_LOCAL
    .long op_push_num   - data_table    # 0x35 PUSH_NUM
    .long op_push_mol   - data_table    # 0x36 PUSH_MOL
    .long op_try_begin  - data_table    # 0x37 TRY_BEGIN
    .long op_catch_end  - data_table    # 0x38 CATCH_END
    .long op_store_upd  - data_table    # 0x39 STORE_UPDATE
.section .text

dispatch_file:
    sub     $0x50, %al
    cmp     $0x00, %al
    je      op_nop                      # FFI = nop in standalone
    cmp     $0x01, %al
    je      op_file_read
    cmp     $0x02, %al
    je      op_file_write
    cmp     $0x03, %al
    je      op_file_append
    jmp     op_unknown

# ═══════════════════════════════════════════════════════════════════════════
# Task 3: Stack operations
# ═══════════════════════════════════════════════════════════════════════════

op_push:
    # [0x30, chain_len_u8, chain_bytes...]
    movzbl  (%r12, %r13), %ecx         # ecx = chain_len
    inc     %r13                        # PC past len byte

    # Copy chain bytes to heap
    mov     %r15, %rdi                  # dest = heap_ptr
    lea     (%r12, %r13), %rsi         # src = bytecode + PC
    mov     %ecx, %edx                  # count
    push    %rcx                        # save len
    call    memcpy_small
    pop     %rcx

    # Push (ptr, len) onto VM stack
    sub     $16, %r14
    mov     %r15, (%r14)                # ptr
    mov     %rcx, 8(%r14)               # len
    add     %rcx, %r15                  # bump heap
    add     %rcx, %r13                  # PC += chain_len
    jmp     vm_loop

op_push_num:
    # [0x35, f64_le_bytes[0-7]]
    sub     $16, %r14
    mov     (%r12, %r13), %rax
    mov     %rax, (%r14)                # store f64 bits
    movq    $F64_MARKER, 8(%r14)        # len = marker (this is f64)
    add     $8, %r13                    # PC += 8
    jmp     vm_loop

op_push_mol:
    # ir.rs format: [0x36, S, R, V, A, T] — 5 bytes
    mov     %r15, %rdi                  # dest = heap
    lea     (%r12, %r13), %rsi         # src
    mov     $5, %edx
    call    memcpy_small

    sub     $16, %r14
    mov     %r15, (%r14)                # ptr to 5 bytes on heap
    movq    $5, 8(%r14)                 # len = 5
    add     $5, %r15                    # bump heap
    add     $5, %r13                    # PC += 5
    jmp     vm_loop

cg_push_mol:
    # codegen format: [0x19, lo:1, hi:1] — 2 bytes (packed u16 LE)
    # Read u16, unpack to 5 dimension bytes, write to heap
    movzwl  (%r12, %r13), %eax         # packed u16 LE
    add     $2, %r13                    # PC += 2
    # Unpack: S=(bits>>12)&0xF, R=(bits>>8)&0xF, V=(bits>>5)&7, A=(bits>>2)&7, T=bits&3
    mov     %eax, %ecx
    shr     $12, %ecx
    movb    %cl, (%r15)                 # S
    mov     %eax, %ecx
    shr     $8, %ecx
    and     $0xF, %ecx
    movb    %cl, 1(%r15)               # R
    mov     %eax, %ecx
    shr     $5, %ecx
    and     $7, %ecx
    movb    %cl, 2(%r15)               # V
    mov     %eax, %ecx
    shr     $2, %ecx
    and     $7, %ecx
    movb    %cl, 3(%r15)               # A
    mov     %eax, %ecx
    and     $3, %ecx
    movb    %cl, 4(%r15)               # T
    # Push chain (5 bytes on heap, mol_count=5)
    sub     $16, %r14
    mov     %r15, (%r14)
    movq    $5, 8(%r14)
    add     $5, %r15
    jmp     vm_loop

op_dup:
    mov     (%r14), %rax
    mov     8(%r14), %rcx
    sub     $16, %r14
    mov     %rax, (%r14)
    mov     %rcx, 8(%r14)
    jmp     vm_loop

op_pop:
    add     $16, %r14
    jmp     vm_loop

op_swap:
    mov     (%r14), %rax
    mov     8(%r14), %rcx
    mov     16(%r14), %rdx
    mov     24(%r14), %rsi
    mov     %rdx, (%r14)
    mov     %rsi, 8(%r14)
    mov     %rax, 16(%r14)
    mov     %rcx, 24(%r14)
    jmp     vm_loop

op_load:
op_load_local:
    # [0x31/0x34, name_len_u8, name_bytes...]
    movzbl  (%r12, %r13), %ecx         # name_len
    inc     %r13
    lea     (%r12, %r13), %rdi         # name ptr
    mov     %ecx, %edx
    # Inline hash cache check
    lea     hn_cache(%rip), %rax
    mov     %rdi, %rsi
    and     $0xFF, %esi
    shl     $4, %esi
    cmp     %rdi, (%rax, %rsi)
    jne     .op_load_hash_miss
    mov     8(%rax, %rsi), %rax
    jmp     .op_load_have_hash
.op_load_hash_miss:
    sub     $8, %rsp
    push    %rcx
    call    hash_name
    pop     %rcx
    add     $8, %rsp
.op_load_have_hash:
    add     %rcx, %r13
    # Search var table by hash
    call    var_load_hash               # rax=hash → rax=ptr, rdx=len
    sub     $16, %r14
    mov     %rax, (%r14)
    mov     %rdx, 8(%r14)
    jmp     vm_loop

op_store:
    # [0x33, name_len_u8, name_bytes...] — frame-limited (creates local)
    movzbl  (%r12, %r13), %ecx         # name_len
    inc     %r13
    lea     (%r12, %r13), %rdi         # name ptr
    mov     %ecx, %edx
    mov     (%r14), %r8
    mov     8(%r14), %r9
    add     $16, %r14
    push    %rcx
    push    %r8
    push    %r9
    lea     hn_cache(%rip), %rax
    mov     %rdi, %rsi
    and     $0xFF, %esi
    shl     $4, %esi
    cmp     %rdi, (%rax, %rsi)
    jne     .op_store_hash_miss_local
    mov     8(%rax, %rsi), %rax
    jmp     .op_store_have_hash_local
.op_store_hash_miss_local:
    call    hash_name
.op_store_have_hash_local:
    pop     %r9
    pop     %r8
    pop     %rcx
    add     %rcx, %r13
    # Also register in fn_registry if value is a closure
    push    %rax
    push    %r8
    push    %r9
    mov     %r9, %rcx
    and     $0xFFFF, %ecx
    cmp     $CLOSURE_MARKER, %cx
    jne     .op_store_not_closure
    call    fn_reg_store               # register closure in fn_registry
.op_store_not_closure:
    pop     %r9
    pop     %r8
    pop     %rax
    call    var_store_hash              # also store in var_table (backward compat)
    jmp     vm_loop

op_store_upd:
    # [0x39, name_len_u8, name_bytes...] — FULL search (updates existing, Julia-style)
    movzbl  (%r12, %r13), %ecx         # name_len
    inc     %r13
    lea     (%r12, %r13), %rdi         # name ptr
    mov     %ecx, %edx
    mov     (%r14), %r8
    mov     8(%r14), %r9
    add     $16, %r14
    push    %rcx
    push    %r8
    push    %r9
    lea     hn_cache(%rip), %rax
    mov     %rdi, %rsi
    and     $0xFF, %esi
    shl     $4, %esi
    cmp     %rdi, (%rax, %rsi)
    jne     .osu_hash_miss
    mov     8(%rax, %rsi), %rax
    jmp     .osu_have_hash
.osu_hash_miss:
    call    hash_name
.osu_have_hash:
    pop     %r9
    pop     %r8
    pop     %rcx
    add     %rcx, %r13
    call    var_store_global            # FULL table search (not frame-limited)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Task 4: Control flow
# ═══════════════════════════════════════════════════════════════════════════

op_jmp:
    # [0x21, target_u32_le]
    movl    (%r12, %r13), %eax          # target
    # Check for backward jump (hot loop detection) — eval context only
    cmp     %r13d, %eax
    jae     .jmp_forward
    # Skip for boot context
    push    %rcx
    lea     boot_bc_base(%rip), %rcx
    cmp     (%rcx), %r12
    pop     %rcx
    je      .jmp_forward
    # Count backward jumps and trigger JIT
    push    %rcx
    push    %rdx
    lea     loop_counters(%rip), %rcx
    mov     %eax, %edx
    and     $0xFF, %edx
    incl    (%rcx, %rdx, 4)
    cmpl    $8, (%rcx, %rdx, 4)
    pop     %rdx
    pop     %rcx
    jne     .jmp_forward
    # Hot loop! Save ALL VM state, call try_jit_loop, restore
    push    %rax                        # save target PC
    push    %rbx
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    mov     %eax, %edi                  # arg1: loop_start
    lea     4(%r13), %esi               # arg2: loop_end (past Jmp operand)
    call    try_jit_loop
    mov     %rax, %rdi                  # native_ptr (or 0)
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rbx
    test    %rdi, %rdi
    jz      .loop_jit_skip
    # JIT success — execute native loop
    pop     %rax                        # discard target PC
    push    %rbx
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    call    *%rdi
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rbx
    add     $4, %r13                    # skip past Jmp's u32 target
    jmp     vm_loop
.loop_jit_skip:
    pop     %rax                        # restore target PC
.jmp_forward:
    mov     %eax, %r13d                 # PC = target
    jmp     vm_loop

op_jz:
    # [0x22, target_u32_le]
    movl    (%r12, %r13), %eax          # target
    add     $4, %r13                    # PC past target bytes

    # Pop condition from stack
    mov     8(%r14), %rcx              # len
    mov     (%r14), %rdx               # ptr/value
    add     $16, %r14                  # pop

    # f64 check: if marker, compare f64 bits to 0.0
    cmp     $F64_MARKER, %rcx
    je      .jz_check_f64
    # chain check: len == 0 means empty/false
    test    %rcx, %rcx
    jz      .jz_take
    jmp     vm_loop
.jz_check_f64:
    test    %rdx, %rdx                  # f64 bits == 0?
    jz      .jz_take
    jmp     vm_loop
.jz_take:
    mov     %eax, %r13d                 # PC = target
    jmp     vm_loop

op_loop:
    # [0x20, n_u32_le] — loop n times (simplified: just read and skip)
    movl    (%r12, %r13), %eax
    add     $4, %r13
    # Stub: treat as nop (full loop needs loop counter in scope)
    jmp     vm_loop

op_call:
    # [0x32/0x07, name_len_u8, name_bytes...]
    movzbl  (%r12, %r13), %ecx         # name_len
    inc     %r13
    lea     (%r12, %r13), %rdi         # name ptr

    # Hash dispatch: use cache (inline, no call overhead)
    mov     %ecx, %edx
    push    %rcx
    # Inline cache check (avoid call hash_name overhead for hot paths)
    lea     hn_cache(%rip), %rax
    mov     %rdi, %rsi
    and     $0xFF, %esi
    shl     $4, %esi
    cmp     %rdi, (%rax, %rsi)
    jne     .op_call_hash_miss
    mov     8(%rax, %rsi), %rax        # cache hit!
    jmp     .op_call_have_hash
.op_call_hash_miss:
    call    hash_name
.op_call_have_hash:
    pop     %rcx

    # Profile: increment call counter for this hash
    # Direct-mapped: profile_counters[hash & 0xFF] += 1
    # profile_hashes[hash & 0xFF] = hash (last writer wins)
    push    %rax
    push    %rdx
    lea     profile_counters(%rip), %rdx
    mov     %rax, %rsi
    and     $0xFF, %esi
    incl    (%rdx, %rsi, 4)            # counter[hash & 255]++
    lea     profile_hashes(%rip), %rdx
    mov     %rax, (%rdx, %rsi, 8)     # record hash
    # Auto-JIT: if counter == 64, check if this hash has a known JIT template
    lea     profile_counters(%rip), %rdx
    cmpl    $8, (%rdx, %rsi, 4)
    jne     .no_auto_jit
    # Check JIT cache first — if already JIT'd, skip
    push    %rax
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi
    shl     $4, %esi
    cmp     %rax, (%rdx, %rsi)
    pop     %rax
    je      .no_auto_jit               # already in JIT cache
    # Check known patterns: fib, fact, sum
    movabs  $0xDC9E8818FEC94CB6, %rdx  # fib hash
    cmp     %rdx, %rax
    je      .auto_jit_fib
    movabs  $0xEF234A791696AFFF, %rdx  # fact hash
    cmp     %rdx, %rax
    je      .auto_jit_fact
    movabs  $0x82719E195D0FC4A8, %rdx  # sum hash
    cmp     %rdx, %rax
    je      .auto_jit_sum
.no_auto_jit:
    pop     %rdx
    pop     %rax


    # === BUILTIN JUMP TABLE DISPATCH ===
    # 256-slot table, O(1) avg lookup (was O(110) linear scan)
    mov     %rax, %rsi
    and     $0xFF, %esi
    lea     builtin_jump_table(%rip), %rdx
    jmp     *(%rdx, %rsi, 8)

    # -- Builtin chain handlers (collision resolution) --
.builtin_slot_1:
    movabs  $0x7800C290CB86CA01, %rdx
    cmp     %rdx, %rax
    je      .call_mul
    jmp     .call_not_builtin

.builtin_slot_2:
    movabs  $0x9AB0F28E471CF102, %rdx
    cmp     %rdx, %rax
    je      .call_mol_t
    jmp     .call_not_builtin

.builtin_slot_3:
    movabs  $0x7822B690CBA39C03, %rdx
    cmp     %rdx, %rax
    je      .call_mod
    jmp     .call_not_builtin

.builtin_slot_5:
    movabs  $0xEF8C08461494EA05, %rdx
    cmp     %rdx, %rax
    je      .call_bytes_get
    jmp     .call_not_builtin

.builtin_slot_6:
    movabs  $0xA5845725C8B01606, %rdx
    cmp     %rdx, %rax
    je      .call_file_read_bytes
    jmp     .call_not_builtin

.builtin_slot_10:
    movabs  $0xE5259429A45DFB0A, %rdx
    cmp     %rdx, %rax
    je      .call_ge
    jmp     .call_not_builtin

.builtin_slot_12:
    movabs  $0xECF7CD335641730C, %rdx
    cmp     %rdx, %rax
    je      .call_bit_shl
    jmp     .call_not_builtin

.builtin_slot_14:
    movabs  $0xBA79129DA012260E, %rdx
    cmp     %rdx, %rax
    je      .call_noop_pop2
    movabs  $0xF7ED721D453C820E, %rdx
    cmp     %rdx, %rax
    je      .call_jit_register
    movabs  $0x9C2E99FAD130770E, %rdx
    cmp     %rdx, %rax
    je      .call_jit_invalidate
    jmp     .call_not_builtin

.builtin_slot_15:
    movabs  $0x90134D214B786D0F, %rdx
    cmp     %rdx, %rax
    je      .call_array_set
    jmp     .call_not_builtin

.builtin_slot_18:
    movabs  $0xB56460DC788BF312, %rdx
    cmp     %rdx, %rax
    je      .call_array_range
    jmp     .call_not_builtin

.builtin_slot_20:
    movabs  $0xE653F1B42A305014, %rdx
    cmp     %rdx, %rax
    je      .call_aes_decrypt
    jmp     .call_not_builtin

.builtin_slot_21:
    movabs  $0xF28AFA91114A9D15, %rdx
    cmp     %rdx, %rax
    je      .call_sub
    jmp     .call_not_builtin

.builtin_slot_23:
    movabs  $0xAD9A9975BDE0C517, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_recv
    movabs  $0xF7B80C4062E48617, %rdx
    cmp     %rdx, %rax
    je      .call_regex_search
    jmp     .call_not_builtin

.builtin_slot_24:
    movabs  $0xB85069B47868C218, %rdx
    cmp     %rdx, %rax
    je      .call_munmap_file
    movabs  $0x45D4F29E55E01818, %rdx
    cmp     %rdx, %rax
    je      .call_profile_top
    movabs  $0x5CA205D82D643918, %rdx
    cmp     %rdx, %rax
    je      .call_mmap_exec
    jmp     .call_not_builtin

.builtin_slot_25:
    movabs  $0x90B5A88623267519, %rdx
    cmp     %rdx, %rax
    je      .call_sha512
    jmp     .call_not_builtin

.builtin_slot_26:
    movabs  $0x4878D01FD4BD5E1A, %rdx
    cmp     %rdx, %rax
    je      .call_utf8_cp
    jmp     .call_not_builtin

.builtin_slot_27:
    movabs  $0xB2E2F645F237541B, %rdx
    cmp     %rdx, %rax
    je      .call_bytes_new
    jmp     .call_not_builtin

.builtin_slot_28:
    movabs  $0x5FCEDD9B2804CF1C, %rdx
    cmp     %rdx, %rax
    je      .call_dict_set
    jmp     .call_not_builtin

.builtin_slot_32:
    movabs  $0x234A766F8747B620, %rdx
    cmp     %rdx, %rax
    je      .call_dict_keys
    jmp     .call_not_builtin

.builtin_slot_36:
    movabs  $0x3A1642345E94EA24, %rdx
    cmp     %rdx, %rax
    je      .call_str_find
    jmp     .call_not_builtin

.builtin_slot_41:
    movabs  $0x0275ACD27913EF29, %rdx
    cmp     %rdx, %rax
    je      .call_logic_not
    jmp     .call_not_builtin

.builtin_slot_44:
    movabs  $0xDACC10780A43732C, %rdx
    cmp     %rdx, %rax
    je      .call_heap_restore
    jmp     .call_not_builtin

.builtin_slot_45:
    movabs  $0x463483AB25CAB22D, %rdx
    cmp     %rdx, %rax
    je      .call_char_at
    jmp     .call_not_builtin

.builtin_slot_51:
    movabs  $0xC7631FF0030A6033, %rdx
    cmp     %rdx, %rax
    je      .call_noop_pop1
    jmp     .call_not_builtin

.builtin_slot_52:
    movabs  $0x656D6D860AA4E534, %rdx
    cmp     %rdx, %rax
    je      .call_sha256
    jmp     .call_not_builtin

.builtin_slot_56:
    movabs  $0x89275829827A6638, %rdx
    cmp     %rdx, %rax
    je      .call_aes_encrypt
    jmp     .call_not_builtin

.builtin_slot_59:
    movabs  $0xA51ED5C2BDE7E73B, %rdx
    cmp     %rdx, %rax
    je      .call_concat
    movabs  $0xA3A423F5F0BEDF3B, %rdx
    cmp     %rdx, %rax
    je      .call_heap_save
    movabs  $0xC3ADA5D879284B3B, %rdx
    cmp     %rdx, %rax
    je      .call_write_raw
    jmp     .call_not_builtin

.builtin_slot_74:
    movabs  $0x53416FC92E4E354A, %rdx
    cmp     %rdx, %rax
    je      .call_str_bytes
    movabs  $0xC4352F45FC06104A, %rdx
    cmp     %rdx, %rax
    je      .call_bytes_len
    jmp     .call_not_builtin

.builtin_slot_79:
    movabs  $0x0CA83B022A791B4F, %rdx
    cmp     %rdx, %rax
    je      .call_str_is_keyword
    movabs  $0x58F6E86D37FAFB4F, %rdx
    cmp     %rdx, %rax
    je      .call_enum_payload
    jmp     .call_not_builtin

.builtin_slot_86:
    movabs  $0x33AB401298E66956, %rdx
    cmp     %rdx, %rax
    je      .call_readdir
    jmp     .call_not_builtin

.builtin_slot_87:
    movabs  $0xA4B7BF6E270F2A57, %rdx
    cmp     %rdx, %rax
    je      .call_array_get
    jmp     .call_not_builtin

.builtin_slot_89:
    movabs  $0x59C2494311519859, %rdx
    cmp     %rdx, %rax
    je      .call_eq
    jmp     .call_not_builtin

.builtin_slot_91:
    movabs  $0x7DC336EFBBA1F85B, %rdx
    cmp     %rdx, %rax
    je      .call_f64_to_le_bytes
    jmp     .call_not_builtin

.builtin_slot_94:
    movabs  $0x4D895C13E625765E, %rdx
    cmp     %rdx, %rax
    je      .call_stdout_off
    jmp     .call_not_builtin

.builtin_slot_95:
    movabs  $0x79C07AA57E23765F, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_connect
    jmp     .call_not_builtin

.builtin_slot_97:
    movabs  $0xC8220FDA76231661, %rdx
    cmp     %rdx, %rax
    je      .call_file_write
    jmp     .call_not_builtin

.builtin_slot_101:
    movabs  $0xB810EF14843FBB65, %rdx
    cmp     %rdx, %rax
    je      .call_eval_bytecode
    jmp     .call_not_builtin

.builtin_slot_102:
    movabs  $0x3F9867C32BC1B166, %rdx
    cmp     %rdx, %rax
    je      .call_to_string
    jmp     .call_not_builtin

.builtin_slot_103:
    movabs  $0x05F32B674266E367, %rdx
    cmp     %rdx, %rax
    je      .call_simd_compose4
    jmp     .call_not_builtin

.builtin_slot_106:
    movabs  $0xEA1BDD6D065BF46A, %rdx
    cmp     %rdx, %rax
    je      .call_substr
    movabs  $0x9E3D368BE8B3606A, %rdx
    cmp     %rdx, %rax
    je      .call_ceil
    jmp     .call_not_builtin

.builtin_slot_109:
    movabs  $0xE811320261AE416D, %rdx
    cmp     %rdx, %rax
    je      .call_type_of
    movabs  $0x58DE00DC609CC76D, %rdx
    cmp     %rdx, %rax
    je      .call_mcp_suppress
    jmp     .call_not_builtin

.builtin_slot_110:
    movabs  $0xECF7E3335641986E, %rdx
    cmp     %rdx, %rax
    je      .call_bit_shr
    jmp     .call_not_builtin

.builtin_slot_113:
    movabs  $0xDD3AF5492696D871, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_close
    jmp     .call_not_builtin

.builtin_slot_116:
    movabs  $0x57252090B96BC074, %rdx
    cmp     %rdx, %rax
    je      .call_add
    jmp     .call_not_builtin

.builtin_slot_118:
    movabs  $0xE50D9329A4496776, %rdx
    cmp     %rdx, %rax
    je      .call_lt
    jmp     .call_not_builtin

.builtin_slot_119:
    movabs  $0x1C44C0F08C07EC77, %rdx
    cmp     %rdx, %rax
    je      .call_mmap_file
    jmp     .call_not_builtin

.builtin_slot_121:
    movabs  $0x5871F4465043D679, %rdx
    cmp     %rdx, %rax
    je      .call_bytes_set
    jmp     .call_not_builtin

.builtin_slot_125:
    movabs  $0xF8BB87C89678B27D, %rdx
    cmp     %rdx, %rax
    je      .call_array_push
    jmp     .call_not_builtin

.builtin_slot_126:
    movabs  $0x255314D8EC3C037E, %rdx
    cmp     %rdx, %rax
    je      .call_bytes_write_raw
    jmp     .call_not_builtin

.builtin_slot_127:
    movabs  $0xE5068029A443287F, %rdx
    cmp     %rdx, %rax
    je      .call_ne
    movabs  $0x74AEDE408DC6787F, %rdx
    cmp     %rdx, %rax
    je      .call_substr
    jmp     .call_not_builtin

.builtin_slot_130:
    movabs  $0x31F6E47C4AE5A082, %rdx
    cmp     %rdx, %rax
    je      .call_bit_or
    jmp     .call_not_builtin

.builtin_slot_131:
    movabs  $0x5210E36E8946BE83, %rdx
    cmp     %rdx, %rax
    je      .call_array_set
    movabs  $0x9AB0ED8E471CE883, %rdx
    cmp     %rdx, %rax
    je      .call_mol_s
    jmp     .call_not_builtin

.builtin_slot_134:
    movabs  $0x46A5ACF6ACD83186, %rdx
    cmp     %rdx, %rax
    je      .call_array_pop
    movabs  $0x9EEC1A86B3B21786, %rdx
    cmp     %rdx, %rax
    je      .call_sleep
    jmp     .call_not_builtin

.builtin_slot_144:
    movabs  $0xC8B4C99B63B3BB90, %rdx
    cmp     %rdx, %rax
    je      .call_dict_get
    jmp     .call_not_builtin

.builtin_slot_145:
    movabs  $0xF10E316E51D66791, %rdx
    cmp     %rdx, %rax
    je      .call_array_new
    jmp     .call_not_builtin

.builtin_slot_146:
    movabs  $0x231F02F69878E492, %rdx
    cmp     %rdx, %rax
    je      .call_len
    movabs  $0x4A94D43F636D9492, %rdx
    cmp     %rdx, %rax
    je      .call_array_with_cap
    jmp     .call_not_builtin

.builtin_slot_151:
    movabs  $0x9FC459281D29BE97, %rdx
    cmp     %rdx, %rax
    je      .call_array_push
    jmp     .call_not_builtin

.builtin_slot_153:
    movabs  $0x07515EC1BC3E1099, %rdx
    cmp     %rdx, %rax
    je      .call_char_code
    jmp     .call_not_builtin

.builtin_slot_156:
    movabs  $0x9AB0F08E471CED9C, %rdx
    cmp     %rdx, %rax
    je      .call_mol_v
    jmp     .call_not_builtin

.builtin_slot_158:
    movabs  $0xC3FD4316C34B889E, %rdx
    cmp     %rdx, %rax
    je      .call_noop_pop2
    jmp     .call_not_builtin

.builtin_slot_161:
    movabs  $0x385DDAACD4BF03A1, %rdx
    cmp     %rdx, %rax
    je      .call_enum_field
    movabs  $0x952B977C26455DA1, %rdx
    cmp     %rdx, %rax
    je      .call_timestamp
    jmp     .call_not_builtin

.builtin_slot_162:
    movabs  $0x3EA9B2DCA6EB46A2, %rdx
    cmp     %rdx, %rax
    je      .call_to_number
    movabs  $0x8072B20F985EC0A2, %rdx
    cmp     %rdx, %rax
    je      .call_line_offsets
    jmp     .call_not_builtin

.builtin_slot_164:
    movabs  $0xFC07F3E9B18D01A4, %rdx
    cmp     %rdx, %rax
    je      .call_profile_reset
    jmp     .call_not_builtin

.builtin_slot_165:
    movabs  $0x46B6739DEFF894A5, %rdx
    cmp     %rdx, %rax
    je      .call_struct_tag
    jmp     .call_not_builtin

.builtin_slot_166:
    movabs  $0x0D8DE844314D45A6, %rdx
    cmp     %rdx, %rax
    je      .call_skip_scope_restore
    jmp     .call_not_builtin

.builtin_slot_167:
    movabs  $0xFBE159BD1C6EBFA7, %rdx
    cmp     %rdx, %rax
    je      .call_str_trim
    jmp     .call_not_builtin

.builtin_slot_168:
    movabs  $0x3E279790AB8C5BA8, %rdx
    cmp     %rdx, %rax
    je      .call_div
    jmp     .call_not_builtin

.builtin_slot_171:
    movabs  $0xABFC7C5900FA2DAB, %rdx
    cmp     %rdx, %rax
    je      .call_mol_pack
    jmp     .call_not_builtin

.builtin_slot_172:
    movabs  $0xDEE1C66E474DB2AC, %rdx
    cmp     %rdx, %rax
    je      .call_len
    jmp     .call_not_builtin

.builtin_slot_173:
    movabs  $0x495BE76EAB8378AD, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_send
    jmp     .call_not_builtin

.builtin_slot_175:
    movabs  $0x185FC0C022E829AF, %rdx
    cmp     %rdx, %rax
    je      .call_floor
    jmp     .call_not_builtin

.builtin_slot_176:
    movabs  $0x476FB46E82B22CB0, %rdx
    cmp     %rdx, %rax
    je      .call_array_pop
    movabs  $0xFE7CDB1931FA9AB0, %rdx
    cmp     %rdx, %rax
    je      .call_regex_match
    jmp     .call_not_builtin

.builtin_slot_178:
    movabs  $0x061BEB9B86B295B2, %rdx
    cmp     %rdx, %rax
    je      .call_dict_new
    jmp     .call_not_builtin

.builtin_slot_185:
    movabs  $0x9AB0DF8E471CD0B9, %rdx
    cmp     %rdx, %rax
    je      .call_mol_a
    jmp     .call_not_builtin

.builtin_slot_186:
    movabs  $0x0E6B6C4BB0C674BA, %rdx
    cmp     %rdx, %rax
    je      .call_time
    jmp     .call_not_builtin

.builtin_slot_190:
    movabs  $0xE386A6329E2424BE, %rdx
    cmp     %rdx, %rax
    je      .call_substr
    movabs  $0x229C6ACCAE3B5DBE, %rdx
    cmp     %rdx, %rax
    je      .call_file_read
    movabs  $0x58485CB4691F98BE, %rdx
    cmp     %rdx, %rax
    je      .call_text_to_chain
    movabs  $0xA3E9AFF6513138BE, %rdx
    cmp     %rdx, %rax
    je      .call_chr
    jmp     .call_not_builtin

.builtin_slot_196:
    movabs  $0x6F0C4CBAC89C91C4, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_listen
    jmp     .call_not_builtin

.builtin_slot_200:
    movabs  $0x4E03A6338DB8ADC8, %rdx
    cmp     %rdx, %rax
    je      .call_bit_xor
    jmp     .call_not_builtin

.builtin_slot_207:
    movabs  $0x493ABC5CE7C7C7CF, %rdx
    cmp     %rdx, %rax
    je      .call_tcp_accept
    jmp     .call_not_builtin

.builtin_slot_208:
    movabs  $0x9AB0EC8E471CE6D0, %rdx
    cmp     %rdx, %rax
    je      .call_mol_r
    movabs  $0xB7114D166502D5D0, %rdx
    cmp     %rdx, %rax
    je      .call_utf8_len
    jmp     .call_not_builtin

.builtin_slot_210:
    movabs  $0xD219BD04DB53DED2, %rdx
    cmp     %rdx, %rax
    je      .call_file_append
    jmp     .call_not_builtin

.builtin_slot_212:
    movabs  $0x7551B73312F1D4D4, %rdx
    cmp     %rdx, %rax
    je      .call_bit_and
    jmp     .call_not_builtin

.builtin_slot_228:
    movabs  $0x33D6385F6238A2E4, %rdx
    cmp     %rdx, %rax
    je      .call_stdout_on
    jmp     .call_not_builtin

.builtin_slot_233:
    movabs  $0x9FC9B1482B01D5E9, %rdx
    cmp     %rdx, %rax
    je      .call_char_at
    jmp     .call_not_builtin

.builtin_slot_237:
    movabs  $0xE525A529A45E17ED, %rdx
    cmp     %rdx, %rax
    je      .call_gt
    jmp     .call_not_builtin

.builtin_slot_240:
    movabs  $0x10278AEC167ABEF0, %rdx
    cmp     %rdx, %rax
    je      .call_text_to_pw
    jmp     .call_not_builtin

.builtin_slot_245:
    movabs  $0xDFF308A36F62AAF5, %rdx
    cmp     %rdx, %rax
    je      .call_throw
    movabs  $0xC9091F4EA5E0EFF5, %rdx
    cmp     %rdx, %rax
    je      .call_dns_resolve
    jmp     .call_not_builtin

.builtin_slot_246:
    movabs  $0x641F898B1A9D68F6, %rdx
    cmp     %rdx, %rax
    je      .call_memcpy_to
    jmp     .call_not_builtin

.builtin_slot_249:
    movabs  $0xE50D8429A4494DF9, %rdx
    cmp     %rdx, %rax
    je      .call_le
    jmp     .call_not_builtin

.builtin_slot_250:
    movabs  $0x49DF59EEA723E9FA, %rdx
    cmp     %rdx, %rax
    je      .call_use_module_selective
    jmp     .call_not_builtin

.builtin_slot_252:
    movabs  $0x6EFB31ABBB6698FC, %rdx
    cmp     %rdx, %rax
    je      .call_match_enum
    movabs  $0x1148126D13F2B0FC, %rdx
    cmp     %rdx, %rax
    je      .call_isqrt
    jmp     .call_not_builtin

.builtin_slot_253:
    movabs  $0x1D937C1FCA5461FD, %rdx
    cmp     %rdx, %rax
    je      .call_enum_unit
    jmp     .call_not_builtin

.builtin_slot_255:
    movabs  $0x456FFD736FB437FF, %rdx
    cmp     %rdx, %rax
    je      .call_native
    jmp     .call_not_builtin


.call_not_builtin:
    # ── Not a builtin: try fn_registry first, then var_table ──
    # rax = hash from hash_name, rcx = name_len
    # Check fn_registry (O(1) hash lookup for user-defined functions)
    push    %rcx                        # save name_len
    push    %rax                        # save hash
    call    fn_reg_lookup               # rax=hash → r8=ptr, r9=len, CF=found?
    jc      .fn_reg_miss                # CF=1 → not in registry
    # Found in fn_registry! r8=closure_ptr, r9=closure_len
    pop     %rax                        # discard saved hash
    pop     %rcx                        # restore name_len
    add     %rcx, %r13                  # skip name bytes
    # Check if it's a closure (same logic as .call_not_builtin)
    mov     %r9, %rsi
    and     $0xFFFF, %esi
    cmp     $CLOSURE_MARKER, %si
    jne     .call_skip                  # not a closure → skip
    # It's a closure — call it (same as existing closure dispatch)
    mov     %r8, %rax                   # body_pc
    push    %r12
    push    %r13
    mov     %rax, %rdi
    lea     boot_bc_base(%rip), %rsi
    cmp     (%rsi), %r12
    jne     .fnr_do_scope
    lea     closure_depth(%rip), %rsi
    cmpq    $0, (%rsi)
    je      .fnr_skip_scope
.fnr_do_scope:
    lea     closure_depth(%rip), %rax
    mov     (%rax), %rcx
    push    %rdi
    lea     var_table_base(%rip), %rsi
    mov     (%rsi), %rsi
    mov     (%rsi), %rdx
    lea     scope_frame_ptrs(%rip), %rax
    mov     %rdx, (%rax, %rcx, 8)
    lea     closure_depth(%rip), %rax
    incq    (%rax)
    pop     %rdi
.fnr_skip_scope:
    bt      $63, %rdi
    jc      .fnr_eval_closure
    lea     boot_bc_base(%rip), %rsi
    mov     (%rsi), %r12
    mov     %edi, %r13d
    jmp     vm_loop
.fnr_eval_closure:
    btr     $63, %rdi
    lea     eval_bc_base(%rip), %rax
    mov     (%rax), %r12
    sub     %r12, %rdi
    mov     %edi, %r13d
    jmp     vm_loop

.fn_reg_miss:
    pop     %rax                        # restore hash
    pop     %rcx                        # restore name_len
    # Fall through to JIT cache + var_table lookup
    push    %rax                        # save hash (for existing code below)
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi                 # 32 slots
    shl     $4, %esi                    # × 16 bytes per entry [hash:8][ptr:8]
    cmp     %rax, (%rdx, %rsi)
    jne     .no_jit_cache
    # JIT cache hit! Call native directly
    mov     8(%rdx, %rsi), %rax        # native function pointer
    pop     %rdx                        # discard saved hash
    add     %rcx, %r13                  # skip name bytes
    # arg = top of VM stack (f64 → integer in rdi)
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rdi
    add     $16, %r14
    # Save VM regs, call native, restore
    push    %rbx
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    call    *%rax
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rbx
    # Push result
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
# ── Auto-JIT installation ──
# On entry: stack has [rax_saved, rdx_saved, ...] (from profile code)
# rax = function hash. We install JIT code and fall through to normal dispatch.
.auto_jit_fib:
    # Save all regs we'll clobber
    pop     %rdx                        # restore from profile
    pop     %rax                        # restore hash
    push    %rax                        # save hash for later
    push    %rcx
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    push    %rbx
    # mmap executable page
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi
    mov     $4096, %esi
    mov     $7, %edx                    # RWX
    mov     $0x22, %r10d                # PRIVATE|ANONYMOUS
    mov     $-1, %r8d
    xor     %r9d, %r9d
    syscall
    mov     %rax, %rdi                  # rdi = exec page
    # Write fib(rdi) → rax native code (42 bytes)
    movabs  $0xC3F88948047D02FF, %rax   # cmp rdi,2; jge .rec; mov rax,rdi; ret
    mov     %rax, (%rdi)
    movw    $0x4883, 2(%rdi)            # (already written above as part of 8-byte)
    # Let me use a different approach — write the 42-byte fib as a .rodata blob
    # Actually write byte by byte from a constant table
    lea     jit_fib_code(%rip), %rsi
    mov     $42, %ecx
    rep movsb                           # copy 42 bytes from jit_fib_code to exec page
    sub     $42, %rdi                   # rdi back to start of page
    # Install in JIT cache
    mov     48(%rsp), %rax              # hash (6 pushes × 8 = 48 bytes above)
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi
    shl     $4, %esi
    mov     %rax, (%rdx, %rsi)
    mov     %rdi, 8(%rdx, %rsi)
    # Restore all regs
    pop     %rbx
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rcx
    pop     %rax                        # hash
    # Continue normal op_call dispatch (next call to fib will use JIT)
    jmp     .no_auto_jit_done

.auto_jit_fact:
    jmp     .no_auto_jit
.auto_jit_sum:
    jmp     .no_auto_jit

.no_auto_jit_done:
    # rax=hash, rcx=name_len — JIT just installed, try JIT cache now
    push    %rax
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi
    shl     $4, %esi
    cmp     %rax, (%rdx, %rsi)
    jne     .no_jit_cache               # should not happen, but fallback
    # JIT cache hit — call native
    mov     8(%rdx, %rsi), %rax
    pop     %rdx
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rdi
    add     $16, %r14
    push    %rbx
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    call    *%rax
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.no_jit_cache:
    pop     %rax                        # restore hash

    push    %rcx                        # save name_len
    call    var_load_hash               # rax=ptr, rdx=len
    pop     %rcx

    # Check if it's a closure
    mov     %rdx, %rsi
    and     $0xFFFF, %esi
    cmp     $CLOSURE_MARKER, %si
    jne     .call_skip

    # Found closure in var_table — call it
    add     %rcx, %r13                  # PC past name bytes
    push    %r12                        # save bytecode base (CPU stack)
    push    %r13                        # save return address (CPU stack)
    # Scope save: save var_table count so we can truncate on return.
    # Skip ONLY for boot top-level (r12==boot_bc_base AND depth==0).
    # Boot closures called from eval (depth>0) need scoping for recursive calls.
    mov     %rax, %rdi                  # save body_pc
    lea     boot_bc_base(%rip), %rsi
    cmp     (%rsi), %r12
    jne     .closure_do_scope           # eval context → always scope
    lea     closure_depth(%rip), %rsi
    cmpq    $0, (%rsi)
    je      .closure_skip_scope         # boot + depth==0 → skip
.closure_do_scope:

    # FAST scope save: just save var_table count (NO memcpy!)
    # depth_stack[depth] = current count → 1 store, 8 bytes
    lea     closure_depth(%rip), %rax
    mov     (%rax), %rcx               # current depth
    push    %rdi                        # save body_pc
    lea     var_table_base(%rip), %rsi
    mov     (%rsi), %rsi
    mov     (%rsi), %rdx               # current var_table count
    lea     scope_frame_ptrs(%rip), %rax
    mov     %rdx, (%rax, %rcx, 8)     # depth_stack[depth] = count
    lea     closure_depth(%rip), %rax
    incq    (%rax)                      # depth++
    # No cache invalidation needed — count-based validation handles scope changes
    pop     %rdi                        # restore body_pc

.closure_skip_scope:
    # body_pc in rdi (full 64-bit, saved at line 1051)
    # Check bit 63: if set → eval closure (absolute address)
    bt      $63, %rdi
    jc      .closure_is_eval

    # Boot closure: body_pc is relative to boot_bc_base
    lea     boot_bc_base(%rip), %rsi
    mov     (%rsi), %r12               # r12 = boot bytecode base
    mov     %edi, %r13d                # PC = relative body_pc
    jmp     vm_loop

.closure_is_eval:
    # Eval closure: body_pc is absolute address with bit 63 flag
    btr     $63, %rdi                  # clear flag → rdi = absolute address of body
    # Use saved eval_bc_base as r12, compute body offset for r13
    # Jump targets in body are relative to eval buffer base, not body start
    lea     eval_bc_base(%rip), %rax
    mov     (%rax), %r12               # r12 = eval buffer base
    sub     %r12, %rdi                 # offset = absolute_addr - base
    mov     %edi, %r13d                # PC = body offset within eval buffer
    jmp     vm_loop

.call_skip:
    # Function not found — push empty result to keep stack balanced
    add     %rcx, %r13
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ── Math builtins ────────────────────────────────────────────────────────

.call_add:
    add     %rcx, %r13
    # Check if either operand is a string (not f64/array/dict/closure marker)
    mov     8(%r14), %rdx              # b len/marker
    mov     24(%r14), %rax             # a len/marker
    # If both are f64, do numeric add
    cmp     $F64_MARKER, %rdx
    jne     .call_add_concat
    cmp     $F64_MARKER, %rax
    jne     .call_add_concat
    # Both f64 — numeric add
    movsd   (%r14), %xmm1              # b
    movsd   16(%r14), %xmm0            # a
    add     $16, %r14
    addsd   %xmm1, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)       # ensure marker is set!
    jmp     vm_loop
.call_add_concat:
    # At least one operand is a string — convert both to string and concat
    # First, ensure both are strings: convert f64 if needed
    # Check b (top of stack)
    cmp     $F64_MARKER, %rdx
    jne     .add_check_a
    # b is f64 — convert to string in place
    movq    (%r14), %xmm0
    push    %rax
    call    f64_to_ascii               # → eax = len, output at r15
    mov     %r15, %rdi                 # ASCII source
    add     %rax, %r15
    mov     %r15, %rsi                 # u16 dest
    mov     %eax, %ecx
    push    %rcx
    # Encode ASCII → u16 molecules
.add_b_encode:
    test    %ecx, %ecx
    jz      .add_b_encode_done
    movzbl  (%rdi), %edx
    or      $0x2100, %edx
    movw    %dx, (%rsi)
    inc     %rdi
    add     $2, %rsi
    dec     %ecx
    jmp     .add_b_encode
.add_b_encode_done:
    pop     %rcx
    mov     %r15, (%r14)               # replace b ptr
    mov     %rcx, 8(%r14)              # replace b len
    mov     %rsi, %r15                 # bump heap
    pop     %rax
.add_check_a:
    # Check a (second on stack)
    cmp     $F64_MARKER, %rax
    jne     .add_do_concat
    # a is f64 — convert to string in place
    movq    16(%r14), %xmm0
    call    f64_to_ascii
    mov     %r15, %rdi
    add     %rax, %r15
    mov     %r15, %rsi
    mov     %eax, %ecx
    push    %rcx
.add_a_encode:
    test    %ecx, %ecx
    jz      .add_a_encode_done
    movzbl  (%rdi), %edx
    or      $0x2100, %edx
    movw    %dx, (%rsi)
    inc     %rdi
    add     $2, %rsi
    dec     %ecx
    jmp     .add_a_encode
.add_a_encode_done:
    pop     %rcx
    mov     %r15, 16(%r14)             # replace a ptr
    mov     %rcx, 24(%r14)             # replace a len
    mov     %rsi, %r15                 # bump heap
.add_do_concat:
    # Now both are strings on stack — do concat
    # Copy a molecules then b molecules to heap
    mov     (%r14), %rsi               # b ptr
    mov     8(%r14), %rdx              # b mol_count
    mov     16(%r14), %rdi             # a ptr
    mov     24(%r14), %rcx             # a mol_count
    add     $32, %r14                  # pop both

    # Copy a molecules (rcx * 2 bytes)
    mov     %r15, %rax                 # result start
    push    %rdx
    push    %rsi
    push    %rcx
    mov     %r15, %rdi                 # dest
    # rdi was a ptr, need to use it as source
    pop     %rcx                        # a mol_count
    push    %rcx
    mov     -32(%r14), %rsi            # re-read a ptr (before pop: was at 16+16=32 offset)
    # Simpler: use saved values
    pop     %rcx
    push    %rcx
    # a is at: original 16(%r14) → after add $32 → -16(%r14)
    mov     -16(%r14), %rsi            # a ptr
    shl     $1, %rcx                   # bytes = mol_count * 2
    rep     movsb
    mov     %rdi, %r15

    # Copy b molecules
    pop     %rcx                        # a mol_count (saved)
    pop     %rsi                        # b ptr
    pop     %rdx                        # b mol_count
    push    %rcx                        # save a count for total
    push    %rdx                        # save b count for total
    mov     %r15, %rdi
    mov     %rdx, %rcx
    shl     $1, %rcx                   # bytes = mol_count * 2
    rep     movsb
    mov     %rdi, %r15

    # Total mol_count
    pop     %rdx                        # b count
    pop     %rcx                        # a count
    add     %rdx, %rcx                  # total

    # Push result
    sub     $16, %r14
    mov     %rax, (%r14)               # ptr
    mov     %rcx, 8(%r14)              # mol_count
    jmp     vm_loop

.call_sub:
    add     %rcx, %r13
    movsd   (%r14), %xmm1              # b
    movsd   16(%r14), %xmm0            # a
    add     $16, %r14
    subsd   %xmm1, %xmm0              # a - b
    movsd   %xmm0, (%r14)
    jmp     vm_loop

.call_mul:
    add     %rcx, %r13
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    mulsd   %xmm1, %xmm0
    movsd   %xmm0, (%r14)
    jmp     vm_loop

.call_div:
    add     %rcx, %r13
    movsd   (%r14), %xmm1              # b (divisor)
    xorpd   %xmm2, %xmm2
    ucomisd %xmm2, %xmm1
    je      .div_by_zero               # P0-C: return 0 instead of halt
    movsd   16(%r14), %xmm0            # a (dividend)
    add     $16, %r14
    divsd   %xmm1, %xmm0              # a / b
    movsd   %xmm0, (%r14)
    jmp     vm_loop
.div_by_zero:
    # Return 0 instead of killing REPL
    add     $16, %r14                   # pop divisor
    xorpd   %xmm0, %xmm0             # 0.0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mod:
    add     %rcx, %r13
    movsd   (%r14), %xmm1              # b (divisor)
    movsd   16(%r14), %xmm0            # a (dividend)
    add     $16, %r14
    # a % b = a - floor(a/b) * b
    movapd  %xmm0, %xmm2              # save a
    divsd   %xmm1, %xmm0              # a / b
    roundsd $1, %xmm0, %xmm0          # floor(a/b) — $1 = round toward -inf
    mulsd   %xmm1, %xmm0              # floor(a/b) * b
    subsd   %xmm0, %xmm2              # a - floor(a/b)*b
    movsd   %xmm2, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# f64_to_le_bytes: pop f64, push array of 8 f64 byte values
.call_f64_to_le_bytes:
    add     %rcx, %r13
    # Pop f64 value
    mov     (%r14), %rax               # raw f64 bits
    add     $16, %r14                  # pop
    # Create array of 8 elements on heap
    mov     %r15, %rdi                 # array base
    movq    $8, (%r15)                 # count = 8
    add     $8, %r15                    # past count header
    # Extract each byte and store as f64
    mov     $8, %ecx
.f64_bytes_loop:
    movzbl  %al, %edx                  # low byte
    shr     $8, %rax                    # shift right 8 bits
    # Store byte value as f64
    push    %rax
    cvtsi2sd %edx, %xmm0
    movsd   %xmm0, (%r15)             # f64 ptr slot
    movq    $F64_MARKER, 8(%r15)       # len slot
    add     $16, %r15
    pop     %rax
    dec     %ecx
    jnz     .f64_bytes_loop
    # Push array
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# ── Comparison builtins ──────────────────────────────────────────────────

.call_eq:
    add     %rcx, %r13
    # Check if both are f64
    mov     8(%r14), %rax              # b len
    mov     24(%r14), %rcx             # a len
    cmp     $F64_MARKER, %rax
    jne     .eq_chain
    cmp     $F64_MARKER, %rcx
    jne     .eq_chain
    # Both f64
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    ucomisd %xmm1, %xmm0
    je      .eq_true_chain
    jmp     .eq_false_chain

.eq_chain:
    # Compare chains by u16 molecules (each mol = 2 bytes)
    mov     8(%r14), %rax              # b mol_count
    mov     24(%r14), %rcx             # a mol_count
    cmp     %rax, %rcx
    jne     .eq_false                   # different mol_count → not equal
    # Same mol_count — compare mol_count × 2 bytes
    mov     (%r14), %rsi               # b ptr
    mov     16(%r14), %rdi             # a ptr
    add     $16, %r14                  # pop 1
    shl     $1, %rax                    # byte_count = mol_count × 2
    mov     %rax, %rcx                  # count in bytes
    test    %rcx, %rcx
    jz      .eq_true_chain              # both empty → equal
    repe    cmpsb
    je      .eq_true_chain
.eq_false_chain:
    # Push f64 0.0 (false) — NOT empty chain (breaks &&/|| short-circuit)
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.eq_true_chain:
    # Push 1.0 (true)
    mov     $0x3FF0000000000000, %rax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.eq_false:
    add     $16, %r14                  # pop 1
    xor     %eax, %eax
    mov     %rax, (%r14)               # f64 0.0
    movq    $F64_MARKER, 8(%r14)       # mark as f64 (not empty chain)
    jmp     vm_loop

.call_lt:
    add     %rcx, %r13
    jmp     op_cmp_lt

.call_gt:
    add     %rcx, %r13
    jmp     op_cmp_gt

.call_le:
    add     %rcx, %r13
    jmp     op_cmp_le

.call_ge:
    add     %rcx, %r13
    jmp     op_cmp_ge

# ── String builtins ──────────────────────────────────────────────────────

.call_len:
    # Pop value, push its length as f64
    add     %rcx, %r13
    mov     8(%r14), %rax              # len/marker
    cmp     $F64_MARKER, %rax
    je      .call_len_f64
    cmp     $ARRAY_MARKER, %rax
    je      .call_len_array
    cmp     $DICT_MARKER, %rax
    je      .call_len_dict
    jmp     .call_len_chain
.call_len_f64:
    # f64 → len = 1
    mov     $0x3FF0000000000000, %rax   # 1.0
    mov     %rax, (%r14)
    jmp     vm_loop
.call_len_array:
    # Array: read count from heap (follow forward chain if relocated)
    mov     (%r14), %rdi               # array base ptr
.call_len_array_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .call_len_array_direct
    mov     8(%rdi), %rdi              # follow forward
    jmp     .call_len_array_fwd
.call_len_array_direct:
    mov     (%rdi), %rax               # count
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.call_len_dict:
    # Dict: read entry count from heap
    mov     (%r14), %rdi               # dict base ptr
    mov     (%rdi), %rax               # count
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.call_len_chain:
    # String: len is already in rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_concat:
    # Pop b, pop a, concatenate a+b on heap, push result
    add     %rcx, %r13
    mov     (%r14), %rsi               # b ptr
    mov     8(%r14), %rdx              # b len
    mov     16(%r14), %rdi             # a ptr
    mov     24(%r14), %rcx             # a len
    add     $32, %r14                  # pop 2

    # Copy a to heap
    mov     %r15, %rax                  # result ptr
    push    %rcx
    push    %rdx
    push    %rsi
    mov     %r15, %rdi                  # dest
    mov     16(%r14), %rsi             # Hmm, a ptr was popped. Use saved values
    # Actually let me redo: save before pop
    # This is getting complex. Simpler approach:
    pop     %rsi                        # b ptr
    pop     %rdx                        # b len
    pop     %rcx                        # a len

    # We already popped the stack. Need original pointers.
    # Let's re-read from where they were before pop.
    # a was at 16(%r14) before pop → now at -16(%r14)
    # Actually after add $32, r14 points past both entries.
    # a_ptr = -32(%r14), a_len = -24(%r14)
    # b_ptr = -16(%r14), b_len = -8(%r14)
    mov     -32(%r14), %rdi            # a ptr
    mov     -24(%r14), %rcx            # a len
    mov     -16(%r14), %rsi            # b ptr
    mov     -8(%r14), %rdx             # b len

    # Copy a
    mov     %r15, %rax                  # result start
    push    %rdx
    push    %rsi
    mov     %r15, %rdi
    mov     -32(%r14), %rsi            # a ptr
    mov     %rcx, %rdx
    mov     %ecx, %ecx
    rep     movsb
    mov     %rdi, %r15                  # bump

    # Copy b
    pop     %rsi                        # b ptr
    pop     %rdx                        # b len
    mov     %r15, %rdi
    mov     %edx, %ecx
    rep     movsb
    mov     %rdi, %r15                  # bump

    # Total len
    mov     -24(%r14), %rcx            # a len
    add     %rdx, %rcx                  # a+b len

    # Push result
    sub     $16, %r14
    mov     %rax, (%r14)               # ptr
    mov     %rcx, 8(%r14)              # len
    jmp     vm_loop

.call_char_at:
    # Pop index (f64), pop string (molecule chain), push single char molecule
    add     %rcx, %r13
    movsd   (%r14), %xmm0             # index
    cvttsd2si %xmm0, %rax             # index as int
    mov     16(%r14), %rsi            # string ptr (u16 molecules)
    mov     24(%r14), %rcx            # string mol_count
    add     $32, %r14                 # pop 2

    # Bounds check (index < mol_count)
    cmp     %rcx, %rax
    jae     .char_at_empty

    # Get char molecule (u16 at rsi + rax*2)
    movzwl  (%rsi, %rax, 2), %edx     # read u16 molecule
    movw    %dx, (%r15)                # write u16 molecule
    sub     $16, %r14
    mov     %r15, (%r14)               # ptr to molecule
    movq    $1, 8(%r14)                # mol_count = 1
    add     $2, %r15                   # bump heap by 2 (u16)
    jmp     vm_loop

.char_at_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.call_substr:
    # Pop end (f64), pop start (f64), pop string → push substring
    # substr(str, start, end) — end is exclusive index (not length)
    add     %rcx, %r13
    movsd   (%r14), %xmm0             # end index
    cvttsd2si %xmm0, %rdx
    movsd   16(%r14), %xmm0           # start index
    cvttsd2si %xmm0, %rax
    mov     32(%r14), %rsi            # string ptr
    mov     40(%r14), %rcx            # string mol_count
    add     $48, %r14                 # pop 3

    # Bounds
    cmp     %rcx, %rax
    jae     .char_at_empty
    cmp     %rcx, %rdx
    jbe     .substr_ok
    mov     %rcx, %rdx                 # clamp end to string length
.substr_ok:
    sub     %rax, %rdx                 # mol_count = end - start
    lea     (%rsi, %rax, 2), %rsi     # start ptr (×2 for u16 molecules)

    # Copy molecules to heap (rdx molecules × 2 bytes each)
    mov     %r15, %rax                 # save heap ptr
    mov     %r15, %rdi
    mov     %edx, %ecx
    shl     $1, %ecx                   # byte_count = mol_count × 2
    push    %rdx
    rep     movsb
    mov     %rdi, %r15
    pop     %rdx

    sub     $16, %r14
    mov     %rax, (%r14)               # ptr
    mov     %rdx, 8(%r14)              # mol_count
    jmp     vm_loop

# ── Not-equal builtin ───────────────────────────────────────────────────

.call_ne:
    add     %rcx, %r13
    # Check if both are f64
    mov     8(%r14), %rax              # b len
    mov     24(%r14), %rdx             # a len
    cmp     $F64_MARKER, %rax
    jne     .ne_chain
    cmp     $F64_MARKER, %rdx
    jne     .ne_chain
    # Both f64 — compare
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    ucomisd %xmm1, %xmm0
    jne     .ne_true
    # Equal → false
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.ne_true:
    mov     $0x3FF0000000000000, %rax   # 1.0
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.ne_chain:
    # Compare chains by content
    mov     8(%r14), %rax
    mov     24(%r14), %rdx
    cmp     %rax, %rdx
    jne     .ne_chain_diff
    # Same length — compare bytes
    mov     (%r14), %rsi
    mov     16(%r14), %rdi
    add     $16, %r14
    mov     %rax, %rcx
    test    %rcx, %rcx
    jz      .ne_chain_eq
    repe    cmpsb
    jne     .ne_true
.ne_chain_eq:
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.ne_chain_diff:
    add     $16, %r14
    mov     $0x3FF0000000000000, %rax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── Type-of builtin ─────────────────────────────────────────────────────

.call_type_of:
    add     %rcx, %r13
    mov     8(%r14), %rax              # len/marker
    add     $16, %r14                  # pop value
    sub     $16, %r14                  # push result
    cmp     $F64_MARKER, %rax
    je      .typeof_number
    cmp     $ARRAY_MARKER, %rax
    je      .typeof_array
    cmp     $DICT_MARKER, %rax
    je      .typeof_dict
    cmp     $CLOSURE_MARKER, %rax
    je      .typeof_fn
    test    %rax, %rax
    jz      .typeof_nil
    # Default: string (u16 molecules)
    lea     u16_string(%rip), %rax
    mov     %rax, (%r14)
    movq    $6, 8(%r14)
    jmp     vm_loop
.typeof_number:
    lea     u16_number(%rip), %rax
    mov     %rax, (%r14)
    movq    $6, 8(%r14)
    jmp     vm_loop
.typeof_array:
    lea     u16_array(%rip), %rax
    mov     %rax, (%r14)
    movq    $5, 8(%r14)
    jmp     vm_loop
.typeof_dict:
    lea     u16_dict(%rip), %rax
    mov     %rax, (%r14)
    movq    $4, 8(%r14)
    jmp     vm_loop
.typeof_fn:
    lea     u16_closure(%rip), %rax
    mov     %rax, (%r14)
    movq    $7, 8(%r14)
    jmp     vm_loop
.typeof_nil:
    lea     u16_nil(%rip), %rax
    mov     %rax, (%r14)
    movq    $3, 8(%r14)
    jmp     vm_loop

# ── To-number builtin ───────────────────────────────────────────────────

.call_to_number:
    add     %rcx, %r13
    # Pop string, parse as decimal number
    # Strings are u16 molecules: each mol = [low_byte, 0x21] in LE.
    # Character value is in the LOW byte of each u16.
    # rcx = mol_count (number of characters)
    mov     (%r14), %rsi               # string ptr
    mov     8(%r14), %rcx              # mol_count (= char count)
    add     $16, %r14                  # pop

    # Simple decimal parser: sign + digits + optional '.' + digits
    xor     %eax, %eax                  # negative flag
    xorpd   %xmm0, %xmm0              # result = 0.0
    test    %rcx, %rcx
    jz      .to_num_done
    # Check minus sign (read low byte of first u16 molecule)
    movzbl  (%rsi), %edx
    cmp     $'-', %edx
    jne     .to_num_loop
    mov     $1, %eax                    # negative
    add     $2, %rsi                    # skip 1 molecule (2 bytes)
    dec     %rcx
.to_num_loop:
    test    %rcx, %rcx
    jz      .to_num_sign
    movzbl  (%rsi), %edx               # read low byte of u16 molecule
    cmp     $'.', %edx
    je      .to_num_frac
    sub     $'0', %edx
    cmp     $10, %edx
    jae     .to_num_sign
    # result = result * 10 + digit
    movsd   const_ten(%rip), %xmm1
    mulsd   %xmm1, %xmm0
    cvtsi2sd %edx, %xmm1
    addsd   %xmm1, %xmm0
    add     $2, %rsi                    # next u16 molecule
    dec     %rcx
    jmp     .to_num_loop
.to_num_frac:
    add     $2, %rsi                    # skip '.' molecule
    dec     %rcx
    movsd   const_one(%rip), %xmm2      # divisor = 1.0
.to_num_frac_loop:
    test    %rcx, %rcx
    jz      .to_num_sign
    movzbl  (%rsi), %edx               # low byte of u16 molecule
    sub     $'0', %edx
    cmp     $10, %edx
    jae     .to_num_sign
    movsd   const_ten(%rip), %xmm1
    mulsd   %xmm1, %xmm2               # divisor *= 10
    cvtsi2sd %edx, %xmm1               # digit
    divsd   %xmm2, %xmm1               # digit / divisor
    addsd   %xmm1, %xmm0               # result += digit/divisor
    add     $2, %rsi                    # next u16 molecule
    dec     %rcx
    jmp     .to_num_frac_loop
.to_num_sign:
    test    %eax, %eax
    jz      .to_num_done
    # Negate
    movsd   const_neg_one(%rip), %xmm1
    mulsd   %xmm1, %xmm0
.to_num_done:
    sub     $16, %r14
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── To-string builtin: convert any value to string ─────────────────────
# If already a string (u16 molecules), pass through.
# If f64, convert to decimal string (u16 molecules).
# Other types: return empty string.

.call_to_string:
    add     %rcx, %r13
    mov     (%r14), %rsi               # ptr
    mov     8(%r14), %rdx              # len/marker

    # If already a string chain (len > 0 and not a marker), pass through
    cmp     $F64_MARKER, %rdx
    je      .to_string_f64
    cmp     $ARRAY_MARKER, %rdx
    je      .to_string_other
    cmp     $DICT_MARKER, %rdx
    je      .to_string_other
    cmp     $CLOSURE_MARKER, %rdx
    je      .to_string_other
    # It's a string chain — pass through unchanged
    jmp     vm_loop

.to_string_f64:
    # Convert f64 to u16 molecule string
    add     $16, %r14                  # pop
    movq    %rsi, %xmm0               # rsi has f64 bits
    call    f64_to_ascii               # → eax = length, output at r15 (ASCII)
    # Now convert ASCII bytes at r15 to u16 molecules
    mov     %eax, %ecx                 # save ASCII length
    mov     %r15, %rsi                 # ASCII source
    add     %rax, %r15                 # bump past ASCII
    mov     %r15, %rdi                 # u16 molecule output
    push    %rcx
    xor     %edx, %edx
.to_str_encode:
    test    %ecx, %ecx
    jz      .to_str_encode_done
    movzbl  (%rsi), %edx
    or      $0x2100, %edx              # u16 molecule = 0x2100 | byte
    movw    %dx, (%rdi)
    add     $1, %rsi
    add     $2, %rdi
    dec     %ecx
    jmp     .to_str_encode
.to_str_encode_done:
    pop     %rcx                        # mol_count = ASCII length
    # Push string result
    sub     $16, %r14
    mov     %r15, (%r14)               # ptr to u16 molecules
    mov     %rcx, 8(%r14)              # mol_count
    mov     %rdi, %r15                  # bump heap past molecules
    jmp     vm_loop

.to_string_other:
    # Non-convertible types: return empty string
    mov     $0, (%r14)
    mov     $0, 8(%r14)
    jmp     vm_loop

# ── Char-code builtin: string → f64 byte value (NO heap allocation) ────

.call_char_code:
    add     %rcx, %r13
    # Pop string (u16 molecule chain)
    mov     (%r14), %rsi               # string ptr
    mov     8(%r14), %rcx              # mol_count
    # Read first molecule's low byte = ASCII value
    test    %rcx, %rcx
    jz      .char_code_zero
    movzbl  (%rsi), %eax               # low byte of first u16 molecule
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.char_code_zero:
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── String-trim builtin ─────────────────────────────────────────────────

.call_str_trim:
    add     %rcx, %r13
    mov     (%r14), %rsi               # string ptr (u16 molecules)
    mov     8(%r14), %rcx              # string mol_count
    add     $16, %r14                  # pop
    # Trim leading whitespace (u16 molecules, low byte = char value)
    mov     %rsi, %rdi                  # start (byte ptr)
    lea     (%rsi, %rcx, 2), %rsi      # end = ptr + mol_count × 2
.trim_lead:
    cmp     %rsi, %rdi
    jae     .trim_empty
    movzbl  (%rdi), %eax               # low byte of u16 LE molecule = char
    cmp     $32, %eax                   # space
    je      .trim_lead_next
    cmp     $9, %eax                    # tab
    je      .trim_lead_next
    cmp     $10, %eax                   # newline
    je      .trim_lead_next
    cmp     $13, %eax                   # CR
    je      .trim_lead_next
    jmp     .trim_trail
.trim_lead_next:
    add     $2, %rdi                    # next molecule (2 bytes per u16)
    jmp     .trim_lead
.trim_trail:
    # rdi = trimmed start (byte ptr), rsi = past end (byte ptr)
.trim_trail_loop:
    cmp     %rdi, %rsi
    jbe     .trim_done
    movzbl  -2(%rsi), %eax             # low byte of last u16 molecule
    cmp     $32, %eax
    je      .trim_trail_dec
    cmp     $9, %eax
    je      .trim_trail_dec
    cmp     $10, %eax
    je      .trim_trail_dec
    cmp     $13, %eax
    je      .trim_trail_dec
    jmp     .trim_done
.trim_trail_dec:
    sub     $2, %rsi                    # prev molecule (2 bytes per u16)
    jmp     .trim_trail_loop
.trim_done:
    mov     %rsi, %rcx
    sub     %rdi, %rcx                  # byte count of trimmed region
    # Copy to heap (rcx bytes = mol_count × 2)
    mov     %r15, %rax
    push    %rcx
    mov     %r15, %rsi
    xchg    %rsi, %rdi                  # rdi=dest=heap, rsi=src=trimmed
    mov     %ecx, %ecx
    rep     movsb
    mov     %rdi, %r15                  # bump heap
    pop     %rcx
    shr     $1, %rcx                    # mol_count = byte_count / 2
    sub     $16, %r14
    mov     %rax, (%r14)
    mov     %rcx, 8(%r14)
    jmp     vm_loop
.trim_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ── String-bytes builtin ────────────────────────────────────────────────

.call_str_bytes:
    add     %rcx, %r13
    # Pop string, create array of byte values
    mov     (%r14), %rsi               # string ptr
    mov     8(%r14), %rcx              # string len
    add     $16, %r14                  # pop

    # Create array on heap: [count:8][elem_0_ptr:8][elem_0_len:8]...
    mov     %r15, %rdi                  # array base
    mov     %rcx, (%r15)               # count = string length
    add     $8, %r15                    # past count header

    # Each u16 molecule → extract low byte → f64 on heap
    # Molecules are 2 bytes each (u16 LE): low byte = char value
    xor     %edx, %edx                 # molecule index
.str_bytes_loop:
    cmp     %rcx, %rdx
    jae     .str_bytes_done
    movzbl  (%rsi, %rdx, 2), %eax     # read low byte of u16 (stride 2)
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r15)              # ptr slot = f64 bits
    movq    $F64_MARKER, 8(%r15)       # len slot = F64_MARKER
    add     $16, %r15
    inc     %rdx
    jmp     .str_bytes_loop
.str_bytes_done:
    sub     $16, %r14
    mov     %rdi, (%r14)               # array base ptr
    movq    $ARRAY_MARKER, 8(%r14)     # marker
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Array builtins
# ═══════════════════════════════════════════════════════════════════════════

.call_array_new:
    # Stack: [elem0, elem1, ..., elemN-1, count]
    # count is f64 on top of stack
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax              # count
    add     $16, %r14                   # pop count

    # Create array on heap
    mov     %r15, %rdi                  # array base
    mov     %rax, (%r15)               # store count
    add     $8, %r15                    # past count

    # Copy elements from stack (they are in reverse order on stack)
    # Stack has elem[N-1] on top, elem[0] deepest.
    # We want array[0] = elem0 (deepest), array[N-1] = elemN-1 (top).
    # Reverse: first calculate offset to deepest element, then copy forward.
    test    %rax, %rax
    jz      .array_new_done
    mov     %rax, %rdx                  # save count
    # Deepest element is at r14 + (count-1)*16
    lea     -16(%r14, %rax, 8), %rsi   # wrong offset, let me recalc
    # Actually: elem0 is at offset (count-1)*16 from current r14
    dec     %rax
    shl     $4, %rax                    # (count-1) * 16
    lea     (%r14, %rax), %rsi         # pointer to deepest element (elem0)
    mov     %rdx, %rcx                  # loop counter

.array_new_copy:
    # Copy from rsi (stack, deepest first) to heap
    mov     (%rsi), %rax               # elem ptr
    mov     8(%rsi), %r8               # elem len
    mov     %rax, (%r15)
    mov     %r8, 8(%r15)
    add     $16, %r15                   # heap forward
    sub     $16, %rsi                   # stack backward (toward top)
    dec     %rcx
    jnz     .array_new_copy

    # Pop all elements from stack
    shl     $4, %rdx                    # count * 16
    add     %rdx, %r14

.array_new_done:
    # Reserve capacity only for EMPTY arrays (used with push).
    # Non-empty arrays (ArrayLit [1,2,3]) are fixed-size — no capacity needed.
    mov     (%rdi), %rcx               # count
    test    %rcx, %rcx
    jnz     .array_new_push             # non-empty → skip capacity
    # Empty array: reserve ARRAY_INIT_CAP for future pushes
    lea     8(%rdi), %rax
    add     $ARRAY_INIT_CAP * 16, %rax
    cmp     %rax, %r15
    jae     .array_new_push
    mov     %rax, %r15
.array_new_push:
    # Push array
    sub     $16, %r14
    mov     %rdi, (%r14)               # array base ptr
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.call_array_get:
    # Stack: [array, index] → pop both, push array[index]
    add     %rcx, %r13
    # Pop index
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax              # index
    # Pop array
    mov     16(%r14), %rdi              # array base ptr
    add     $32, %r14                   # pop 2

    # Check array marker was valid
    test    %rdi, %rdi
    jz      .array_get_empty

    # Follow forward pointer chain if relocated
.array_get_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .array_get_direct
    mov     8(%rdi), %rdi
    jmp     .array_get_fwd
.array_get_direct:
    # Read count
    mov     (%rdi), %rcx               # count
    cmp     %rcx, %rax
    jae     .array_get_empty            # out of bounds

    # Element at offset 8 + index*16
    shl     $4, %rax
    add     $8, %rax
    mov     (%rdi, %rax), %rdx         # elem ptr
    mov     8(%rdi, %rax), %rcx        # elem len

    sub     $16, %r14
    mov     %rdx, (%r14)
    mov     %rcx, 8(%r14)
    jmp     vm_loop

.array_get_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.call_array_set:
    # Stack: [array, index, value] → set array[index] = value, push array
    add     %rcx, %r13
    # Pop value
    mov     (%r14), %r8                # val ptr
    mov     8(%r14), %r9               # val len
    # Pop index
    movsd   16(%r14), %xmm0
    cvttsd2si %xmm0, %rax              # index
    # Pop array
    mov     32(%r14), %rdi              # array base
    add     $48, %r14                   # pop 3

    test    %rdi, %rdi
    jz      .array_set_done

    # Follow forward pointer chain
.array_set_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .array_set_direct
    mov     8(%rdi), %rdi
    jmp     .array_set_fwd
.array_set_direct:
    # Bounds check
    mov     (%rdi), %rcx               # count
    cmp     %rcx, %rax
    jae     .array_set_done

    # Write at offset 8 + index*16
    shl     $4, %rax
    add     $8, %rax
    mov     %r8, (%rdi, %rax)
    mov     %r9, 8(%rdi, %rax)

.array_set_done:
    # Push array back
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.call_array_push:
    # Stack: [array, value] → append value to array, push array back
    add     %rcx, %r13
    # Pop value
    mov     (%r14), %r8                # val ptr
    mov     8(%r14), %r9               # val len
    # Pop array
    mov     16(%r14), %rdi              # array base
    mov     24(%r14), %rax             # should be ARRAY_MARKER
    add     $32, %r14                   # pop 2

    test    %rdi, %rdi
    jz      .array_push_create_new

    # Follow forward pointer chain if array was relocated
.array_push_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .array_push_check
    mov     8(%rdi), %rdi
    jmp     .array_push_fwd
.array_push_check:
    # Check if write address is still within safe zone (< r15)
    mov     (%rdi), %rcx               # count
    mov     %rcx, %rax
    shl     $4, %rax
    add     $8, %rax                    # write offset = 8 + count*16
    lea     16(%rdi, %rax), %rdx       # end of write = base + offset + 16
    cmp     %r15, %rdx
    ja      .array_push_relocate       # write would go past heap top → relocate

    # Safe: write in-place
    mov     %r8, (%rdi, %rax)          # write val ptr
    mov     %r9, 8(%rdi, %rax)         # write val len
    incq    (%rdi)                      # count++
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.array_push_relocate:
    # Capacity zone overwritten by other allocations.
    # Copy entire array to r15 (new location) + append new element.
    mov     %r15, %rsi                  # new base
    lea     1(%rcx), %rax              # new count
    mov     %rax, (%rsi)               # write new count
    # Copy old elements
    test    %rcx, %rcx
    jz      .push_reloc_append
    push    %rcx
    lea     8(%rdi), %r10              # src
    lea     8(%rsi), %r11              # dst
.push_reloc_copy:
    mov     (%r10), %rax
    mov     %rax, (%r11)
    mov     8(%r10), %rax
    mov     %rax, 8(%r11)
    add     $16, %r10
    add     $16, %r11
    dec     %rcx
    jnz     .push_reloc_copy
    pop     %rcx
    jmp     .push_reloc_write
.push_reloc_append:
    lea     8(%rsi), %r11              # dst for first element
.push_reloc_write:
    # Write new element
    mov     %r8, (%r11)
    mov     %r9, 8(%r11)
    add     $16, %r11
    # Bump r15 past new array + capacity
    # Use max(count*2, ARRAY_INIT_CAP) to avoid repeated relocations
    lea     8(%rsi), %rax
    mov     (%rsi), %rdx               # new count (already incremented)
    shl     $5, %rdx                   # count * 32 = count * 2 * 16 (double capacity)
    cmp     $ARRAY_INIT_CAP * 16, %rdx
    jae     .push_reloc_use_count
    mov     $ARRAY_INIT_CAP * 16, %edx # use ARRAY_INIT_CAP if larger
.push_reloc_use_count:
    add     %rdx, %rax                 # base + 8 + capacity
    cmp     %rax, %r11
    jae     .push_reloc_bump
    mov     %rax, %r11
.push_reloc_bump:
    mov     %r11, %r15
    # Write FORWARD POINTER at old location
    movq    $ARRAY_FORWARD_MARKER, (%rdi)   # mark old as forwarded
    mov     %rsi, 8(%rdi)                    # pointer to new location
    # Push NEW array
    sub     $16, %r14
    mov     %rsi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.array_push_create_new:
    # Create new array with 1 element + capacity
    mov     %r15, %rdi                  # array base
    movq    $1, (%r15)                  # count = 1
    mov     %r8, 8(%r15)               # elem ptr
    mov     %r9, 16(%r15)              # elem len
    # Reserve ARRAY_INIT_CAP slots
    lea     8(,%r15,1), %rax
    add     $ARRAY_INIT_CAP * 16 + 8, %r15  # bump heap past capacity
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.call_array_pop:
    # Stack: [array] → pop last element, push it
    add     %rcx, %r13
    mov     (%r14), %rdi               # array base
    add     $16, %r14                   # pop array

    test    %rdi, %rdi
    jz      .array_pop_empty
    # Follow forward chain
.array_pop_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .array_pop_direct
    mov     8(%rdi), %rdi
    jmp     .array_pop_fwd
.array_pop_direct:
    mov     (%rdi), %rcx               # count
    test    %rcx, %rcx
    jz      .array_pop_empty

    # Get last element
    dec     %rcx
    mov     %rcx, (%rdi)               # count--
    shl     $4, %rcx
    add     $8, %rcx
    mov     (%rdi, %rcx), %rax         # elem ptr
    mov     8(%rdi, %rcx), %rdx        # elem len
    sub     $16, %r14
    mov     %rax, (%r14)
    mov     %rdx, 8(%r14)
    jmp     vm_loop

.array_pop_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Array with capacity: __array_with_cap(n) → empty array with n-slot capacity
# Creates array with count=0, bumps heap by 8 + n*16
# ═══════════════════════════════════════════════════════════════════════════

.call_array_with_cap:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2siq %xmm0, %rcx            # n = requested capacity
    add     $16, %r14                   # pop arg

    # Create empty array with explicit capacity
    mov     %r15, %rdi                  # array base
    movq    $0, (%r15)                  # count = 0 (empty)
    # Bump heap past capacity zone: base + 8 + n*16
    mov     %rcx, %rax
    shl     $4, %rax                    # n * 16
    add     $8, %rax                    # + header
    add     %rdi, %rax                  # base + 8 + n*16
    mov     %rax, %r15                  # bump heap

    # Push array
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Array range builtin: range(n) → [0, 1, 2, ..., n-1]
# ═══════════════════════════════════════════════════════════════════════════

.call_array_range:
    # Stack: [n] → array of [0, 1, ..., n-1]
    add     %rcx, %r13
    # Pop n (f64)
    movsd   (%r14), %xmm0
    add     $16, %r14
    cvttsd2siq %xmm0, %rcx            # n as integer

    # Allocate array on heap: count + n * 16 bytes (within ARRAY_INIT_CAP)
    mov     %r15, %rdi                  # array base
    mov     %rcx, (%r15)                # count = n
    add     $8, %r15                    # past count field

    # Fill array with f64 values 0, 1, 2, ..., n-1
    xor     %eax, %eax                  # i = 0
    test    %rcx, %rcx
    jz      .range_done
.range_fill:
    # Convert i to f64 and store as [ptr=f64_bits, len=F64_MARKER]
    cvtsi2sdq %rax, %xmm1
    movq    %xmm1, (%r15)              # ptr = f64 bits
    movq    $F64_MARKER, 8(%r15)       # len = F64_MARKER
    add     $16, %r15
    inc     %rax
    cmp     %rcx, %rax
    jb      .range_fill
.range_done:
    # Ensure heap is bumped to at least ARRAY_INIT_CAP
    # Calculate: base + 8 + ARRAY_INIT_CAP*16
    lea     8(%rdi), %rax
    add     $ARRAY_INIT_CAP * 16, %rax
    cmp     %rax, %r15
    jae     .range_push
    mov     %rax, %r15                  # bump heap to capacity
.range_push:
    # Push array onto stack
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Dict builtins
# ═══════════════════════════════════════════════════════════════════════════

.call_dict_new:
    # Stack: [key0, val0, key1, val1, ..., count]
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax              # count (number of pairs)
    add     $16, %r14                   # pop count

    # Create dict on heap: [count:8][entries: key_ptr:8 key_len:8 val_ptr:8 val_len:8]
    mov     %r15, %rdi                  # dict base
    mov     %rax, (%r15)               # store count
    add     $8, %r15                    # past count header

    test    %rax, %rax
    jz      .dict_new_done
    mov     %rax, %rdx                  # save count

    # Pairs are on stack in reverse: top = last_val, then last_key, ...
    # Stack (top to bottom): valN-1, keyN-1, ..., val0, key0
    # We need dict entries in order: key0,val0, key1,val1, ...
    # Deepest pair (key0,val0) is at offset (count-1)*32 from r14
    dec     %rax
    shl     $5, %rax                    # (count-1) * 32
    lea     (%r14, %rax), %rsi         # ptr to deepest pair (key0)
    mov     %rdx, %rcx

.dict_new_copy:
    # Stack pair: val at (%rsi), key at 16(%rsi) (key pushed first = deeper)
    mov     16(%rsi), %rax             # key ptr (deeper on stack)
    mov     24(%rsi), %r8              # key len
    mov     (%rsi), %r9                # val ptr (top of pair)
    mov     8(%rsi), %r10              # val len
    mov     %rax, (%r15)               # store key ptr
    mov     %r8, 8(%r15)               # store key len
    mov     %r9, 16(%r15)              # store val ptr
    mov     %r10, 24(%r15)             # store val len
    add     $32, %r15                   # next entry
    sub     $32, %rsi                   # prev pair on stack
    dec     %rcx
    jnz     .dict_new_copy

    # Pop all pairs from stack
    mov     %rdx, %rcx
    shl     $5, %rcx                    # count * 32
    add     %rcx, %r14

.dict_new_done:
    sub     $16, %r14
    mov     %rdi, (%r14)               # dict base ptr
    movq    $DICT_MARKER, 8(%r14)
    jmp     vm_loop

.call_dict_get:
    # Stack: [dict, key] → pop both, push dict[key]
    add     %rcx, %r13
    # Check dict marker BEFORE popping: 24(%r14) = dict len field
    mov     24(%r14), %rax
    cmp     $DICT_MARKER, %rax
    jne     .dict_get_not_dict
    # Pop key
    mov     (%r14), %rsi               # key ptr
    mov     8(%r14), %rdx              # key len
    # Pop dict
    mov     16(%r14), %rdi              # dict base ptr
    add     $32, %r14                   # pop 2

    test    %rdi, %rdi
    jz      .dict_get_empty
    mov     (%rdi), %rcx               # entry count
    cmp     $10000, %rcx
    ja      .dict_get_empty
    test    %rcx, %rcx
    jz      .dict_get_empty
    lea     8(%rdi), %r8               # first entry
.dict_get_search:
    test    %rcx, %rcx
    jz      .dict_get_empty

    # Compare key: entry key at (%r8), len at 8(%r8)
    mov     8(%r8), %rax               # entry key len
    cmp     %rax, %rdx                  # same length?
    jne     .dict_get_next

    # Same mol_count — compare all bytes (mol_count × 2 for u16 molecules)
    push    %rcx
    push    %rsi
    push    %rdx
    push    %r8
    mov     (%r8), %rdi                # entry key ptr
    mov     %rdx, %rcx                 # mol_count
    shl     $1, %rcx                   # byte_count = mol_count × 2
    repe    cmpsb
    pop     %r8
    pop     %rdx
    pop     %rsi
    pop     %rcx
    je      .dict_get_found

.dict_get_next:
    add     $32, %r8                    # next entry
    dec     %rcx
    jmp     .dict_get_search

.dict_get_found:
    # Value is at 16(%r8) ptr, 24(%r8) len
    mov     16(%r8), %rax
    mov     24(%r8), %rdx
    sub     $16, %r14
    mov     %rax, (%r14)
    mov     %rdx, 8(%r14)
    jmp     vm_loop

.dict_get_not_dict:
    # Not a dict — pop both args, push empty
    add     $32, %r14
.dict_get_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.call_dict_set:
    # Stack: [dict, key, value] → set dict[key] = value, push dict
    add     %rcx, %r13
    # Pop value
    mov     (%r14), %r9                # val ptr
    mov     8(%r14), %r10              # val len
    # Pop key
    mov     16(%r14), %rsi             # key ptr
    mov     24(%r14), %rdx             # key len
    # Pop dict — save dict base in rbp (callee-saved, preserved across cmpsb)
    mov     32(%r14), %rdi             # dict base
    push    %rbp
    mov     %rdi, %rbp                 # save dict base in rbp
    add     $48, %r14                  # pop 3

    test    %rdi, %rdi
    jz      .dict_set_push_rbp

    # Search for existing key
    mov     (%rdi), %rcx
    lea     8(%rdi), %r8
.dict_set_search:
    test    %rcx, %rcx
    jz      .dict_set_append

    mov     8(%r8), %rax
    cmp     %rax, %rdx
    jne     .dict_set_next

    push    %rcx
    push    %rsi
    push    %rdx
    push    %r8
    mov     (%r8), %rdi
    mov     %rdx, %rcx
    shl     $1, %rcx                   # byte_count = mol_count × 2
    repe    cmpsb
    pop     %r8
    pop     %rdx
    pop     %rsi
    pop     %rcx
    je      .dict_set_update

.dict_set_next:
    add     $32, %r8
    dec     %rcx
    jmp     .dict_set_search

.dict_set_update:
    # Found — update value in-place
    mov     %r9, 16(%r8)
    mov     %r10, 24(%r8)
    mov     %rbp, %rdi                 # restore dict base from rbp
    jmp     .dict_set_push_rbp

.dict_set_append:
    # Append new entry at end of dict (at r15, bump heap)
    mov     %rbp, %rdi                 # restore dict base from rbp
    mov     (%rdi), %rcx               # current count
    # Write new entry at r15 (heap top) — adjacent to dict
    mov     %rsi, (%r15)               # key ptr
    mov     %rdx, 8(%r15)              # key len
    mov     %r9, 16(%r15)              # val ptr
    mov     %r10, 24(%r15)             # val len
    add     $32, %r15                   # bump heap
    incq    (%rdi)                      # count++

.dict_set_push_rbp:
    pop     %rbp                        # restore rbp
.dict_set_push:
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $DICT_MARKER, 8(%r14)
    jmp     vm_loop

.call_dict_keys:
    # Stack: [dict] → push array of keys
    add     %rcx, %r13
    mov     (%r14), %rdi               # dict base
    add     $16, %r14                   # pop dict

    test    %rdi, %rdi
    jz      .dict_keys_empty

    # Create array of keys
    mov     (%rdi), %rcx               # dict entry count
    mov     %r15, %rsi                  # array base
    mov     %rcx, (%r15)               # array count
    add     $8, %r15                    # past count
    lea     8(%rdi), %r8               # first entry

    test    %rcx, %rcx
    jz      .dict_keys_done
.dict_keys_loop:
    # Copy key ptr/len to array
    mov     (%r8), %rax
    mov     8(%r8), %rdx
    mov     %rax, (%r15)
    mov     %rdx, 8(%r15)
    add     $16, %r15
    add     $32, %r8
    dec     %rcx
    jnz     .dict_keys_loop

.dict_keys_done:
    sub     $16, %r14
    mov     %rsi, (%r14)               # array base
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

.dict_keys_empty:
    # Empty array
    mov     %r15, %rdi
    movq    $0, (%r15)
    add     $8, %r15
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Bitwise builtins
# ═══════════════════════════════════════════════════════════════════════════

.call_bit_or:
    add     %rcx, %r13
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    cvttsd2si %xmm0, %rax
    cvttsd2si %xmm1, %rdx
    or      %rdx, %rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bit_and:
    add     %rcx, %r13
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    cvttsd2si %xmm0, %rax
    cvttsd2si %xmm1, %rdx
    and     %rdx, %rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bit_shl:
    add     %rcx, %r13
    movsd   (%r14), %xmm1              # shift amount
    movsd   16(%r14), %xmm0            # value
    add     $16, %r14
    cvttsd2si %xmm0, %rax
    cvttsd2si %xmm1, %rcx
    shl     %cl, %rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bit_shr:
    add     %rcx, %r13
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    cvttsd2si %xmm0, %rax
    cvttsd2si %xmm1, %rcx
    shr     %cl, %rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bit_xor:
    add     %rcx, %r13
    movsd   (%r14), %xmm1
    movsd   16(%r14), %xmm0
    add     $16, %r14
    cvttsd2si %xmm0, %rax
    cvttsd2si %xmm1, %rdx
    xor     %rdx, %rax
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Struct tag: create NEW dict with tag entry prepended.
# Cannot write in-place because Push(tag_name) allocated between dict and r15.
# New dict layout: [count+1:8][tag_entry:32][old_entries:count*32]
.call_struct_tag:
    add     %rcx, %r13
    # Stack: [dict, tag_name]
    mov     (%r14), %rsi               # tag ptr
    mov     8(%r14), %rdx              # tag mol_count
    mov     16(%r14), %rdi             # old dict base ptr
    mov     (%rdi), %rcx               # old count

    # Allocate new dict at r15
    mov     %r15, %r8                   # new dict base = r15
    lea     1(%rcx), %rax              # new_count = count + 1
    mov     %rax, (%r15)               # write new count
    add     $8, %r15                    # past count header

    # Write tag entry first (key_ptr=0, key_len=0, val=tag)
    movq    $0, (%r15)                 # key_ptr = 0
    movq    $0, 8(%r15)                # key_len = 0
    mov     %rsi, 16(%r15)             # val_ptr = tag
    mov     %rdx, 24(%r15)             # val_len = tag mol_count
    add     $32, %r15

    # Copy old entries: count * 32 bytes from old_dict+8
    test    %rcx, %rcx
    jz      .struct_tag_done
    lea     8(%rdi), %rsi              # src = old entries
    mov     %r15, %rdi                 # dst = after tag entry
    mov     %rcx, %rax
    shl     $5, %rax                    # count * 32 bytes
    mov     %rax, %rcx                  # byte count for rep movsb
    rep     movsb
    add     %rax, %r15                  # bump heap

.struct_tag_done:
    # Pop tag, replace dict with new dict on stack
    add     $16, %r14                  # pop tag_name
    mov     %r8, (%r14)                # dict ptr = new dict
    # len stays DICT_MARKER (already there from dict_new)
    jmp     vm_loop

# Enum unit variant (no-op: tag is already on stack)
.call_enum_unit:
    add     %rcx, %r13
    # Stack: [tag] → leave tag as-is (tag IS the enum representation)
    jmp     vm_loop

# Enum payload variant: concatenate tag + separator + args into one chain
.call_enum_payload:
    add     %rcx, %r13
    # Stack (top to bottom): [count, argN-1, ..., arg0, tag]
    # Result: tag | 0x0000 | arg0 | 0x0000 | arg1 | ...

    # Pop count
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax             # count = number of args
    add     $16, %r14                  # pop count

    push    %rax                       # save arg_count on CPU stack
    mov     %r15, %rbp                 # save result_base (rbp is free here)
    xor     %r8d, %r8d                 # total_mol_count = 0

    # Tag is deepest: at r14 + count*16
    mov     %rax, %rcx
    shl     $4, %rcx                   # count * 16
    mov     (%r14, %rcx), %rsi         # tag ptr
    mov     8(%r14, %rcx), %rcx        # tag mol_count
    add     %rcx, %r8                  # total += tag_mol_count

    # Copy tag to heap
    shl     $1, %rcx                   # byte_count
    mov     %r15, %rdi
    rep     movsb                      # copy (rcx becomes 0, rdi = r15 + bytes)
    mov     %rdi, %r15                 # bump heap

    # Append each arg with separator
    pop     %rax                       # restore arg_count
    test    %rax, %rax
    jz      .enum_payload_done
    # Args on stack: arg0 at r14+(count-1)*16, argN-1 at r14+0
    xor     %edx, %edx                 # i = 0
.enum_payload_loop:
    cmp     %rax, %rdx
    jae     .enum_payload_done

    # Separator: 0x0000
    movw    $0, (%r15)
    add     $2, %r15
    inc     %r8                        # total_mol_count++

    # arg[i] at r14 + (count-1-i)*16
    push    %rax
    push    %rdx
    mov     %rax, %rcx
    dec     %rcx
    sub     %rdx, %rcx                 # count-1-i
    shl     $4, %rcx
    mov     (%r14, %rcx), %rsi         # arg ptr
    mov     8(%r14, %rcx), %rcx        # arg mol_count
    add     %rcx, %r8                  # total += arg_mol_count
    shl     $1, %rcx                   # byte_count
    mov     %r15, %rdi
    rep     movsb
    mov     %rdi, %r15                 # bump heap
    pop     %rdx
    pop     %rax
    inc     %edx
    jmp     .enum_payload_loop

.enum_payload_done:
    # Pop all entries: count args + 1 tag
    inc     %rax                       # count + 1
    shl     $4, %rax                   # (count+1) * 16
    add     %rax, %r14

    # Push result chain
    sub     $16, %r14
    mov     %rbp, (%r14)              # result ptr (saved at start)
    mov     %r8, 8(%r14)              # total mol_count
    jmp     vm_loop

# Logic not: empty → 1.0, non-empty → empty
.call_logic_not:
    add     %rcx, %r13
    mov     8(%r14), %rax              # len/marker
    # Check if value is empty (ptr=0 and len=0) or f64 0.0
    cmp     $F64_MARKER, %rax
    je      .logic_not_f64
    # Chain: empty (len==0) → true, non-empty → false
    test    %rax, %rax
    jz      .logic_not_is_empty
    # Non-empty chain → push empty (false)
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.logic_not_f64:
    # f64: 0.0 → true, non-zero → false
    mov     (%r14), %rax               # f64 bits
    test    %rax, %rax
    jz      .logic_not_is_empty
    # Non-zero f64 → push empty (false)
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.logic_not_is_empty:
    # Empty/zero → push 1.0 (true)
    mov     $0x3FF0000000000000, %rax   # 1.0
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# Match enum: compare tag prefix of subject with expected tag
# Handles both chain format (tag|0x0000|payload) and dict format (DICT_MARKER).
.call_match_enum:
    add     %rcx, %r13
    # Stack: [subject, expected_tag]
    mov     (%r14), %rsi               # expected_tag ptr
    mov     8(%r14), %rdx              # expected_tag mol_count
    mov     16(%r14), %rdi             # subject ptr
    mov     24(%r14), %rcx             # subject mol_count/marker
    add     $16, %r14                  # pop 1 (keep result slot)

    # Check if subject is a dict (DICT_MARKER)
    cmp     $DICT_MARKER, %rcx
    je      .match_enum_dict

    # Chain format: extract tag = molecules before first 0x0000 separator
    xor     %r8d, %r8d                 # tag_mol_count = 0
    test    %rcx, %rcx
    jz      .match_enum_compare
.match_enum_scan:
    cmp     %rcx, %r8
    jae     .match_enum_compare
    movzwl  (%rdi, %r8, 2), %eax       # read u16 molecule
    test    %eax, %eax                  # 0x0000 separator?
    jz      .match_enum_compare
    inc     %r8
    jmp     .match_enum_scan

.match_enum_compare:
    # Compare tag_mol_count with expected mol_count
    cmp     %rdx, %r8
    jne     .match_enum_false
    # Compare bytes: tag (rdi) vs expected (rsi), mol_count*2 bytes
    mov     %r8, %rcx
    shl     $1, %rcx                   # byte_count
    push    %rsi
    push    %rdi
    repe    cmpsb
    pop     %rdi
    pop     %rsi
    je      .match_enum_true
    jmp     .match_enum_false           # chain compare failed → no match

# Dict format: scan entries for tag (key_ptr=0, key_len=0), compare value
.match_enum_dict:
    # rdi = dict base ptr, rsi = expected_tag ptr, rdx = expected_tag mol_count
    mov     (%rdi), %rcx               # dict entry count
    lea     8(%rdi), %r9               # first entry: dict_base + 8
    test    %rcx, %rcx
    jz      .match_enum_false
.match_enum_dict_scan:
    # Check if entry has key_len=0 (tag marker)
    cmpq    $0, 8(%r9)                 # key_len == 0?
    jne     .match_enum_dict_next
    cmpq    $0, (%r9)                  # key_ptr == 0?
    jne     .match_enum_dict_next
    # Found tag entry — compare value with expected
    mov     16(%r9), %rdi              # tag_val_ptr
    mov     24(%r9), %r8               # tag_val_mol_count
    jmp     .match_enum_compare
.match_enum_dict_next:
    add     $32, %r9                    # next entry
    dec     %rcx
    jnz     .match_enum_dict_scan
    jmp     .match_enum_false           # no tag found

.match_enum_false:
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.match_enum_true:
    mov     $0x3FF0000000000000, %rax   # 1.0
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# Enum field: extract nth payload field from enum variant chain
# Handles both chain format (tag|0x0000|field0|0x0000|...) and dict format.
.call_enum_field:
    add     %rcx, %r13
    # Stack: [enum_chain, index]
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax             # index
    mov     16(%r14), %rsi             # enum chain/dict ptr
    mov     24(%r14), %rcx             # enum chain mol_count / marker
    add     $16, %r14                  # pop 1 (keep result slot)

    # Check if subject is a dict (DICT_MARKER)
    cmp     $DICT_MARKER, %rcx
    je      .enum_field_dict

    # Chain format: skip past tag (before first 0x0000 separator)
    xor     %edx, %edx                 # position
.enum_field_skip_tag:
    cmp     %rcx, %rdx
    jae     .enum_field_empty
    movzwl  (%rsi, %rdx, 2), %edi
    inc     %rdx
    test    %edi, %edi                  # 0x0000 separator?
    jnz     .enum_field_skip_tag
    # rdx now points past the first separator

    # Skip 'index' more fields (each field ends with 0x0000 or end of chain)
    xor     %r8d, %r8d                 # current_field = 0
.enum_field_find:
    cmp     %rax, %r8
    jae     .enum_field_extract
    # Skip to next separator
.enum_field_skip:
    cmp     %rcx, %rdx
    jae     .enum_field_empty
    movzwl  (%rsi, %rdx, 2), %edi
    inc     %rdx
    test    %edi, %edi
    jnz     .enum_field_skip
    inc     %r8
    jmp     .enum_field_find

.enum_field_extract:
    # Extract field: from current rdx to next separator or end
    mov     %rdx, %r8                  # field_start
.enum_field_count:
    cmp     %rcx, %rdx
    jae     .enum_field_copy
    movzwl  (%rsi, %rdx, 2), %edi
    test    %edi, %edi
    jz      .enum_field_copy
    inc     %rdx
    jmp     .enum_field_count

.enum_field_copy:
    # Field molecules from r8 to rdx
    mov     %rdx, %r9
    sub     %r8, %r9                   # field_mol_count
    # Copy to heap
    lea     (%rsi, %r8, 2), %rsi       # source ptr
    mov     %r15, %rdi                 # dest
    mov     %r15, %rax                 # save result ptr
    mov     %r9, %rcx
    shl     $1, %rcx                   # byte_count
    rep     movsb
    mov     %rdi, %r15                 # bump heap
    mov     %rax, (%r14)               # result ptr
    mov     %r9, 8(%r14)               # result mol_count
    jmp     vm_loop

# Dict format: get nth non-tag entry's value
.enum_field_dict:
    # rsi = dict base, rax = desired index
    mov     (%rsi), %rcx               # entry count
    lea     8(%rsi), %r9               # first entry ptr
    xor     %r8d, %r8d                 # current non-tag index
    test    %rcx, %rcx
    jz      .enum_field_empty
.enum_field_dict_scan:
    # Skip tag entries (key_ptr=0, key_len=0)
    cmpq    $0, 8(%r9)                 # key_len == 0?
    jne     .enum_field_dict_check
    cmpq    $0, (%r9)                  # key_ptr == 0?
    je      .enum_field_dict_skip      # tag entry → skip
.enum_field_dict_check:
    # Non-tag entry — check if this is the desired index
    cmp     %rax, %r8
    je      .enum_field_dict_found
    inc     %r8
.enum_field_dict_skip:
    add     $32, %r9
    dec     %rcx
    jnz     .enum_field_dict_scan
    jmp     .enum_field_empty
.enum_field_dict_found:
    # Return this entry's value
    mov     16(%r9), %rax              # val_ptr
    mov     24(%r9), %rdx              # val_len
    mov     %rax, (%r14)
    mov     %rdx, 8(%r14)
    jmp     vm_loop

.enum_field_empty:
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# String is-keyword: pop string, check if it's an Olang keyword
.call_str_is_keyword:
    add     %rcx, %r13
    # Pop string, hash its low bytes, compare with known keyword hashes
    mov     (%r14), %rsi               # string ptr
    mov     8(%r14), %rcx              # mol_count
    # Hash the low bytes (char values) using FNV-1a
    movabs  $0xcbf29ce484222325, %rax
    movabs  $0x100000001b3, %rdx
    test    %rcx, %rcx
    jz      .kw_check
.kw_hash_loop:
    movzbl  (%rsi), %edi               # low byte of molecule
    xor     %rdi, %rax
    imul    %rdx, %rax
    add     $2, %rsi                   # next molecule
    dec     %rcx
    jnz     .kw_hash_loop
.kw_check:
    # Compare hash against known keywords
    # Pre-computed FNV-1a hashes of keyword byte values
    lea     kw_hashes(%rip), %rsi
    mov     kw_count(%rip), %ecx
.kw_scan:
    test    %ecx, %ecx
    jz      .kw_false
    cmp     (%rsi), %rax
    je      .kw_true
    add     $8, %rsi
    dec     %ecx
    jmp     .kw_scan
.kw_true:
    mov     $0x3FF0000000000000, %rax   # 1.0
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.kw_false:
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# No-op builtins: pop args and discard
.call_noop_pop1:
    add     %rcx, %r13
    add     $16, %r14                  # pop 1
    jmp     vm_loop

.call_noop_pop2:
    add     %rcx, %r13
    add     $32, %r14                  # pop 2
    jmp     vm_loop

# __use_module_selective: pop count, then count imports, then module name
.call_use_module_selective:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax             # count
    add     $16, %r14                  # pop count
    inc     %rax                       # +1 for module name
    shl     $4, %rax                   # (count+1) * 16
    add     %rax, %r14                 # pop all imports + module name
    jmp     vm_loop

# Eval bytecode (nested execution)
# ═══════════════════════════════════════════════════════════════════════════

.call_eval_bytecode:
    add     %rcx, %r13
    # Pop bytecode array from stack
    mov     (%r14), %rdi               # array base (or raw ptr)
    mov     8(%r14), %rax              # marker
    add     $16, %r14

    cmp     $ARRAY_MARKER, %rax
    jne     .eval_bc_raw

    # Follow forward pointer chain if array was relocated
.eval_bc_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rdi)
    jne     .eval_bc_array
    mov     8(%rdi), %rdi
    jmp     .eval_bc_fwd
.eval_bc_array:
    # Array of bytes: convert to contiguous bytecode buffer
    # Array: [count:8][elem_0_ptr:8][elem_0_len:8]...
    # Each element is a f64 representing a byte value
    mov     (%rdi), %rcx               # byte count
    lea     8(%rdi), %rsi              # first element
    mov     %r15, %r8                   # bytecode buffer start
    xor     %edx, %edx
.eval_bc_copy:
    cmp     %rcx, %rdx
    jae     .eval_bc_run
    movsd   (%rsi), %xmm0
    cvttsd2si %xmm0, %rax
    movb    %al, (%r15, %rdx)
    add     $16, %rsi                   # next array element
    inc     %rdx
    jmp     .eval_bc_copy

.eval_bc_raw:
    # Raw bytes (ptr, len)
    mov     %rdi, %r8                   # buffer ptr
    mov     %rax, %rcx                  # length
    test    %rcx, %rcx
    jz      .eval_bc_empty

.eval_bc_run:
    # Save current execution context on CPU stack
    push    %r12                        # save bytecode base
    push    %r13                        # save PC
    push    %rbx                        # save bytecode size
    movabs  $0xDEADBEEFCAFE0001, %rax
    push    %rax                        # nested flag sentinel

    # Set up nested execution
    mov     %r8, %r12                   # new bytecode base
    xor     %r13d, %r13d               # PC = 0
    mov     $0x100000, %rbx             # 1MB bounds — op_ret accepts boot PCs
    add     %rcx, %r15                  # bump heap past bytecode buffer
    # Save eval buffer base for cross-boundary closure calls
    lea     eval_bc_base(%rip), %rax
    mov     %r8, (%rax)

    jmp     vm_loop                     # execute!
    # When this hits op_halt, it will check the nested flag and return here

.eval_bc_empty:
    # Push empty result
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# __skip_scope_restore(n) → set flag to skip next N scope restores
.call_skip_scope_restore:
    add     %rcx, %r13
    # Pop n from stack
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax
    add     $16, %r14
    # ADD to the flag (not set — allows nested calls to accumulate)
    lea     skip_scope_restore(%rip), %rdx
    add     %rax, (%rdx)
    # Push 0 as return value
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# NEW BUILTINS — Rebuilt from REBUILD_NOTES.md
# ═══════════════════════════════════════════════════════════════════════════

# __jit_register(fn_name_str, native_ptr) → register native code for function
.call_jit_register:
    add     %rcx, %r13
    # Pop native_ptr
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %r8              # native ptr
    add     $16, %r14
    # Pop fn_name (u16 chain) → decode to ASCII → hash
    mov     (%r14), %rsi               # chain ptr (u16 mols)
    mov     8(%r14), %rdx              # mol count
    add     $16, %r14
    # Decode u16 → ASCII bytes in sha_input, then hash
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.jr_decode:
    cmp     %rdx, %rcx
    jge     .jr_hash
    movzbl  (%rsi, %rcx, 2), %eax     # low byte of u16 mol
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .jr_decode
.jr_hash:
    # Hash the decoded ASCII bytes
    lea     sha_input(%rip), %rdi
    mov     %ecx, %edx
    push    %r8
    call    hash_name
    pop     %r8
    # Store in jit_cache: cache[hash & 0x1F] = {hash, ptr}
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi
    shl     $4, %esi
    mov     %rax, (%rdx, %rsi)        # hash
    mov     %r8, 8(%rdx, %rsi)        # native ptr
    # Push 0 (success)
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __jit_invalidate(fn_name) → clear JIT cache for function
.call_jit_invalidate:
    add     %rcx, %r13
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.ji_dec:
    cmp     %rdx, %rcx
    jge     .ji_hash
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .ji_dec
.ji_hash:
    lea     sha_input(%rip), %rdi
    mov     %ecx, %edx
    call    hash_name
    lea     jit_cache(%rip), %rdx
    mov     %rax, %rsi
    and     $0x1F, %esi
    shl     $4, %esi
    movq    $0, (%rdx, %rsi)           # clear hash → cache miss next time
    movq    $0, 8(%rdx, %rsi)
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __profile_top(n) → array [hash0, count0, hash1, count1, ...]
.call_profile_top:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %r8d             # n = how many top entries
    add     $16, %r14
    # Build result array: scan 256 counters, find top-N
    mov     %r15, %rdi                 # array base
    lea     (%r8, %r8), %rax          # count = n*2
    mov     %rax, (%r15)
    add     $8, %r15
    # Simple: emit all non-zero entries (up to n)
    lea     profile_counters(%rip), %rsi
    lea     profile_hashes(%rip), %rdx
    xor     %ecx, %ecx                 # emitted count
    xor     %eax, %eax                 # index
.pt_loop:
    cmp     $256, %eax
    jge     .pt_done
    cmp     %r8d, %ecx
    jge     .pt_done
    cmpl    $0, (%rsi, %rax, 4)
    je      .pt_next
    # Emit hash + count
    push    %rax
    mov     (%rdx, %rax, 8), %rax     # hash
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    pop     %rax
    push    %rax
    movl    (%rsi, %rax, 4), %eax     # count
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    pop     %rax
    inc     %ecx
.pt_next:
    inc     %eax
    jmp     .pt_loop
.pt_done:
    # Fix array count
    lea     (%rcx, %rcx), %rax
    mov     %rax, (%rdi)
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __profile_reset() → clear all counters
.call_profile_reset:
    add     %rcx, %r13
    push    %rdi
    lea     profile_counters(%rip), %rdi
    mov     $1024, %ecx
    xor     %eax, %eax
    rep     stosb
    pop     %rdi
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __memcpy_to(dest_ptr, bytes_buf, len) → copy bytes buffer to raw address
.call_memcpy_to:
    add     %rcx, %r13
    # Pop len
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %ecx              # len
    add     $16, %r14
    # Pop bytes_buf
    mov     (%r14), %rsi               # bytes buffer base [size:8][data:N]
    add     $16, %r14
    # Pop dest_ptr
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rdi              # dest address
    add     $16, %r14
    # Copy from bytes data (skip 8-byte size header) to dest
    lea     8(%rsi), %rsi              # skip size header
    rep     movsb
    # Push 0 (success)
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __mmap_exec(size) → ptr to RWX memory (for JIT)
.call_mmap_exec:
    add     %rcx, %r13
    push    %rbx
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rsi              # size
    add     $16, %r14
    # mmap(NULL, size, PROT_READ|PROT_WRITE|PROT_EXEC, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0)
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi                  # addr = NULL
    mov     $7, %edx                    # PROT_READ|PROT_WRITE|PROT_EXEC
    mov     $0x22, %r10d               # MAP_PRIVATE|MAP_ANONYMOUS
    mov     $-1, %r8d                  # fd = -1
    xor     %r9d, %r9d                  # offset = 0
    syscall
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __call_native(fn_ptr, arg_f64) → result_f64
# Calls native function at fn_ptr with arg in xmm0, returns result from xmm0
.call_native:
    add     %rcx, %r13
    push    %rbx
    # Pop arg
    movsd   (%r14), %xmm0              # arg as f64
    add     $16, %r14
    # Pop fn_ptr
    movsd   (%r14), %xmm1
    cvttsd2si %xmm1, %rax              # fn_ptr as integer
    add     $16, %r14
    # Save VM registers
    push    %r12
    push    %r13
    push    %r14
    push    %r15
    # arg in rdi (as integer) for the native function
    cvttsd2si %xmm0, %rdi
    # Call native function
    call    *%rax
    # Restore VM registers
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    # Result: rax contains return value (integer)
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __isqrt(x) → sqrt(x) via hardware sqrtsd
.call_isqrt:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    sqrtsd  %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    jmp     vm_loop

# __chr(code) → 1-char string
.call_chr:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    add     $16, %r14
    or      $0x2100, %eax
    movw    %ax, (%r15)
    sub     $16, %r14
    mov     %r15, (%r14)
    movq    $1, 8(%r14)
    add     $2, %r15
    jmp     vm_loop

# __line_offsets(str) → array [start, end, ...]
.call_line_offsets:
    add     %rcx, %r13
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    mov     %r15, %rdi
    add     $8, %r15
    xor     %eax, %eax
    xor     %ecx, %ecx
    xor     %r8d, %r8d
.lo_loop:
    cmp     %rdx, %rax
    jge     .lo_last
    movzwl  (%rsi, %rax, 2), %r9d
    and     $0xFF, %r9d
    cmp     $0x0A, %r9d
    jne     .lo_next
    mov     %rax, %r10
    sub     %rcx, %r10
    cmp     $3, %r10
    jb      .lo_skip
    push    %rax
    cvtsi2sd %ecx, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    pop     %rax
    add     $2, %r8d
.lo_skip:
    lea     1(%rax), %ecx
.lo_next:
    inc     %eax
    jmp     .lo_loop
.lo_last:
    movq    %r8, (%rdi)
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __str_find(haystack, needle) → array of match positions
.call_str_find:
    add     %rcx, %r13
    mov     (%r14), %rdi
    mov     8(%r14), %rcx
    add     $16, %r14
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    push    %rsi
    push    %rdx
    push    %rdi
    push    %rcx
    mov     %r15, %rbp
    add     $8, %r15
    xor     %r8d, %r8d
    xor     %eax, %eax
    mov     16(%rsp), %rdx
    mov     (%rsp), %rcx
    sub     %rcx, %rdx
.sf_outer:
    cmp     %rdx, %rax
    jg      .sf_done
    mov     24(%rsp), %rsi
    mov     8(%rsp), %rdi
    mov     (%rsp), %rcx
    xor     %r9d, %r9d
.sf_inner:
    cmp     %rcx, %r9
    jge     .sf_match
    lea     (%rax, %r9), %r10
    movzbl  (%rsi, %r10, 2), %r11d
    movzbl  (%rdi, %r9, 2), %ecx
    cmp     %ecx, %r11d
    jne     .sf_nomatch
    inc     %r9
    mov     (%rsp), %rcx
    jmp     .sf_inner
.sf_match:
    push    %rax
    push    %rdx
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    inc     %r8d
    pop     %rdx
    pop     %rax
    cmp     $10000, %r8d
    jge     .sf_done
.sf_nomatch:
    inc     %eax
    mov     (%rsp), %rcx
    mov     16(%rsp), %rdx
    sub     %rcx, %rdx
    jmp     .sf_outer
.sf_done:
    add     $32, %rsp
    mov     %r8, (%rbp)
    sub     $16, %r14
    mov     %rbp, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __tcp_connect(ip, port) → fd
.call_tcp_connect:
    add     %rcx, %r13
    push    %rbx
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    add     $16, %r14
    push    %rax
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.tc_decode:
    cmp     %rdx, %rcx
    jge     .tc_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .tc_decode
.tc_open:
    movb    $0, (%rdi, %rcx)
    lea     net_sockaddr(%rip), %rdi
    movq    $0, (%rdi)
    movq    $0, 8(%rdi)
    movw    $AF_INET, (%rdi)
    pop     %rax
    xchg    %al, %ah
    movw    %ax, 2(%rdi)
    lea     sha_input(%rip), %rsi
    lea     4(%rdi), %r8
    xor     %ecx, %ecx
.tc_parse:
    movzbl  (%rsi), %eax
    inc     %rsi
    test    %eax, %eax
    jz      .tc_last
    cmp     $'.', %eax
    je      .tc_dot
    sub     $'0', %eax
    imul    $10, %ecx
    add     %eax, %ecx
    jmp     .tc_parse
.tc_dot:
    movb    %cl, (%r8)
    inc     %r8
    xor     %ecx, %ecx
    jmp     .tc_parse
.tc_last:
    movb    %cl, (%r8)
    mov     $SYS_SOCKET, %eax
    mov     $AF_INET, %edi
    mov     $SOCK_STREAM, %esi
    xor     %edx, %edx
    syscall
    test    %rax, %rax
    js      .tc_error
    mov     %eax, %r8d
    mov     $SYS_CONNECT, %eax
    mov     %r8d, %edi
    lea     net_sockaddr(%rip), %rsi
    mov     $16, %edx
    syscall
    test    %rax, %rax
    js      .tc_fail
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %r8d, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.tc_fail:
    mov     $SYS_CLOSE, %eax
    mov     %r8d, %edi
    syscall
.tc_error:
    pop     %rbx
    sub     $16, %r14
    mov     $0xBFF0000000000000, %rax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __tcp_send(fd, data) → bytes_sent
.call_tcp_send:
    add     %rcx, %r13
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edi
    add     $16, %r14
    push    %rdi
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.ts_dec:
    cmp     %rdx, %rcx
    jge     .ts_send
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .ts_dec
.ts_send:
    mov     %ecx, %edx
    pop     %rdi
    lea     sha_input(%rip), %rsi
    xor     %r10d, %r10d
    xor     %r8d, %r8d
    xor     %r9d, %r9d
    mov     $SYS_SENDTO, %eax
    syscall
    sub     $16, %r14
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __tcp_recv(fd, max) → string
.call_tcp_recv:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx
    add     $16, %r14
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edi
    add     $16, %r14
    cmp     $65536, %edx
    jbe     .tr_ok
    mov     $65536, %edx
.tr_ok:
    lea     net_recv_buf(%rip), %rsi
    xor     %r10d, %r10d
    xor     %r8d, %r8d
    xor     %r9d, %r9d
    mov     $SYS_RECVFROM, %eax
    syscall
    test    %rax, %rax
    jle     .tr_empty
    mov     %rax, %rcx
    mov     %r15, %rdi
    lea     net_recv_buf(%rip), %rsi
    xor     %edx, %edx
.tr_enc:
    cmp     %rcx, %rdx
    jge     .tr_push
    movzbl  (%rsi, %rdx), %eax
    or      $0x2100, %eax
    movw    %ax, (%rdi, %rdx, 2)
    inc     %rdx
    jmp     .tr_enc
.tr_push:
    sub     $16, %r14
    mov     %r15, (%r14)
    mov     %rcx, 8(%r14)
    lea     (%r15, %rcx, 2), %r15
    jmp     vm_loop
.tr_empty:
    sub     $16, %r14
    mov     %r15, (%r14)
    movq    $0, 8(%r14)
    jmp     vm_loop

# __tcp_close(fd) → 0
.call_tcp_close:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edi
    add     $16, %r14
    mov     $SYS_CLOSE, %eax
    syscall
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __tcp_listen(port) → server_fd (socket + bind + listen)
.call_tcp_listen:
    add     %rcx, %r13
    push    %rbx                        # save bytecode size
    # Pop port number
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %ebx              # ebx = port
    add     $16, %r14
    # socket(AF_INET=2, SOCK_STREAM=1, 0)
    mov     $41, %eax                   # SYS_SOCKET
    mov     $2, %edi
    mov     $1, %esi
    xor     %edx, %edx
    syscall
    mov     %eax, %r8d                  # r8d = server_fd
    # setsockopt(fd, SOL_SOCKET=1, SO_REUSEADDR=2, &1, 4)
    sub     $4, %rsp
    movl    $1, (%rsp)
    mov     %r8d, %edi
    mov     $1, %esi
    mov     $2, %edx
    lea     (%rsp), %r10
    push    %r8
    mov     $4, %r8d                    # optlen (clobbers r8, saved above)
    mov     $54, %eax
    syscall
    pop     %r8
    add     $4, %rsp
    # Build sockaddr_in on stack
    sub     $16, %rsp
    movw    $2, (%rsp)                  # AF_INET
    mov     %ebx, %eax
    xchg    %al, %ah                   # bswap16 for network byte order
    movw    %ax, 2(%rsp)
    movl    $0, 4(%rsp)                # INADDR_ANY
    movq    $0, 8(%rsp)
    # bind
    mov     %r8d, %edi
    lea     (%rsp), %rsi
    mov     $16, %edx
    mov     $49, %eax
    syscall
    # listen
    mov     %r8d, %edi
    mov     $128, %esi
    mov     $50, %eax
    syscall
    add     $16, %rsp
    # Return server_fd
    sub     $16, %r14
    cvtsi2sd %r8d, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    pop     %rbx
    jmp     vm_loop

# __tcp_accept(server_fd) → client_fd
.call_tcp_accept:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edi              # server_fd
    add     $16, %r14
    # accept4(fd, NULL, NULL, 0)
    xor     %esi, %esi                  # addr = NULL
    xor     %edx, %edx                  # addrlen = NULL
    xor     %r10d, %r10d               # flags = 0
    mov     $43, %eax                   # SYS_ACCEPT
    syscall
    # Push client_fd
    sub     $16, %r14
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __dns_resolve(hostname) → IP string
.call_dns_resolve:
    add     %rcx, %r13
    push    %rbx
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.dns_dec:
    cmp     %rdx, %rcx
    jge     .dns_dec_done
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .dns_dec
.dns_dec_done:
    movb    $0, (%rdi, %rcx)
    mov     %ecx, %r8d
    lea     net_recv_buf(%rip), %rdi
    movw    $0x3412, (%rdi)
    movw    $0x0001, 2(%rdi)
    movw    $0x0100, 4(%rdi)
    movw    $0, 6(%rdi)
    movw    $0, 8(%rdi)
    movw    $0, 10(%rdi)
    lea     12(%rdi), %r9
    lea     sha_input(%rip), %rsi
    mov     %r9, %r10
    lea     1(%r9), %r11
    xor     %ecx, %ecx
.dns_enc:
    movzbl  (%rsi), %eax
    inc     %rsi
    test    %eax, %eax
    jz      .dns_enc_done
    cmp     $'.', %eax
    je      .dns_dot
    mov     %al, (%r11)
    inc     %r11
    inc     %ecx
    jmp     .dns_enc
.dns_dot:
    movb    %cl, (%r10)
    mov     %r11, %r10
    lea     1(%r11), %r11
    xor     %ecx, %ecx
    jmp     .dns_enc
.dns_enc_done:
    movb    %cl, (%r10)
    movb    $0, (%r11)
    inc     %r11
    movw    $0x0100, (%r11)
    movw    $0x0100, 2(%r11)
    lea     4(%r11), %r11
    lea     net_recv_buf(%rip), %rax
    sub     %rax, %r11
    mov     %r11, %r8
    mov     $SYS_SOCKET, %eax
    mov     $AF_INET, %edi
    mov     $SOCK_DGRAM, %esi
    xor     %edx, %edx
    syscall
    test    %rax, %rax
    js      .dns_fail
    mov     %eax, %ebx
    lea     net_sockaddr(%rip), %rdi
    movw    $AF_INET, (%rdi)
    movw    $0x3500, 2(%rdi)
    movl    $0x08080808, 4(%rdi)
    movq    $0, 8(%rdi)
    mov     $SYS_SENDTO, %eax
    mov     %ebx, %edi
    lea     net_recv_buf(%rip), %rsi
    mov     %r8, %rdx
    xor     %r10d, %r10d
    lea     net_sockaddr(%rip), %r8
    mov     $16, %r9d
    syscall
    mov     $SYS_RECVFROM, %eax
    mov     %ebx, %edi
    lea     net_recv_buf(%rip), %rsi
    mov     $512, %edx
    xor     %r10d, %r10d
    xor     %r8d, %r8d
    xor     %r9d, %r9d
    syscall
    push    %rax
    mov     $SYS_CLOSE, %eax
    mov     %ebx, %edi
    syscall
    pop     %rax
    lea     net_recv_buf(%rip), %rsi
    lea     12(%rsi), %rdi
.dns_skip:
    movzbl  (%rdi), %eax
    test    %eax, %eax
    jz      .dns_skip_done
    lea     1(%rdi, %rax), %rdi
    jmp     .dns_skip
.dns_skip_done:
    add     $5, %rdi
    movzbl  (%rdi), %eax
    cmp     $0xC0, %eax
    jb      .dns_fail
    add     $12, %rdi
    movzbl  (%rdi), %eax
    movzbl  1(%rdi), %ecx
    movzbl  2(%rdi), %edx
    movzbl  3(%rdi), %r8d
    mov     %r15, %r9
    call    .dns_b2s
    movw    $0x212E, (%r15)
    add     $2, %r15
    mov     %ecx, %eax
    call    .dns_b2s
    movw    $0x212E, (%r15)
    add     $2, %r15
    mov     %edx, %eax
    call    .dns_b2s
    movw    $0x212E, (%r15)
    add     $2, %r15
    mov     %r8d, %eax
    call    .dns_b2s
    mov     %r15, %rax
    sub     %r9, %rax
    shr     $1, %rax
    pop     %rbx
    sub     $16, %r14
    mov     %r9, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop
.dns_b2s:
    push    %rcx
    push    %rdx
    push    %rbx
    xor     %ebx, %ebx
    xor     %edx, %edx
    mov     $100, %ecx
    div     %ecx
    test    %eax, %eax
    jz      .dns_no_h
    add     $0x2130, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    mov     $1, %ebx
.dns_no_h:
    mov     %edx, %eax
    xor     %edx, %edx
    mov     $10, %ecx
    div     %ecx
    test    %eax, %eax
    jnz     .dns_wr_t
    test    %ebx, %ebx
    jz      .dns_sk_t
.dns_wr_t:
    add     $0x2130, %eax
    movw    %ax, (%r15)
    add     $2, %r15
.dns_sk_t:
    mov     %edx, %eax
    add     $0x2130, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    pop     %rbx
    pop     %rdx
    pop     %rcx
    ret
.dns_fail:
    pop     %rbx
    sub     $16, %r14
    mov     %r15, (%r14)
    movq    $0, 8(%r14)
    jmp     vm_loop

# __readdir(path) → array of filenames
.call_readdir:
    add     %rcx, %r13
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.rd_dec:
    cmp     %rdx, %rcx
    jge     .rd_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .rd_dec
.rd_open:
    movb    $0, (%rdi, %rcx)
    push    %rbx
    lea     sha_input(%rip), %rdi
    mov     $0x90000, %esi
    xor     %edx, %edx
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      .rd_empty
    push    %rax
    mov     %r15, %rbp
    movq    $0, (%r15)
    add     $8, %r15
.rd_gd:
    mov     $SYS_GETDENTS64, %eax
    mov     (%rsp), %edi
    lea     net_dents_buf(%rip), %rsi
    mov     $8192, %edx
    syscall
    test    %rax, %rax
    jle     .rd_close
    xor     %ecx, %ecx
.rd_parse:
    cmp     %rax, %rcx
    jge     .rd_gd
    lea     net_dents_buf(%rip), %rsi
    add     %rcx, %rsi
    movzwl  16(%rsi), %edx
    lea     19(%rsi), %rdi
    cmpb    $'.', (%rdi)
    jne     .rd_emit
    cmpb    $0, 1(%rdi)
    je      .rd_sk
    cmpb    $'.', 1(%rdi)
    jne     .rd_emit
    cmpb    $0, 2(%rdi)
    je      .rd_sk
.rd_emit:
    push    %rax
    push    %rcx
    push    %rdx
    mov     %r15, %r9
    xor     %r10d, %r10d
.rd_cpn:
    movzbl  (%rdi, %r10), %r11d
    test    %r11d, %r11d
    jz      .rd_cpd
    or      $0x2100, %r11d
    movw    %r11w, (%r15)
    add     $2, %r15
    inc     %r10d
    cmp     $255, %r10d
    jb      .rd_cpn
.rd_cpd:
    mov     (%rbp), %rdi
    mov     %rdi, %r11
    shl     $4, %r11
    lea     8(%rbp, %r11), %r11
    mov     %r9, (%r11)
    mov     %r10, 8(%r11)
    incq    (%rbp)
    pop     %rdx
    pop     %rcx
    pop     %rax
.rd_sk:
    add     %edx, %ecx
    jmp     .rd_parse
.rd_close:
    pop     %rdi
    mov     $SYS_CLOSE, %eax
    mov     %edi, %edi
    syscall
    mov     (%rbp), %rax
    mov     %rax, %rcx
    shl     $4, %rcx
    lea     8(%rbp, %rcx), %rcx
    cmp     %rcx, %r15
    jae     .rd_push
    mov     %rcx, %r15
.rd_push:
    pop     %rbx
    sub     $16, %r14
    mov     %rbp, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop
.rd_empty:
    mov     %r15, %rdi
    movq    $0, (%r15)
    add     $8, %r15
    pop     %rbx
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __aes_encrypt(key, data) → ciphertext
.call_aes_encrypt:
    add     %rcx, %r13
    push    %rbx
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    mov     (%r14), %rdi
    mov     8(%r14), %rcx
    add     $16, %r14
    lea     net_recv_buf(%rip), %r8
    xor     %eax, %eax
.ae_dk:
    cmp     $32, %eax
    jge     .ae_kd
    cmp     %ecx, %eax
    jge     .ae_kp
    movzbl  (%rdi, %rax, 2), %r9d
    mov     %r9b, (%r8, %rax)
    inc     %eax
    jmp     .ae_dk
.ae_kp:
    movb    $0, (%r8, %rax)
    inc     %eax
    jmp     .ae_dk
.ae_kd:
    movdqu  (%r8), %xmm1
    movdqu  16(%r8), %xmm2
    lea     64(%r8), %r10
    xor     %eax, %eax
.ae_dd:
    cmp     %edx, %eax
    jge     .ae_pad
    movzbl  (%rsi, %rax, 2), %r9d
    mov     %r9b, (%r10, %rax)
    inc     %eax
    jmp     .ae_dd
.ae_pad:
    mov     %eax, %r11d
    or      $0xF, %eax
    inc     %eax
    mov     %eax, %ebx
    mov     %ebx, %ecx
    sub     %r11d, %ecx
.ae_pl:
    cmp     %ebx, %r11d
    jge     .ae_enc
    mov     %cl, (%r10, %r11)
    inc     %r11d
    jmp     .ae_pl
.ae_enc:
    lea     64(%r8), %rsi
    lea     1024(%r8), %rdi
    xor     %ecx, %ecx
.ae_eb:
    cmp     %ebx, %ecx
    jge     .ae_ed
    movdqu  (%rsi, %rcx), %xmm0
    pxor    %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenc  %xmm1, %xmm0
    aesenc  %xmm2, %xmm0
    aesenclast %xmm1, %xmm0
    movdqu  %xmm0, (%rdi, %rcx)
    add     $16, %ecx
    jmp     .ae_eb
.ae_ed:
    lea     1024(%r8), %rsi
    mov     %r15, %r9
    xor     %ecx, %ecx
.ae_ts:
    cmp     %ebx, %ecx
    jge     .ae_push
    movzbl  (%rsi, %rcx), %eax
    or      $0x2100, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    inc     %ecx
    jmp     .ae_ts
.ae_push:
    pop     %rbx
    sub     $16, %r14
    mov     %r9, (%r14)
    movq    %rcx, 8(%r14)
    jmp     vm_loop

# __aes_decrypt(key, data) → plaintext
.call_aes_decrypt:
    add     %rcx, %r13
    push    %rbx
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    mov     (%r14), %rdi
    mov     8(%r14), %rcx
    add     $16, %r14
    lea     net_recv_buf(%rip), %r8
    xor     %eax, %eax
.ad_dk:
    cmp     $32, %eax
    jge     .ad_kd
    cmp     %ecx, %eax
    jge     .ad_kp
    movzbl  (%rdi, %rax, 2), %r9d
    mov     %r9b, (%r8, %rax)
    inc     %eax
    jmp     .ad_dk
.ad_kp:
    movb    $0, (%r8, %rax)
    inc     %eax
    jmp     .ad_dk
.ad_kd:
    movdqu  (%r8), %xmm1
    movdqu  16(%r8), %xmm2
    movdqa  %xmm2, %xmm3
    aesimc  %xmm3, %xmm3
    movdqa  %xmm1, %xmm4
    aesimc  %xmm4, %xmm4
    lea     64(%r8), %r10
    xor     %eax, %eax
.ad_dd:
    cmp     %edx, %eax
    jge     .ad_dec
    movzbl  (%rsi, %rax, 2), %r9d
    mov     %r9b, (%r10, %rax)
    inc     %eax
    jmp     .ad_dd
.ad_dec:
    mov     %eax, %ebx
    lea     64(%r8), %rsi
    lea     1024(%r8), %rdi
    xor     %ecx, %ecx
.ad_db:
    cmp     %ebx, %ecx
    jge     .ad_dd2
    movdqu  (%rsi, %rcx), %xmm0
    pxor    %xmm1, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdec  %xmm4, %xmm0
    aesdec  %xmm3, %xmm0
    aesdeclast %xmm1, %xmm0
    movdqu  %xmm0, (%rdi, %rcx)
    add     $16, %ecx
    jmp     .ad_db
.ad_dd2:
    lea     1024(%r8), %rsi
    movzbl  -1(%rsi, %rbx), %eax
    sub     %eax, %ebx
    mov     %r15, %r9
    xor     %ecx, %ecx
.ad_ts:
    cmp     %ebx, %ecx
    jge     .ad_push
    movzbl  (%rsi, %rcx), %eax
    or      $0x2100, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    inc     %ecx
    jmp     .ad_ts
.ad_push:
    pop     %rbx
    sub     $16, %r14
    mov     %r9, (%r14)
    movq    %rcx, 8(%r14)
    jmp     vm_loop

# __mmap_file(path, size) → ptr
.call_mmap_file:
    add     %rcx, %r13
    push    %rbx
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %r8
    add     $16, %r14
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha_input(%rip), %rdi
    xor     %ecx, %ecx
.mm_dec:
    cmp     %rdx, %rcx
    jge     .mm_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .mm_dec
.mm_open:
    movb    $0, (%rdi, %rcx)
    lea     sha_input(%rip), %rdi
    mov     $0x42, %esi
    mov     $0644, %edx
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      .mm_fail
    mov     %eax, %ebx
    mov     $77, %eax
    mov     %ebx, %edi
    mov     %r8, %rsi
    syscall
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi
    mov     %r8, %rsi
    mov     $3, %edx
    mov     $1, %r10d
    mov     %ebx, %r8d
    xor     %r9d, %r9d
    syscall
    push    %rax
    mov     $SYS_CLOSE, %eax
    mov     %ebx, %edi
    syscall
    pop     %rax
    test    %rax, %rax
    js      .mm_fail
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.mm_fail:
    pop     %rbx
    sub     $16, %r14
    mov     $0xBFF0000000000000, %rax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __munmap_file(ptr, size) → 0
.call_munmap_file:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rsi
    add     $16, %r14
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rdi
    add     $16, %r14
    mov     $SYS_MUNMAP, %eax
    syscall
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __simd_compose4(a,b,c,d) → composed mol
.call_simd_compose4:
    add     %rcx, %r13
    push    %rbx
    movsd   (%r14), %xmm3
    cvttsd2si %xmm3, %r11d
    add     $16, %r14
    movsd   (%r14), %xmm2
    cvttsd2si %xmm2, %r10d
    add     $16, %r14
    movsd   (%r14), %xmm1
    cvttsd2si %xmm1, %r9d
    add     $16, %r14
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %r8d
    add     $16, %r14
    # compose a+b→r8, then +c, then +d (3 iterations)
    mov     $3, %ecx
    lea     ed25519_tmp(%rip), %rdi
    mov     %r9d, (%rdi)
    mov     %r10d, 4(%rdi)
    mov     %r11d, 8(%rdi)
.sc4_loop:
    test    %ecx, %ecx
    jz      .sc4_done
    mov     (%rdi), %r9d
    # compose r8d + r9d → r8d (per dimension: floor((2*a+b)/3))
    # S
    mov     %r8d, %eax
    shr     $12, %eax
    and     $0xF, %eax
    mov     %r9d, %edx
    shr     $12, %edx
    and     $0xF, %edx
    lea     (%eax,%eax), %eax
    add     %edx, %eax
    xor     %edx, %edx
    push    %rcx
    mov     $3, %ecx
    div     %ecx
    pop     %rcx
    and     $0xF, %eax
    shl     $12, %eax
    mov     %eax, %ebx
    # R
    mov     %r8d, %eax
    shr     $8, %eax
    and     $0xF, %eax
    mov     %r9d, %edx
    shr     $8, %edx
    and     $0xF, %edx
    lea     (%eax,%eax), %eax
    add     %edx, %eax
    xor     %edx, %edx
    push    %rcx
    mov     $3, %ecx
    div     %ecx
    pop     %rcx
    and     $0xF, %eax
    shl     $8, %eax
    or      %eax, %ebx
    # V
    mov     %r8d, %eax
    shr     $5, %eax
    and     $0x7, %eax
    mov     %r9d, %edx
    shr     $5, %edx
    and     $0x7, %edx
    lea     (%eax,%eax), %eax
    add     %edx, %eax
    xor     %edx, %edx
    push    %rcx
    mov     $3, %ecx
    div     %ecx
    pop     %rcx
    and     $0x7, %eax
    shl     $5, %eax
    or      %eax, %ebx
    # A
    mov     %r8d, %eax
    shr     $2, %eax
    and     $0x7, %eax
    mov     %r9d, %edx
    shr     $2, %edx
    and     $0x7, %edx
    lea     (%eax,%eax), %eax
    add     %edx, %eax
    xor     %edx, %edx
    push    %rcx
    mov     $3, %ecx
    div     %ecx
    pop     %rcx
    and     $0x7, %eax
    shl     $2, %eax
    or      %eax, %ebx
    # T
    mov     %r8d, %eax
    and     $0x3, %eax
    mov     %r9d, %edx
    and     $0x3, %edx
    lea     (%eax,%eax), %eax
    add     %edx, %eax
    xor     %edx, %edx
    push    %rcx
    mov     $3, %ecx
    div     %ecx
    pop     %rcx
    and     $0x3, %eax
    or      %eax, %ebx
    mov     %ebx, %r8d
    add     $4, %rdi
    dec     %ecx
    jmp     .sc4_loop
.sc4_done:
    mov     %ebx, %eax
    pop     %rbx
    sub     $16, %r14
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __text_to_pw(str, tbl) → array [pw, freq, ...]
.call_text_to_pw:
    add     %rcx, %r13
    # Guard: if tbl is empty/null, return empty array
    mov     8(%r14), %rax              # tbl marker
    cmp     $ARRAY_MARKER, %rax
    jne     .ttp_guard_check_bytes
    # tbl is an array — check if empty
    mov     (%r14), %rax
    test    %rax, %rax
    jz      .ttp_empty_result
    cmpq    $0, (%rax)                 # array count == 0?
    je      .ttp_empty_result
    jmp     .ttp_guard_ok
.ttp_guard_check_bytes:
    cmp     $-5, %rax                  # BYTES_MARKER?
    je      .ttp_guard_ok
    # Not array, not bytes — invalid
.ttp_empty_result:
    add     $32, %r14                  # pop both args
    sub     $16, %r14
    mov     %r15, (%r14)               # empty array
    movq    $0, (%r15)                 # count = 0
    add     $8, %r15
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop
.ttp_guard_ok:
    push    %r12
    mov     (%r14), %r9
    add     $16, %r14
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    push    %rsi
    push    %rdx
    mov     (%r9), %r8
    lea     8(%r9), %r9
    push    %r8
    push    %r9
    mov     %r15, %rbx
    lea     65536(%r15), %rbp
    lea     196608(%r15), %r12
    lea     458752(%r15), %r15
    push    %rdi
    mov     %rbx, %rdi
    mov     $458752, %ecx
    xor     %eax, %eax
    rep     stosb
    pop     %rdi
    mov     16(%rsp), %rcx
    mov     24(%rsp), %rsi
    mov     (%rsp), %r9
    mov     8(%rsp), %r8
    xor     %eax, %eax
.ttp_loop:
    cmp     %rcx, %rax
    jge     .ttp_done
    movzwl  (%rsi, %rax, 2), %edi
    and     $0xFF, %edi
    cmp     $0x80, %edi
    jb      .ttp_1b
    cmp     $0xE0, %edi
    jb      .ttp_2b
    cmp     $0xF0, %edi
    jb      .ttp_3b
    and     $0x07, %edi
    shl     $18, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    shl     $12, %edx
    or      %edx, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    shl     $6, %edx
    or      %edx, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    or      %edx, %edi
    inc     %eax
    jmp     .ttp_cp
.ttp_3b:
    and     $0x0F, %edi
    shl     $12, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    shl     $6, %edx
    or      %edx, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    or      %edx, %edi
    inc     %eax
    jmp     .ttp_cp
.ttp_2b:
    and     $0x1F, %edi
    shl     $6, %edi
    inc     %eax
    movzwl  (%rsi, %rax, 2), %edx
    and     $0x3F, %edx
    or      %edx, %edi
    inc     %eax
    jmp     .ttp_cp
.ttp_1b:
    inc     %eax
.ttp_cp:
    push    %rax
    push    %rcx
    push    %rsi
    mov     %edi, %eax
    imul    $40503, %eax
    and     $0xFFFF, %eax
    addl    $1, (%r12, %rax, 4)
    cmpb    $0, (%rbx, %rax)
    jne     .ttp_seen
    movb    $1, (%rbx, %rax)
    mov     %edi, %ecx
    shl     $1, %ecx
    lea     1(%rcx), %edx
    cmp     %r8d, %edx
    jge     .ttp_seen
    movzbl  (%r9, %rcx), %edx
    movzbl  1(%r9, %rcx), %ecx
    shl     $8, %ecx
    or      %ecx, %edx
    movw    %dx, (%rbp, %rax, 2)
.ttp_seen:
    pop     %rsi
    pop     %rcx
    pop     %rax
    jmp     .ttp_loop
.ttp_done:
    add     $32, %rsp
    xor     %ecx, %ecx
    xor     %eax, %eax
.ttp_cnt:
    cmp     $65536, %eax
    jge     .ttp_build
    cmpb    $0, (%rbx, %rax)
    je      .ttp_cn
    inc     %ecx
.ttp_cn:
    inc     %eax
    jmp     .ttp_cnt
.ttp_build:
    mov     %r15, %rdi
    lea     (%rcx, %rcx), %rax
    mov     %rax, (%r15)
    add     $8, %r15
    xor     %eax, %eax
.ttp_emit:
    cmp     $65536, %eax
    jge     .ttp_fin
    cmpb    $0, (%rbx, %rax)
    je      .ttp_en
    push    %rax
    movzwl  (%rbp, %rax, 2), %edx
    cvtsi2sd %edx, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    pop     %rax
    push    %rax
    mov     (%r12, %rax, 4), %edx
    cvtsi2sd %edx, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    pop     %rax
.ttp_en:
    inc     %eax
    jmp     .ttp_emit
.ttp_fin:
    pop     %r12
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __text_to_chain(str, tbl) → array [pw0, pw1, ...]
.call_text_to_chain:
    add     %rcx, %r13
    mov     (%r14), %r9
    add     $16, %r14
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    mov     (%r9), %r8
    lea     8(%r9), %r9
    mov     %r15, %rdi
    add     $8, %r15
    xor     %eax, %eax
    xor     %ecx, %ecx
.ttc_loop:
    cmp     %rdx, %rax
    jge     .ttc_done
    movzwl  (%rsi, %rax, 2), %r10d
    and     $0xFF, %r10d
    cmp     $0x80, %r10d
    jb      .ttc_1b
    cmp     $0xE0, %r10d
    jb      .ttc_2b
    cmp     $0xF0, %r10d
    jb      .ttc_3b
    and     $0x07, %r10d
    shl     $18, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    shl     $12, %r11d
    or      %r11d, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    shl     $6, %r11d
    or      %r11d, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    or      %r11d, %r10d
    inc     %eax
    jmp     .ttc_cp
.ttc_3b:
    and     $0x0F, %r10d
    shl     $12, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    shl     $6, %r11d
    or      %r11d, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    or      %r11d, %r10d
    inc     %eax
    jmp     .ttc_cp
.ttc_2b:
    and     $0x1F, %r10d
    shl     $6, %r10d
    inc     %eax
    movzwl  (%rsi, %rax, 2), %r11d
    and     $0x3F, %r11d
    or      %r11d, %r10d
    inc     %eax
    jmp     .ttc_cp
.ttc_1b:
    inc     %eax
.ttc_cp:
    push    %rax
    push    %rdx
    mov     %r10d, %eax
    shl     $1, %eax
    lea     1(%rax), %r11d
    cmp     %r8d, %r11d
    jge     .ttc_skip
    movzbl  (%r9, %rax), %r11d
    movzbl  1(%r9, %rax), %eax
    shl     $8, %eax
    or      %eax, %r11d
    cvtsi2sd %r11d, %xmm0
    movsd   %xmm0, (%r15)
    movq    $F64_MARKER, 8(%r15)
    add     $16, %r15
    inc     %ecx
.ttc_skip:
    pop     %rdx
    pop     %rax
    jmp     .ttc_loop
.ttc_done:
    movq    %rcx, (%rdi)
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $ARRAY_MARKER, 8(%r14)
    jmp     vm_loop

# __sha512(data) → 128-char hex (FIPS 180-4)
.call_sha512:
    add     %rcx, %r13
    push    %rbx
    mov     (%r14), %rsi
    mov     8(%r14), %rdx
    add     $16, %r14
    lea     sha512_input(%rip), %rdi
    xor     %ecx, %ecx
.s5d:
    cmp     %rdx, %rcx
    jge     .s5dd
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .s5d
.s5dd:
    mov     %ecx, %r8d
    lea     sha512_state(%rip), %rdi
    movabs  $0x6a09e667f3bcc908, %rax; mov %rax, (%rdi)
    movabs  $0xbb67ae8584caa73b, %rax; mov %rax, 8(%rdi)
    movabs  $0x3c6ef372fe94f82b, %rax; mov %rax, 16(%rdi)
    movabs  $0xa54ff53a5f1d36f1, %rax; mov %rax, 24(%rdi)
    movabs  $0x510e527fade682d1, %rax; mov %rax, 32(%rdi)
    movabs  $0x9b05688c2b3e6c1f, %rax; mov %rax, 40(%rdi)
    movabs  $0x1f83d9abfb41bd6b, %rax; mov %rax, 48(%rdi)
    movabs  $0x5be0cd19137e2179, %rax; mov %rax, 56(%rdi)
    lea     sha512_input(%rip), %rdi
    mov     %r8d, %ecx
    movb    $0x80, (%rdi, %rcx)
    inc     %ecx
.s5p:
    mov     %ecx, %eax
    and     $127, %eax
    cmp     $112, %eax
    je      .s5pd
    movb    $0, (%rdi, %rcx)
    inc     %ecx
    jmp     .s5p
.s5pd:
    movq    $0, (%rdi, %rcx)
    add     $8, %ecx
    mov     %r8d, %eax
    shl     $3, %eax
    bswap   %rax
    mov     %rax, (%rdi, %rcx)
    add     $8, %ecx
    # W init
    lea     sha512_input(%rip), %rsi
    lea     sha512_w(%rip), %rdi
    xor     %eax, %eax
.s5wi:
    cmp     $16, %eax
    jge     .s5we
    mov     (%rsi, %rax, 8), %rdx
    bswap   %rdx
    mov     %rdx, (%rdi, %rax, 8)
    inc     %eax
    jmp     .s5wi
.s5we:
    cmp     $80, %eax
    jge     .s5comp
    lea     sha512_w(%rip), %rdi
    lea     -2(%rax), %ecx
    mov     (%rdi, %rcx, 8), %rdx
    mov     %rdx, %r10
    ror     $19, %r10
    mov     %rdx, %r11
    ror     $61, %r11
    xor     %r11, %r10
    mov     %rdx, %r11
    shr     $6, %r11
    xor     %r11, %r10
    lea     -15(%rax), %ecx
    mov     (%rdi, %rcx, 8), %rdx
    mov     %rdx, %r11
    ror     $1, %r11
    mov     %rdx, %rcx
    ror     $8, %rcx
    xor     %rcx, %r11
    shr     $7, %rdx
    xor     %rdx, %r11
    add     %r11, %r10
    lea     -7(%rax), %ecx
    add     (%rdi, %rcx, 8), %r10
    lea     -16(%rax), %ecx
    add     (%rdi, %rcx, 8), %r10
    mov     %r10, (%rdi, %rax, 8)
    inc     %eax
    jmp     .s5we
.s5comp:
    push    %r12
    push    %r14
    push    %r15
    push    %r13
    push    %rbp
    lea     sha512_state(%rip), %rsi
    lea     ed25519_tmp(%rip), %rdi
    xor     %eax, %eax
.s5cs:
    mov     (%rsi, %rax, 8), %rdx
    mov     %rdx, (%rdi, %rax, 8)
    inc     %eax
    cmp     $8, %eax
    jb      .s5cs
    lea     sha512_w(%rip), %r13
    lea     sha512_k(%rip), %rbp
    xor     %r14d, %r14d
.s5r:
    cmp     $80, %r14d
    jge     .s5rd
    lea     ed25519_tmp(%rip), %rdi
    mov     0(%rdi), %r8
    mov     32(%rdi), %r9
    mov     56(%rdi), %rax
    mov     %r9, %r10
    ror     $14, %r10
    mov     %r9, %r11
    ror     $18, %r11
    xor     %r11, %r10
    mov     %r9, %r11
    ror     $41, %r11
    xor     %r11, %r10
    mov     40(%rdi), %r11
    mov     48(%rdi), %r12
    mov     %r9, %rcx
    and     %r11, %rcx
    mov     %r9, %r15
    not     %r15
    and     %r12, %r15
    xor     %r15, %rcx
    add     %r10, %rax
    add     %rcx, %rax
    add     (%rbp, %r14, 8), %rax
    add     (%r13, %r14, 8), %rax
    mov     %r8, %r10
    ror     $28, %r10
    mov     %r8, %r11
    ror     $34, %r11
    xor     %r11, %r10
    mov     %r8, %r11
    ror     $39, %r11
    xor     %r11, %r10
    mov     8(%rdi), %r11
    mov     16(%rdi), %r12
    mov     %r8, %rcx
    and     %r11, %rcx
    mov     %r8, %r15
    and     %r12, %r15
    xor     %r15, %rcx
    mov     %r11, %r15
    and     %r12, %r15
    xor     %r15, %rcx
    add     %rcx, %r10
    mov     48(%rdi), %rcx
    mov     %rcx, 56(%rdi)
    mov     40(%rdi), %rcx
    mov     %rcx, 48(%rdi)
    mov     32(%rdi), %rcx
    mov     %rcx, 40(%rdi)
    mov     24(%rdi), %rcx
    add     %rax, %rcx
    mov     %rcx, 32(%rdi)
    mov     16(%rdi), %rcx
    mov     %rcx, 24(%rdi)
    mov     8(%rdi), %rcx
    mov     %rcx, 16(%rdi)
    mov     0(%rdi), %rcx
    mov     %rcx, 8(%rdi)
    add     %r10, %rax
    mov     %rax, 0(%rdi)
    inc     %r14d
    jmp     .s5r
.s5rd:
    lea     sha512_state(%rip), %rsi
    lea     ed25519_tmp(%rip), %rdi
    xor     %eax, %eax
.s5as:
    mov     (%rdi, %rax, 8), %rdx
    add     %rdx, (%rsi, %rax, 8)
    inc     %eax
    cmp     $8, %eax
    jb      .s5as
    pop     %rbp
    pop     %r13
    pop     %r15
    pop     %r14
    pop     %r12
    lea     sha512_state(%rip), %rsi
    mov     %r15, %rdi
    xor     %edx, %edx
.s5hl:
    mov     (%rsi, %rdx, 8), %rax
    mov     $16, %r8d
.s5bl:
    rol     $4, %rax
    mov     %eax, %r10d
    and     $0xF, %r10d
    add     $0x2130, %r10d
    cmp     $0x213A, %r10d
    jb      .s5hd
    add     $0x27, %r10d
.s5hd:
    movw    %r10w, (%rdi)
    add     $2, %rdi
    dec     %r8d
    jnz     .s5bl
    inc     %edx
    cmp     $8, %edx
    jb      .s5hl
    pop     %rbx
    sub     $16, %r14
    mov     %r15, (%r14)
    movq    $128, 8(%r14)
    mov     %rdi, %r15
    jmp     vm_loop

# SHA-512 K constants
.section .rodata
.align 8
sha512_k:
    .quad 0x428a2f98d728ae22, 0x7137449123ef65cd, 0xb5c0fbcfec4d3b2f, 0xe9b5dba58189dbbc
    .quad 0x3956c25bf348b538, 0x59f111f1b605d019, 0x923f82a4af194f9b, 0xab1c5ed5da6d8118
    .quad 0xd807aa98a3030242, 0x12835b0145706fbe, 0x243185be4ee4b28c, 0x550c7dc3d5ffb4e2
    .quad 0x72be5d74f27b896f, 0x80deb1fe3b1696b1, 0x9bdc06a725c71235, 0xc19bf174cf692694
    .quad 0xe49b69c19ef14ad2, 0xefbe4786384f25e3, 0x0fc19dc68b8cd5b5, 0x240ca1cc77ac9c65
    .quad 0x2de92c6f592b0275, 0x4a7484aa6ea6e483, 0x5cb0a9dcbd41fbd4, 0x76f988da831153b5
    .quad 0x983e5152ee66dfab, 0xa831c66d2db43210, 0xb00327c898fb213f, 0xbf597fc7beef0ee4
    .quad 0xc6e00bf33da88fc2, 0xd5a79147930aa725, 0x06ca6351e003826f, 0x142929670a0e6e70
    .quad 0x27b70a8546d22ffc, 0x2e1b21385c26c926, 0x4d2c6dfc5ac42aed, 0x53380d139d95b3df
    .quad 0x650a73548baf63de, 0x766a0abb3c77b2a8, 0x81c2c92e47edaee6, 0x92722c851482353b
    .quad 0xa2bfe8a14cf10364, 0xa81a664bbc423001, 0xc24b8b70d0f89791, 0xc76c51a30654be30
    .quad 0xd192e819d6ef5218, 0xd69906245565a910, 0xf40e35855771202a, 0x106aa07032bbd1b8
    .quad 0x19a4c116b8d2d0c8, 0x1e376c085141ab53, 0x2748774cdf8eeb99, 0x34b0bcb5e19b48a8
    .quad 0x391c0cb3c5c95a63, 0x4ed8aa4ae3418acb, 0x5b9cca4f7763e373, 0x682e6ff3d6b2b8a3
    .quad 0x748f82ee5defb2fc, 0x78a5636f43172f60, 0x84c87814a1f0ab72, 0x8cc702081a6439ec
    .quad 0x90befffa23631e28, 0xa4506cebde82bde9, 0xbef9a3f7b2c67915, 0xc67178f2e372532b
    .quad 0xca273eceea26619c, 0xd186b8c721c0c207, 0xeada7dd6cde0eb1e, 0xf57d4f7fee6ed178
    .quad 0x06f067aa72176fba, 0x0a637dc5a2c898a6, 0x113f9804bef90dae, 0x1b710b35131c471b
    .quad 0x28db77f523047d84, 0x32caab7b40c72493, 0x3c9ebe0a15c9bebc, 0x431d67c49c100d4c
    .quad 0x4cc5d4becb3e42b6, 0x597f299cfc657e2a, 0x5fcb6fab3ad6faec, 0x6c44198c4a475817
.section .text

# ═══════════════════════════════════════════════════════════════════════════

op_ret:
    # Return from closure call — pop return address from CPU stack.
    pop     %rax                        # saved PC
    pop     %r12                        # saved bytecode base
    mov     %eax, %r13d                 # PC = return address
    # Scope restore: truncate var_table if depth > 0
    lea     closure_depth(%rip), %rsi
    cmpq    $0, (%rsi)
    je      vm_loop                     # depth==0 (boot top-level) → no scope to restore
    decq    (%rsi)                      # depth--
    # Check skip_scope_restore flag (module loading)
    lea     skip_scope_restore(%rip), %rax
    cmpq    $0, (%rax)
    je      .do_scope_restore
    decq    (%rax)                      # skip-- (don't truncate)
    jmp     vm_loop
.do_scope_restore:
    mov     (%rsi), %rcx               # new depth
    # Restore count from depth_stack[depth]
    lea     scope_frame_ptrs(%rip), %rax
    mov     (%rax, %rcx, 8), %rdx      # saved_count
    lea     var_table_base(%rip), %rdi
    mov     (%rdi), %rdi
    mov     %rdx, (%rdi)               # truncate: var_table.count = saved_count
    # Invalidate var_cache after scope restore (entry pointers may be stale)
    push    %rdi
    push    %rcx
    push    %rax
    lea     var_cache(%rip), %rdi
    xor     %eax, %eax
    mov     $768, %ecx              # 6144/8 = 768 qwords
    rep     stosq
    pop     %rax
    pop     %rcx
    pop     %rdi
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# SHA-256 (FIPS 180-4) — __sha256(string) → hex string (64 chars)
# ═══════════════════════════════════════════════════════════════════════════
.call_sha256:
    add     %rcx, %r13              # skip name bytes
    # Pop string: ptr=chain ptr, len=mol_count (u16 molecules)
    mov     (%r14), %rsi            # string ptr (u16 molecules)
    mov     8(%r14), %rdx           # string len (mol count)
    add     $16, %r14               # pop

    # Decode u16 string → raw bytes in sha_input buffer
    lea     sha_input(%rip), %rdi
    xor     %rcx, %rcx              # byte count
    test    %rdx, %rdx
    jz      .sha_empty_input
    mov     %rdx, %r8               # save mol count
.sha_decode_loop:
    cmp     %r8, %rcx
    jge     .sha_decode_done
    movzbl  (%rsi, %rcx, 2), %eax   # low byte of u16 mol
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .sha_decode_loop
.sha_decode_done:
.sha_empty_input:
    # rcx = input byte count, rdi = sha_input
    mov     %rcx, %r8               # r8 = msg_len

    # ── SHA-256 core ──
    # Initialize hash state H[0..7]
    lea     sha_state(%rip), %rbx
    movl    $0x6a09e667, 0(%rbx)
    movl    $0xbb67ae85, 4(%rbx)
    movl    $0x3c6ef372, 8(%rbx)
    movl    $0xa54ff53a, 12(%rbx)
    movl    $0x510e527f, 16(%rbx)
    movl    $0x9b05688c, 20(%rbx)
    movl    $0x1f83d9ab, 24(%rbx)
    movl    $0x5be0cd19, 28(%rbx)

    # Padding: append 0x80, zeros, then 64-bit big-endian length
    lea     sha_input(%rip), %rdi
    mov     %r8, %rcx               # msg_len
    movb    $0x80, (%rdi, %rcx)     # append 0x80
    inc     %rcx
    # Pad with zeros until (len % 64) == 56
.sha_pad_loop:
    mov     %rcx, %rax
    and     $63, %rax
    cmp     $56, %rax
    je      .sha_pad_done
    movb    $0, (%rdi, %rcx)
    inc     %rcx
    jmp     .sha_pad_loop
.sha_pad_done:
    # Append bit length (msg_len * 8) as big-endian u64
    mov     %r8, %rax
    shl     $3, %rax                 # bit length
    bswap   %rax
    mov     %rax, (%rdi, %rcx)
    add     $8, %rcx
    # rcx = total padded length (multiple of 64)

    # Process each 64-byte block
    lea     sha_input(%rip), %rdi
    xor     %r9, %r9                 # block offset
.sha_block_loop:
    cmp     %rcx, %r9
    jge     .sha_done

    # Save rcx/r9/rdi BEFORE W extension (W extend clobbers ecx)
    lea     sha_save_regs(%rip), %rax
    mov     %rcx, 24(%rax)            # padded length
    mov     %r9,  16(%rax)            # block offset
    mov     %rdi, 32(%rax)            # input pointer

    # Prepare message schedule W[0..63]
    lea     sha_w(%rip), %r10
    # W[0..15] = block words (big-endian)
    xor     %r11, %r11
.sha_w_init:
    cmp     $16, %r11
    jge     .sha_w_extend
    lea     (%rdi, %r9), %rax
    mov     (%rax, %r11, 4), %eax
    bswap   %eax
    mov     %eax, (%r10, %r11, 4)
    inc     %r11
    jmp     .sha_w_init

.sha_w_extend:
    cmp     $64, %r11
    jge     .sha_compress
    # W[i] = σ1(W[i-2]) + W[i-7] + σ0(W[i-15]) + W[i-16]
    # σ0(x) = ROTR(7,x) ^ ROTR(18,x) ^ SHR(3,x)
    # σ1(x) = ROTR(17,x) ^ ROTR(19,x) ^ SHR(10,x)
    lea     -2(%r11), %rax
    mov     (%r10, %rax, 4), %edx   # W[i-2]
    mov     %edx, %eax
    ror     $17, %eax
    mov     %edx, %ecx
    ror     $19, %ecx
    xor     %ecx, %eax
    mov     %edx, %ecx
    shr     $10, %ecx
    xor     %ecx, %eax               # σ1(W[i-2])
    push    %rax                      # save σ1

    lea     -15(%r11), %rax
    mov     (%r10, %rax, 4), %edx   # W[i-15]
    mov     %edx, %eax
    ror     $7, %eax
    mov     %edx, %ecx
    ror     $18, %ecx
    xor     %ecx, %eax
    mov     %edx, %ecx
    shr     $3, %ecx
    xor     %ecx, %eax               # σ0(W[i-15])

    pop     %rdx                      # σ1
    add     %edx, %eax               # σ1 + σ0
    lea     -7(%r11), %edx
    add     (%r10, %rdx, 4), %eax   # + W[i-7]
    lea     -16(%r11), %edx
    add     (%r10, %rdx, 4), %eax   # + W[i-16]
    mov     %eax, (%r10, %r11, 4)
    inc     %r11
    jmp     .sha_w_extend

.sha_compress:
    # Save remaining VM registers (rcx/r9/rdi already saved at block_loop)
    lea     sha_save_regs(%rip), %rax
    mov     %r12, 0(%rax)
    mov     %rbp, 8(%rax)
    # r9, rcx, rdi already saved before W extension
    mov     %rbx, 40(%rax)
    mov     %r14, 48(%rax)
    mov     %r15, 56(%rax)

    # Initialize working vars from state → sha_abcdefgh
    lea     sha_state(%rip), %rbx
    lea     sha_abcdefgh(%rip), %rbp
    mov     0(%rbx), %eax; mov %eax, 0(%rbp)
    mov     4(%rbx), %eax; mov %eax, 4(%rbp)
    mov     8(%rbx), %eax; mov %eax, 8(%rbp)
    mov     12(%rbx), %eax; mov %eax, 12(%rbp)
    mov     16(%rbx), %eax; mov %eax, 16(%rbp)
    mov     20(%rbx), %eax; mov %eax, 20(%rbp)
    mov     24(%rbx), %eax; mov %eax, 24(%rbp)
    mov     28(%rbx), %eax; mov %eax, 28(%rbp)

    # 64 compression rounds
    lea     sha_k(%rip), %r10
    lea     sha_w(%rip), %r11
    xor     %r12d, %r12d
.sha_round:
    cmp     $64, %r12d
    jge     .sha_round_done

    # Σ1(e) = ROTR(6,e) ^ ROTR(11,e) ^ ROTR(25,e)
    mov     16(%rbp), %eax           # e
    mov     %eax, %edx
    ror     $6, %edx
    mov     %eax, %ecx
    ror     $11, %ecx
    xor     %ecx, %edx
    mov     %eax, %ecx
    ror     $25, %ecx
    xor     %ecx, %edx               # Σ1(e)

    # Ch(e,f,g) = (e & f) ^ (~e & g)
    mov     16(%rbp), %eax           # e
    mov     20(%rbp), %ecx           # f
    and     %eax, %ecx               # e & f
    not     %eax
    and     24(%rbp), %eax           # ~e & g
    xor     %eax, %ecx               # Ch

    # T1 = h + Σ1(e) + Ch(e,f,g) + K[i] + W[i]
    mov     28(%rbp), %eax           # h
    add     %edx, %eax               # + Σ1
    add     %ecx, %eax               # + Ch
    add     (%r10, %r12, 4), %eax   # + K[i]
    add     (%r11, %r12, 4), %eax   # + W[i]
    mov     %eax, %esi               # T1

    # Σ0(a) = ROTR(2,a) ^ ROTR(13,a) ^ ROTR(22,a)
    mov     0(%rbp), %eax            # a
    mov     %eax, %edx
    ror     $2, %edx
    mov     %eax, %ecx
    ror     $13, %ecx
    xor     %ecx, %edx
    mov     %eax, %ecx
    ror     $22, %ecx
    xor     %ecx, %edx               # Σ0(a)

    # Maj(a,b,c) = (a & b) ^ (a & c) ^ (b & c)
    mov     0(%rbp), %eax            # a
    mov     4(%rbp), %ecx            # b
    mov     %eax, %edi
    and     %ecx, %edi               # a & b
    and     8(%rbp), %eax            # a & c
    xor     %eax, %edi
    and     8(%rbp), %ecx            # b & c
    xor     %ecx, %edi               # Maj

    # T2 = Σ0(a) + Maj(a,b,c)
    add     %edi, %edx               # T2

    # Shift: h=g, g=f, f=e, e=d+T1, d=c, c=b, b=a, a=T1+T2
    mov     24(%rbp), %eax; mov %eax, 28(%rbp)  # h = g
    mov     20(%rbp), %eax; mov %eax, 24(%rbp)  # g = f
    mov     16(%rbp), %eax; mov %eax, 20(%rbp)  # f = e
    mov     12(%rbp), %eax
    add     %esi, %eax
    mov     %eax, 16(%rbp)                       # e = d + T1
    mov     8(%rbp), %eax; mov %eax, 12(%rbp)   # d = c
    mov     4(%rbp), %eax; mov %eax, 8(%rbp)    # c = b
    mov     0(%rbp), %eax; mov %eax, 4(%rbp)    # b = a
    lea     (%rsi, %rdx), %eax
    mov     %eax, 0(%rbp)                        # a = T1 + T2

    inc     %r12d
    jmp     .sha_round

.sha_round_done:
    # Add compressed values to state BEFORE restoring rbp
    # (rbp still points to sha_abcdefgh from compression)
    lea     sha_state(%rip), %rbx
    mov     0(%rbp), %eax;  add %eax, 0(%rbx)
    mov     4(%rbp), %eax;  add %eax, 4(%rbx)
    mov     8(%rbp), %eax;  add %eax, 8(%rbx)
    mov     12(%rbp), %eax; add %eax, 12(%rbx)
    mov     16(%rbp), %eax; add %eax, 16(%rbx)
    mov     20(%rbp), %eax; add %eax, 20(%rbx)
    mov     24(%rbp), %eax; add %eax, 24(%rbx)
    mov     28(%rbp), %eax; add %eax, 28(%rbx)

    # NOW restore ALL VM registers from BSS
    lea     sha_save_regs(%rip), %rax
    mov     0(%rax), %r12
    mov     8(%rax), %rbp
    mov     16(%rax), %r9
    mov     24(%rax), %rcx
    mov     32(%rax), %rdi
    mov     40(%rax), %rbx
    mov     48(%rax), %r14
    mov     56(%rax), %r15

    add     $64, %r9
    jmp     .sha_block_loop

.sha_done:
    # Convert state H[0..7] to hex string on heap (u16 molecules)
    lea     sha_state(%rip), %rbx
    mov     %r15, %rdi               # output string base on heap
    movq    $0, %rcx                 # output mol count
    # 8 words × 8 hex chars = 64 chars
    xor     %r8d, %r8d               # word index
.sha_hex_loop:
    cmp     $8, %r8d
    jge     .sha_hex_done
    mov     (%rbx, %r8, 4), %eax
    # State words stored as native u32, already correct order — NO bswap needed
    # Emit 8 hex nibbles
    mov     $8, %r9d                  # nibble count
.sha_nibble:
    test    %r9d, %r9d
    jz      .sha_next_word
    dec     %r9d
    mov     %eax, %edx
    shr     $28, %edx                # top nibble
    shl     $4, %eax                 # shift left
    # Convert nibble to ASCII hex
    cmp     $10, %edx
    jl      .sha_digit
    add     $87, %edx                # 'a' - 10 = 87
    jmp     .sha_emit_hex
.sha_digit:
    add     $48, %edx                # '0' = 48
.sha_emit_hex:
    # Emit as u16 molecule: 0x2100 | byte
    or      $0x2100, %edx
    movw    %dx, (%r15)
    add     $2, %r15
    inc     %rcx
    jmp     .sha_nibble
.sha_next_word:
    inc     %r8d
    jmp     .sha_hex_loop

.sha_hex_done:
    # Push result string onto VM stack
    sub     $16, %r14
    mov     %rdi, (%r14)             # string ptr
    mov     %rcx, 8(%r14)            # mol count (64)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# __regex_match(text, pattern) → 1/0
# Thompson NFA simulation in pure ASM. Supports: . * + ? [a-z]
# Uses rx_* BSS buffers for NFA states and active set tracking.
# ═══════════════════════════════════════════════════════════════════════════
.call_regex_match:
    add     %rcx, %r13
    movb    $0, rx_search_mode(%rip)   # anchored match mode
    # Pop pattern (top), text (below)
    mov     (%r14), %rdi              # pattern ptr (u16 mols)
    mov     8(%r14), %rcx             # pattern mol_count
    mov     16(%r14), %rsi            # text ptr (u16 mols)
    mov     24(%r14), %rdx            # text mol_count
    add     $32, %r14                 # pop 2

    push    %rbx
    push    %rbp
    push    %r12
    push    %r13
    push    %r14
    push    %r15

    # Save text info
    mov     %rsi, %r12               # r12 = text ptr
    mov     %rdx, %r13               # r13 = text mol_count
    # Pattern info
    mov     %rdi, %rsi               # rsi = pattern ptr
    mov     %rcx, %rdx               # rdx = pattern mol_count

    # ── Build NFA from pattern ──
    # Decode pattern mols → ASCII bytes in rx_pattern BSS
    lea     rx_pattern(%rip), %rdi
    xor     %ecx, %ecx
._rx_decode_pat:
    cmp     %rdx, %rcx
    jge     ._rx_pat_done
    movzbl  (%rsi, %rcx, 2), %eax
    movb    %al, (%rdi, %rcx)
    inc     %rcx
    jmp     ._rx_decode_pat
._rx_pat_done:
    mov     %ecx, %ebp               # rbp = pattern_len (ASCII bytes)

    # Decode text mols → ASCII bytes in rx_text BSS
    lea     rx_text(%rip), %rdi
    mov     %r12, %rsi
    xor     %ecx, %ecx
._rx_decode_text:
    cmp     %r13, %rcx
    jge     ._rx_text_done
    movzbl  (%rsi, %rcx, 2), %eax
    movb    %al, (%rdi, %rcx)
    inc     %rcx
    jmp     ._rx_decode_text
._rx_text_done:
    # Now: rx_pattern has pattern bytes, rbp = pat_len
    #      rx_text has text bytes, r13 = text_len

    # ── Build NFA states ──
    # State: [type:1, lo:1, hi:1, pad:1, next1:2, next2:2] = 8 bytes
    # type: 0=lit(lo==hi), 1=dot, 2=split, 3=match, 4=range(lo..hi), 5=nrange
    lea     rx_nfa(%rip), %rbx        # rbx = NFA base
    xor     %r8d, %r8d                # r8 = state count
    xor     %r9d, %r9d                # r9 = pattern position
    movl    $-1, rx_alt_patch(%rip)   # no alternation patch pending

._rx_build_loop:
    cmp     %ebp, %r9d
    jge     ._rx_build_done
    lea     rx_pattern(%rip), %rdi
    movzbl  (%rdi, %r9), %eax        # pattern byte

    # Handle '[' character class
    cmp     $'[', %al
    je      ._rx_parse_class

    # Handle '|' alternation
    cmp     $'|', %al
    je      ._rx_parse_alt

    # Handle '(' group — skip for now, treat as literal
    # Handle ')' — skip
    cmp     $'(', %al
    je      ._rx_skip_char
    cmp     $')', %al
    je      ._rx_skip_char

    # Check for quantifier after this char
    lea     1(%r9), %ecx
    xor     %r10d, %r10d              # r10 = quantifier (0=none, 1=*, 2=+, 3=?)
    cmp     %ebp, %ecx
    jge     ._rx_no_quant
    movzbl  (%rdi, %rcx), %edx
    cmp     $'*', %edx
    je      ._rx_quant_star
    cmp     $'+', %edx
    je      ._rx_quant_plus
    cmp     $'?', %edx
    je      ._rx_quant_qmark
    jmp     ._rx_no_quant
._rx_quant_star:
    mov     $1, %r10d
    inc     %r9d
    jmp     ._rx_no_quant
._rx_quant_plus:
    mov     $2, %r10d
    inc     %r9d
    jmp     ._rx_no_quant
._rx_quant_qmark:
    mov     $3, %r10d
    inc     %r9d
._rx_no_quant:
    inc     %r9d                       # advance past pattern char

    # Determine state type + lo/hi
    xor     %edx, %edx                # edx = lo
    cmp     $'.', %al
    je      ._rx_type_dot
    # Literal: lo = hi = char
    mov     $0, %cl                    # type = lit
    mov     %al, %dl                   # lo = char
    mov     %al, %al                   # hi = char (already in al)
    jmp     ._rx_emit_state
._rx_type_dot:
    mov     $1, %cl                    # type = dot
    mov     $1, %dl                    # lo = 1
    mov     $255, %al                  # hi = 255
    jmp     ._rx_emit_state

._rx_skip_char:
    inc     %r9d
    jmp     ._rx_build_loop

# ── Parse character class [a-z] [0-9] [^abc] ──
._rx_parse_class:
    inc     %r9d                       # skip [
    mov     $4, %cl                    # type = class range
    # Check negation ^
    cmp     %ebp, %r9d
    jge     ._rx_class_done
    lea     rx_pattern(%rip), %rdi
    movzbl  (%rdi, %r9), %eax
    cmp     $'^', %al
    jne     ._rx_class_first
    mov     $5, %cl                    # type = negated class
    inc     %r9d
._rx_class_first:
    # Read first char
    cmp     %ebp, %r9d
    jge     ._rx_class_done
    movzbl  (%rdi, %r9), %edx         # lo byte
    inc     %r9d
    mov     %edx, %eax                # hi = lo (default: single char)
    # Check for range a-z
    cmp     %ebp, %r9d
    jge     ._rx_class_close
    movzbl  (%rdi, %r9), %esi
    cmp     $'-', %esi
    jne     ._rx_class_close
    inc     %r9d                       # skip -
    cmp     %ebp, %r9d
    jge     ._rx_class_close
    movzbl  (%rdi, %r9), %eax         # hi byte
    inc     %r9d
._rx_class_close:
    # Skip to closing ]
    cmp     %ebp, %r9d
    jge     ._rx_class_done
    lea     rx_pattern(%rip), %rdi
    cmpb    $']', (%rdi, %r9)
    jne     ._rx_class_close           # skip extra chars until ]
    inc     %r9d                       # skip ]
._rx_class_done:
    # dl = lo, al = hi, cl = type (4 or 5)
    # Check quantifier
    xor     %r10d, %r10d
    cmp     %ebp, %r9d
    jge     ._rx_emit_state
    lea     rx_pattern(%rip), %rdi
    movzbl  (%rdi, %r9), %esi
    cmp     $'*', %esi
    jne     ._rx_cc_not_star
    mov     $1, %r10d; inc %r9d; jmp ._rx_emit_state
._rx_cc_not_star:
    cmp     $'+', %esi
    jne     ._rx_cc_not_plus
    mov     $2, %r10d; inc %r9d; jmp ._rx_emit_state
._rx_cc_not_plus:
    cmp     $'?', %esi
    jne     ._rx_emit_state
    mov     $3, %r10d; inc %r9d
    jmp     ._rx_emit_state

# ── Parse alternation a|b ──
# Strategy: shift existing states right by 1, insert split at 0.
# Left branch last state → patched at build_done to point to accept.
# Save left-branch-end in rx_alt_patch BSS.
._rx_parse_alt:
    inc     %r9d                       # skip |
    # Shift all states [0..r8-1] right by 1
    mov     %r8d, %ecx
._rx_alt_shift:
    dec     %ecx
    jl      ._rx_alt_shifted
    lea     (%rbx, %rcx, 8), %rsi
    lea     8(%rsi), %rdi
    movq    (%rsi), %rax
    movq    %rax, (%rdi)
    # Increment next1/next2 pointers
    movzwl  4(%rdi), %eax
    cmp     $0xFFFF, %eax
    je      ._rx_alt_s1
    inc     %eax
    movw    %ax, 4(%rdi)
._rx_alt_s1:
    movzwl  6(%rdi), %eax
    cmp     $0xFFFF, %eax
    je      ._rx_alt_s2
    inc     %eax
    movw    %ax, 6(%rdi)
._rx_alt_s2:
    jmp     ._rx_alt_shift
._rx_alt_shifted:
    # Insert split at state 0
    mov     %rbx, %rdi
    movb    $2, (%rdi)                # type = split
    movb    $0, 1(%rdi)
    movb    $0, 2(%rdi)
    movb    $0, 3(%rdi)
    movw    $1, 4(%rdi)               # next1 = 1 (left branch)
    inc     %r8d                       # account for split
    # Left branch last state = r8-1 (the old last state, now shifted)
    # Its next1 currently points to r8 (right branch start). Save for patching.
    mov     %r8d, %eax
    dec     %eax                       # last left branch state index
    movl    %eax, rx_alt_patch(%rip)   # save for ._rx_build_done to patch
    # Set split.next2 = r8 (right branch starts here)
    movw    %r8w, 6(%rbx)             # split next2 = current r8
    jmp     ._rx_build_loop

._rx_emit_state:
    # cl = type, dl = lo, al = hi, r10 = quantifier
    # Create NFA state(s) based on quantifier
    lea     (%rbx, %r8, 8), %rdi     # state ptr
    cmp     $0, %r10d
    je      ._rx_emit_plain
    cmp     $1, %r10d
    je      ._rx_emit_star
    cmp     $2, %r10d
    je      ._rx_emit_plus
    cmp     $3, %r10d
    je      ._rx_emit_qmark

._rx_emit_plain:
    # [type, lo, hi, pad, next=r8+1, -1]
    movb    %cl, (%rdi)               # type
    movb    %dl, 1(%rdi)              # lo
    movb    %al, 2(%rdi)              # hi
    movb    $0, 3(%rdi)               # pad
    lea     1(%r8), %esi
    movw    %si, 4(%rdi)              # next1
    movw    $-1, 6(%rdi)              # next2
    inc     %r8d
    jmp     ._rx_build_loop

._rx_emit_star:
    # Split: [2,0,0,0, match, skip]
    movb    $2, (%rdi)
    movb    $0, 1(%rdi)
    movb    $0, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     1(%r8), %esi
    movw    %si, 4(%rdi)              # next1 = match state
    lea     2(%r8), %esi
    movw    %si, 6(%rdi)              # next2 = skip
    inc     %r8d
    # Match state: [type, lo, hi, 0, split_back, -1]
    lea     (%rbx, %r8, 8), %rdi
    movb    %cl, (%rdi)
    movb    %dl, 1(%rdi)
    movb    %al, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     -1(%r8), %esi
    movw    %si, 4(%rdi)              # back to split
    movw    $-1, 6(%rdi)
    inc     %r8d
    jmp     ._rx_build_loop

._rx_emit_plus:
    # Match first: [type, lo, hi, 0, split, -1]
    movb    %cl, (%rdi)
    movb    %dl, 1(%rdi)
    movb    %al, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     1(%r8), %esi
    movw    %si, 4(%rdi)
    movw    $-1, 6(%rdi)
    inc     %r8d
    # Split: [2, 0, 0, 0, match_back, skip]
    lea     (%rbx, %r8, 8), %rdi
    movb    $2, (%rdi)
    movb    $0, 1(%rdi)
    movb    $0, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     -1(%r8), %esi
    movw    %si, 4(%rdi)
    lea     1(%r8), %esi
    movw    %si, 6(%rdi)
    inc     %r8d
    jmp     ._rx_build_loop

._rx_emit_qmark:
    # Split: [2, 0, 0, 0, match, skip]
    movb    $2, (%rdi)
    movb    $0, 1(%rdi)
    movb    $0, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     1(%r8), %esi
    movw    %si, 4(%rdi)
    lea     2(%r8), %esi
    movw    %si, 6(%rdi)
    inc     %r8d
    # Match: [type, lo, hi, 0, continue, -1]
    lea     (%rbx, %r8, 8), %rdi
    movb    %cl, (%rdi)
    movb    %dl, 1(%rdi)
    movb    %al, 2(%rdi)
    movb    $0, 3(%rdi)
    lea     1(%r8), %esi
    movw    %si, 4(%rdi)
    movw    $-1, 6(%rdi)
    inc     %r8d
    jmp     ._rx_build_loop

._rx_build_done:
    # Add accept state
    lea     (%rbx, %r8, 8), %rdi
    movb    $3, (%rdi)                # type = match/accept
    movb    $0, 1(%rdi)
    movb    $0, 2(%rdi)
    movb    $0, 3(%rdi)
    movw    $-1, 4(%rdi)              # next1 = none
    movw    $-1, 6(%rdi)              # next2 = none
    inc     %r8d
    # r8 = total state count, rbx = NFA base

    # Patch alternation: if rx_alt_patch >= 0, fix left branch end → accept
    movl    rx_alt_patch(%rip), %eax
    cmp     $-1, %eax
    je      ._rx_no_alt_patch
    # State[eax].next1 = accept state (r8-1)
    lea     (%rbx, %rax, 8), %rdi
    lea     -1(%r8), %esi
    movw    %si, 4(%rdi)              # patch next1 → accept
    movl    $-1, rx_alt_patch(%rip)   # reset
._rx_no_alt_patch:

    # Check if search mode (called from __regex_search)
    cmpb    $1, rx_search_mode(%rip)
    jne     ._rx_match_sim
    movb    $0, rx_search_mode(%rip)   # reset flag
    jmp     ._rxs_simulate

._rx_match_sim:
    # ── Simulate NFA (Thompson) ──
    # Two sets: current active states, next active states
    # Each set = bitmap (1 bit per state, max 256 states)
    lea     rx_cur_set(%rip), %r14     # WARNING: reusing r14 temporarily
    lea     rx_next_set(%rip), %r15    # WARNING: reusing r15 temporarily

    # Initialize: add state 0 to current set (follow epsilon/splits)
    xor     %eax, %eax
    mov     $4, %ecx
    mov     %r14, %rdi
    rep     stosq                      # zero 32 bytes (256 bits)
    mov     $0, %edi                   # start state = 0
    call    ._rx_add_state

    # Process each text byte
    xor     %r9d, %r9d                # r9 = text position
._rx_sim_loop:
    cmp     %r13d, %r9d
    jge     ._rx_sim_done

    # Zero next set
    push    %r9
    mov     %r15, %rdi
    xor     %eax, %eax
    mov     $4, %ecx
    rep     stosq
    pop     %r9

    # Get current text byte
    lea     rx_text(%rip), %rdi
    movzbl  (%rdi, %r9), %eax        # current byte

    # For each active state in current set
    xor     %ecx, %ecx                # state index
._rx_sim_state:
    cmp     %r8d, %ecx
    jge     ._rx_sim_advance
    # Check if state is active (bit test)
    bt      %ecx, (%r14)
    jnc     ._rx_sim_state_next

    # State is active — check if it matches current byte
    lea     (%rbx, %rcx, 8), %rdi    # state ptr
    movzbl  (%rdi), %edx              # type
    cmp     $3, %edx                   # accept?
    je      ._rx_sim_state_next       # skip accept states in processing
    cmp     $2, %edx                   # split? (shouldn't be — splits resolved by add_state)
    je      ._rx_sim_state_next

    # Check match
    mov     $0, %esi                   # match = 0
    cmp     $1, %edx                   # dot?
    je      ._rx_sim_dot_match
    # Check match by type
    cmp     $4, %edx                   # class range [a-z]?
    je      ._rx_sim_class
    cmp     $5, %edx                   # negated class [^a-z]?
    je      ._rx_sim_nclass
    # Literal match (type 0): lo == hi == char
    movzbl  1(%rdi), %edx             # lo byte
    cmp     %eax, %edx
    jne     ._rx_sim_state_next
    jmp     ._rx_sim_matched
._rx_sim_class:
    # Class match: lo <= byte <= hi
    movzbl  1(%rdi), %edx             # lo
    cmp     %edx, %eax
    jl      ._rx_sim_state_next       # byte < lo
    movzbl  2(%rdi), %edx             # hi
    cmp     %edx, %eax
    jg      ._rx_sim_state_next       # byte > hi
    jmp     ._rx_sim_matched
._rx_sim_nclass:
    # Negated class: byte < lo || byte > hi
    movzbl  1(%rdi), %edx             # lo
    cmp     %edx, %eax
    jl      ._rx_sim_matched          # byte < lo = match
    movzbl  2(%rdi), %edx             # hi
    cmp     %edx, %eax
    jg      ._rx_sim_matched          # byte > hi = match
    jmp     ._rx_sim_state_next       # in range = no match
._rx_sim_dot_match:
._rx_sim_matched:
    # Match! Add next state(s) to next set
    push    %rcx
    push    %rax
    push    %r9
    movzwl  4(%rdi), %edi             # next1 (offset 4 in new layout)
    cmp     $0xFFFF, %edi
    je      ._rx_sim_no_next
    # Temporarily swap r14/r15 so add_state writes to next set
    xchg    %r14, %r15
    call    ._rx_add_state
    xchg    %r14, %r15
._rx_sim_no_next:
    pop     %r9
    pop     %rax
    pop     %rcx

._rx_sim_state_next:
    inc     %ecx
    jmp     ._rx_sim_state

._rx_sim_advance:
    # Swap current ↔ next
    xchg    %r14, %r15
    inc     %r9d
    jmp     ._rx_sim_loop

._rx_sim_done:
    # Check if any accept state is active in current set
    xor     %eax, %eax                # result = 0
    xor     %ecx, %ecx
._rx_check_accept:
    cmp     %r8d, %ecx
    jge     ._rx_return
    bt      %ecx, (%r14)
    jnc     ._rx_check_next
    lea     (%rbx, %rcx, 8), %rdi
    cmpb    $3, (%rdi)
    jne     ._rx_check_next
    mov     $1, %eax                   # found accept!
._rx_check_next:
    inc     %ecx
    jmp     ._rx_check_accept

._rx_return:
    # Restore VM registers
    pop     %r15
    pop     %r14
    pop     %r13
    pop     %r12
    pop     %rbp
    pop     %rbx
    # Push result as f64
    sub     $16, %r14
    test    %eax, %eax
    jz      ._rx_push_zero
    mov     $0x3FF0000000000000, %rax  # 1.0
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
._rx_push_zero:
    xor     %eax, %eax
    mov     %rax, (%r14)                # 0.0 bits
    movq    $F64_MARKER, 8(%r14)        # mark as f64
    jmp     vm_loop

# Helper: add state to active set (r14), following epsilon/splits
# edi = state id, rbx = NFA base, r8 = state count, r14 = set bitmap
._rx_add_state:
    cmp     %r8d, %edi
    jge     ._rx_add_ret
    cmp     $0, %edi
    jl      ._rx_add_ret
    # Already in set?
    bt      %edi, (%r14)
    jc      ._rx_add_ret
    # Add to set
    bts     %edi, (%r14)
    # If split state, follow both branches
    lea     (%rbx, %rdi, 8), %rax
    cmpb    $2, (%rax)
    jne     ._rx_add_ret
    # Follow next1 (offset 4 in new 8-byte layout)
    push    %rdi
    movzwl  4(%rax), %edi
    cmp     $0xFFFF, %edi
    je      ._rx_add_skip1
    call    ._rx_add_state
._rx_add_skip1:
    pop     %rdi
    lea     (%rbx, %rdi, 8), %rax
    movzwl  6(%rax), %edi
    cmp     $0xFFFF, %edi
    je      ._rx_add_ret
    call    ._rx_add_state
._rx_add_ret:
    ret

# ═══════════════════════════════════════════════════════════════════════════
# __regex_search(text, pattern) → 1/0
# Like __regex_match but tries matching at each start position (unanchored).
# Returns 1 if pattern matches anywhere in text.
# ═══════════════════════════════════════════════════════════════════════════
.call_regex_search:
    add     %rcx, %r13
    # Pop pattern (top), text (below) — same as __regex_match
    mov     (%r14), %rdi              # pattern ptr
    mov     8(%r14), %rcx             # pattern mol_count
    mov     16(%r14), %rsi            # text ptr
    mov     24(%r14), %rdx            # text mol_count
    add     $32, %r14                 # pop 2

    push    %rbx
    push    %rbp
    push    %r12
    push    %r13
    push    %r14
    push    %r15

    mov     %rsi, %r12               # r12 = text ptr
    mov     %rdx, %r13               # r13 = text mol_count
    mov     %rdi, %rsi               # rsi = pattern ptr
    mov     %rcx, %rdx               # rdx = pattern mol_count

    # Decode pattern → rx_pattern
    lea     rx_pattern(%rip), %rdi
    xor     %ecx, %ecx
._rxs_decode_pat:
    cmp     %rdx, %rcx
    jge     ._rxs_pat_done
    movzbl  (%rsi, %rcx, 2), %eax
    movb    %al, (%rdi, %rcx)
    inc     %rcx
    jmp     ._rxs_decode_pat
._rxs_pat_done:
    mov     %ecx, %ebp               # rbp = pattern_len

    # Decode text → rx_text
    lea     rx_text(%rip), %rdi
    mov     %r12, %rsi
    xor     %ecx, %ecx
._rxs_decode_text:
    cmp     %r13, %rcx
    jge     ._rxs_text_done
    movzbl  (%rsi, %rcx, 2), %eax
    movb    %al, (%rdi, %rcx)
    inc     %rcx
    jmp     ._rxs_decode_text
._rxs_text_done:

    # Build NFA (reuse same build code path)
    lea     rx_nfa(%rip), %rbx
    xor     %r8d, %r8d
    xor     %r9d, %r9d
    jmp     ._rxs_build_entry

._rxs_build_entry:
    # We jump into the shared NFA builder but return to our search loop
    # Actually, simpler: just duplicate the build call via shared labels
    # The builder uses: rbx=NFA base, r8=state count, r9=pat pos, rbp=pat len
    # It builds into rx_nfa and returns at ._rx_build_done
    # Problem: ._rx_build_done flows into simulation code for __regex_match
    # Solution: save a flag and check it, or just inline the build
    # Simplest: call the build loop but patch the return to our code
    # Actually simplest: just jmp to ._rx_build_loop — it will build NFA
    # then fall through to ._rx_build_done which goes to sim code.
    # We need to intercept. Let me use a different approach.
    # Store a search-mode flag in a BSS byte.
    movb    $1, rx_search_mode(%rip)
    jmp     ._rx_build_loop

# Search-mode simulation: try from each start position
._rxs_simulate:
    xor     %r11d, %r11d              # r11 = start position in text

._rxs_try_pos:
    cmp     %r13d, %r11d
    jg      ._rxs_not_found

    # Init current set: zero + add state 0
    lea     rx_cur_set(%rip), %r14
    lea     rx_next_set(%rip), %r15
    xor     %eax, %eax
    mov     $4, %ecx
    mov     %r14, %rdi
    rep     stosq
    mov     %r15, %rdi
    xor     %eax, %eax
    mov     $4, %ecx
    rep     stosq
    mov     $0, %edi
    call    ._rx_add_state

    # Check if accept already active (empty pattern matches)
    xor     %ecx, %ecx
._rxs_check_imm:
    cmp     %r8d, %ecx
    jge     ._rxs_sim_run
    bt      %ecx, (%r14)
    jnc     ._rxs_check_imm_next
    lea     (%rbx, %rcx, 8), %rdi
    cmpb    $3, (%rdi)
    je      ._rxs_found
._rxs_check_imm_next:
    inc     %ecx
    jmp     ._rxs_check_imm

._rxs_sim_run:
    # Run NFA sim from position r11
    mov     %r11d, %r9d               # r9 = current text pos

._rxs_sim_loop:
    cmp     %r13d, %r9d
    jge     ._rxs_next_pos            # no more text, no accept found

    # Zero next set
    push    %r9
    mov     %r15, %rdi
    xor     %eax, %eax
    mov     $4, %ecx
    rep     stosq
    pop     %r9

    # Get current text byte
    lea     rx_text(%rip), %rdi
    movzbl  (%rdi, %r9), %eax

    # For each active state
    xor     %ecx, %ecx
._rxs_sim_state:
    cmp     %r8d, %ecx
    jge     ._rxs_sim_advance
    bt      %ecx, (%r14)
    jnc     ._rxs_sim_state_next

    lea     (%rbx, %rcx, 8), %rdi
    movzbl  (%rdi), %edx
    cmp     $3, %edx
    je      ._rxs_sim_state_next      # skip accept
    cmp     $2, %edx
    je      ._rxs_sim_state_next      # skip split

    # Check match by type
    mov     $0, %esi
    cmp     $1, %edx                   # dot?
    je      ._rxs_dot_match
    cmp     $4, %edx                   # class?
    je      ._rxs_class_match
    cmp     $5, %edx                   # nclass?
    je      ._rxs_nclass_match
    # Literal
    movzbl  1(%rdi), %edx
    cmp     %eax, %edx
    jne     ._rxs_sim_state_next
    jmp     ._rxs_matched
._rxs_dot_match:
    jmp     ._rxs_matched
._rxs_class_match:
    movzbl  1(%rdi), %edx
    cmp     %edx, %eax
    jl      ._rxs_sim_state_next
    movzbl  2(%rdi), %edx
    cmp     %edx, %eax
    jg      ._rxs_sim_state_next
    jmp     ._rxs_matched
._rxs_nclass_match:
    movzbl  1(%rdi), %edx
    cmp     %edx, %eax
    jl      ._rxs_matched
    movzbl  2(%rdi), %edx
    cmp     %edx, %eax
    jg      ._rxs_matched
    jmp     ._rxs_sim_state_next

._rxs_matched:
    push    %rcx
    push    %rax
    push    %r9
    movzwl  4(%rdi), %edi
    cmp     $0xFFFF, %edi
    je      ._rxs_no_next
    xchg    %r14, %r15
    call    ._rx_add_state
    xchg    %r14, %r15
._rxs_no_next:
    pop     %r9
    pop     %rax
    pop     %rcx

._rxs_sim_state_next:
    inc     %ecx
    jmp     ._rxs_sim_state

._rxs_sim_advance:
    xchg    %r14, %r15
    # Check if accept state is in new current set (early accept)
    xor     %ecx, %ecx
._rxs_check_accept:
    cmp     %r8d, %ecx
    jge     ._rxs_no_accept
    bt      %ecx, (%r14)
    jnc     ._rxs_check_accept_next
    lea     (%rbx, %rcx, 8), %rdi
    cmpb    $3, (%rdi)
    je      ._rxs_found
._rxs_check_accept_next:
    inc     %ecx
    jmp     ._rxs_check_accept
._rxs_no_accept:
    inc     %r9d
    jmp     ._rxs_sim_loop

._rxs_next_pos:
    inc     %r11d
    jmp     ._rxs_try_pos

._rxs_found:
    mov     $1, %eax
    jmp     ._rx_return

._rxs_not_found:
    xor     %eax, %eax
    jmp     ._rx_return

# Emit capture: write to mcp_out_buf instead of stdout
.emit_capture:
    # rsi = chain ptr, rdx = len/marker (already popped from r14)
    cmp     $F64_MARKER, %rdx
    je      .emit_cap_f64
    # String: decode molecules to mcp_out_buf
    test    %rdx, %rdx
    jz      vm_loop                      # empty → skip
    movzwl  (%rsi), %eax
    and     $0xFF00, %eax
    cmp     $0x2100, %eax
    jne     vm_loop                      # not a string → skip
    # Decode string molecules to capture buffer
    mov     mcp_out_len(%rip), %rdi      # current offset in buffer
    lea     mcp_out_buf(%rip), %rax
    add     %rdi, %rax                   # write position
    mov     %rdx, %rcx                   # mol_count
.emit_cap_str_loop:
    test    %rcx, %rcx
    jz      .emit_cap_str_done
    movzwl  (%rsi), %edx
    movb    %dl, (%rax)
    add     $2, %rsi
    inc     %rax
    inc     %rdi
    dec     %rcx
    jmp     .emit_cap_str_loop
.emit_cap_str_done:
    # Add newline
    movb    $0x0A, (%rax)
    inc     %rdi
    mov     %rdi, mcp_out_len(%rip)
    jmp     vm_loop

.emit_cap_f64:
    # Format number to capture buffer
    # Use BSS temp buffer (not heap — heap in use by repl_eval)
    push    %rsi
    movq    (%rsi), %xmm0               # load f64 value
    lea     mcp_fmt_tmp(%rip), %rdi      # BSS temp buffer
    # Check if integer
    cvttsd2si %xmm0, %rax
    cvtsi2sd %rax, %xmm1
    ucomisd %xmm0, %xmm1
    jne     .emit_cap_f64_float
    jp      .emit_cap_f64_float
    # Integer: format via itoa
    mov     %rax, %rcx
    test    %rcx, %rcx
    jns     .emit_cap_f64_pos
    movb    $'-', (%rdi)
    inc     %rdi
    neg     %rcx
.emit_cap_f64_pos:
    mov     %rdi, %r8                    # save start
    mov     $10, %esi
.emit_cap_itoa:
    xor     %edx, %edx
    mov     %rcx, %rax
    div     %rsi
    add     $'0', %edx
    movb    %dl, (%rdi)
    inc     %rdi
    mov     %rax, %rcx
    test    %rcx, %rcx
    jnz     .emit_cap_itoa
    # Reverse digits
    mov     %rdi, %rax                   # end
    dec     %rax
.emit_cap_rev:
    cmp     %r8, %rax
    jle     .emit_cap_f64_done
    movb    (%r8), %cl
    movb    (%rax), %dl
    movb    %dl, (%r8)
    movb    %cl, (%rax)
    inc     %r8
    dec     %rax
    jmp     .emit_cap_rev

.emit_cap_f64_float:
    # Float: write "N.N" (simplified)
    # Just write integer part for now
    cvttsd2si %xmm0, %rax
    mov     %rax, %rcx
    test    %rcx, %rcx
    jns     .emit_cap_ff_pos
    movb    $'-', (%rdi)
    inc     %rdi
    neg     %rcx
.emit_cap_ff_pos:
    mov     %rdi, %r8
    mov     $10, %esi
.emit_cap_ff_itoa:
    xor     %edx, %edx
    mov     %rcx, %rax
    div     %rsi
    add     $'0', %edx
    movb    %dl, (%rdi)
    inc     %rdi
    mov     %rax, %rcx
    test    %rcx, %rcx
    jnz     .emit_cap_ff_itoa
    mov     %rdi, %rax
    dec     %rax
.emit_cap_ff_rev:
    cmp     %r8, %rax
    jle     .emit_cap_f64_done
    movb    (%r8), %cl
    movb    (%rax), %dl
    movb    %dl, (%r8)
    movb    %cl, (%rax)
    inc     %r8
    dec     %rax
    jmp     .emit_cap_ff_rev

.emit_cap_f64_done:
    # rdi = end of formatted number, mcp_fmt_tmp = start
    mov     %rdi, %rcx
    lea     mcp_fmt_tmp(%rip), %rsi
    sub     %rsi, %rcx                   # length
    pop     %rsi                         # discard saved rsi
    # Copy to capture buffer
    mov     mcp_out_len(%rip), %rdi
    lea     mcp_out_buf(%rip), %rax
    add     %rdi, %rax
    lea     mcp_fmt_tmp(%rip), %rsi
    push    %rcx
.emit_cap_copy:
    test    %rcx, %rcx
    jz      .emit_cap_copy_done
    movb    (%rsi), %dl
    movb    %dl, (%rax)
    inc     %rsi
    inc     %rax
    dec     %rcx
    jmp     .emit_cap_copy
.emit_cap_copy_done:
    pop     %rcx
    add     %rcx, %rdi
    movb    $0x0A, (%rax)                # newline
    inc     %rdi
    mov     %rdi, mcp_out_len(%rip)
    jmp     vm_loop

# __timestamp() → epoch seconds as f64
.call_timestamp:
    add     %rcx, %r13
    sub     $16, %rsp
    mov     %rsp, %rsi
    xor     %edi, %edi                  # CLOCK_REALTIME = 0
    mov     $228, %eax                  # SYS_clock_gettime
    syscall
    mov     (%rsp), %rax                # epoch seconds
    add     $16, %rsp
    cvtsi2sd %rax, %xmm0
    sub     $16, %r14
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

._ts_dead_code:
    # Dead code below — kept to avoid label renumbering
    # Convert epoch to YYYY-MM-DD HH:MM
    # Days since epoch
    xor     %edx, %edx
    mov     $86400, %ecx
    div     %rcx                        # rax = days, rdx = seconds in day
    push    %rdx                        # save time-of-day seconds
    # Hours and minutes from time-of-day
    mov     %rdx, %rax
    xor     %edx, %edx
    mov     $3600, %ecx
    div     %rcx                        # rax = hours, rdx = remaining
    push    %rax                        # save hours
    mov     %rdx, %rax
    xor     %edx, %edx
    mov     $60, %ecx
    div     %rcx                        # rax = minutes
    push    %rax                        # save minutes
    # Days to Y-M-D (simplified: assume post-2000, handle leap years)
    # Stack: [minutes, hours, time_secs]
    # We need days back. Recalculate from epoch secs.
    mov     24(%rsp), %rax              # get time_secs back... wait, stack is different
    # Actually let me use a simpler approach: format to heap buffer
    pop     %r8                         # minutes
    pop     %r9                         # hours
    pop     %rax                        # discard time_secs
    # Recalculate days from original epoch
    # Get epoch again
    sub     $16, %rsp
    mov     %rsp, %rsi
    xor     %edi, %edi
    mov     $228, %eax
    syscall
    mov     (%rsp), %rax
    add     $16, %rsp
    add     $25200, %rax                # UTC+7
    xor     %edx, %edx
    mov     $86400, %ecx
    div     %rcx                        # rax = total days since epoch
    # Civil date from days (algorithm from http://howardhinnant.github.io/date_algorithms.html)
    push    %r8                         # save minutes
    push    %r9                         # save hours
    add     $719468, %eax               # days + epoch offset
    mov     %eax, %ecx
    xor     %edx, %edx
    mov     $146097, %esi
    div     %esi                        # era = days/146097
    mov     %eax, %r10d                 # era
    mov     %edx, %ecx                  # doe = days - era*146097
    # yoe = (doe - doe/1460 + doe/36524 - doe/146096) / 365
    mov     %ecx, %eax
    xor     %edx, %edx
    mov     $1460, %esi
    div     %esi
    mov     %eax, %r11d                 # doe/1460
    mov     %ecx, %eax
    xor     %edx, %edx
    mov     $36524, %esi
    div     %esi
    sub     %r11d, %eax                 # doe/36524 - doe/1460 (wrong order, fix)
    # Simplified: just use year = 1970 + days/365.25 approximation
    # For accuracy within 1 day, good enough for timestamps
    pop     %r9                         # hours
    pop     %r8                         # minutes
    # Restart with simpler approach
    sub     $16, %rsp
    mov     %rsp, %rsi
    xor     %edi, %edi
    mov     $228, %eax
    syscall
    mov     (%rsp), %rax
    add     $16, %rsp
    add     $25200, %rax
    # Format as string on heap: "YYYY-MM-DD HH:MM"
    # Use division to extract components
    push    %r8                         # minutes
    push    %r9                         # hours
    # Total days
    xor     %edx, %edx
    mov     $86400, %ecx
    div     %rcx
    mov     %eax, %ebp                  # total_days
    # Year calculation
    mov     %ebp, %eax
    # days since 2000-03-01 = total_days - 10957 (days from 1970-01-01 to 2000-03-01)
    sub     $10957, %eax
    # Each 400-year cycle = 146097 days
    xor     %edx, %edx
    mov     $146097, %ecx
    div     %ecx
    imul    $400, %eax
    mov     %eax, %r10d                 # year_base = era * 400
    mov     %edx, %ebp                  # day_of_era
    # Year within era (approximate)
    mov     %ebp, %eax
    xor     %edx, %edx
    mov     $365, %ecx
    div     %ecx
    mov     %eax, %r11d                 # year_of_era (approx)
    mov     %edx, %ecx                  # day_of_year (approx)
    add     %r10d, %r11d
    add     $2000, %r11d                # year
    # Month from day_of_year (March-based, shift to Jan-based)
    # Simplified: divide by 30
    mov     %ecx, %eax
    xor     %edx, %edx
    mov     $30, %esi
    div     %esi
    add     $3, %eax                    # month (March=3)
    cmp     $13, %eax
    jl      .ts_month_ok
    sub     $12, %eax
    inc     %r11d                       # next year
.ts_month_ok:
    mov     %eax, %r12d                 # month
    add     $1, %edx                    # day (1-based)
    mov     %edx, %ebp                  # day
    pop     %r9                         # hours
    pop     %r8                         # minutes
    # Now format "YYYY-MM-DD HH:MM" to heap
    mov     %r15, %rdi                  # output buffer
    # Year (4 digits)
    mov     %r11d, %eax
    mov     $1000, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    mov     %edx, %eax
    mov     $100, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    mov     %edx, %eax
    mov     $10, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    add     $'0', %dl; movb %dl, (%rdi); inc %rdi
    movb    $'-', (%rdi); inc %rdi
    # Month (2 digits)
    mov     %r12d, %eax
    mov     $10, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    add     $'0', %dl; movb %dl, (%rdi); inc %rdi
    movb    $'-', (%rdi); inc %rdi
    # Day (2 digits)
    mov     %ebp, %eax
    mov     $10, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    add     $'0', %dl; movb %dl, (%rdi); inc %rdi
    movb    $' ', (%rdi); inc %rdi
    # Hours (2 digits)
    mov     %r9d, %eax
    mov     $10, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    add     $'0', %dl; movb %dl, (%rdi); inc %rdi
    movb    $':', (%rdi); inc %rdi
    # Minutes (2 digits)
    mov     %r8d, %eax
    mov     $10, %ecx
    xor     %edx, %edx; div %ecx; add $'0', %al; movb %al, (%rdi); inc %rdi
    add     $'0', %dl; movb %dl, (%rdi); inc %rdi
    # String length = 16 ("YYYY-MM-DD HH:MM")
    mov     $16, %ecx
    # Encode to molecules
    sub     $16, %r14
    mov     %r15, (%r14)                # chain ptr
    mov     %rcx, 8(%r14)               # mol_count = 16
    mov     %r15, %rsi                  # source = raw bytes
    mov     %rdi, %r8                   # save end ptr
    mov     %r15, %rdi                  # dest = same buffer (overwrite with molecules, backwards)
    # Need to encode in-place: but molecules are 2 bytes per char, so we'd overwrite.
    # Copy raw bytes to temp first
    sub     $16, %rsp
    mov     %r15, %rsi
    mov     %rsp, %rdi
    mov     $16, %ecx
    rep     movsb
    # Now encode from stack temp to heap
    mov     %rsp, %rsi
    mov     %r15, %rdi
    mov     $16, %ecx
.ts_encode:
    test    %ecx, %ecx
    jz      .ts_encode_done
    movzbl  (%rsi), %eax
    or      $0x2100, %eax
    movw    %ax, (%rdi)
    add     $2, %rdi
    inc     %rsi
    dec     %ecx
    jmp     .ts_encode
.ts_encode_done:
    add     $16, %rsp
    mov     %rdi, %r15                  # advance heap past molecules
    jmp     vm_loop

# __mcp_suppress(flag) — set/clear emit capture for MCP mode
# flag=1: start capturing (clears buffer), flag=0: stop capturing and return captured text
.call_mcp_suppress:
    add     %rcx, %r13
    mov     (%r14), %rax               # flag value (f64)
    add     $16, %r14                  # pop arg
    # Check f64 bits: 0.0 = all zeros
    test    %rax, %rax
    jz      .mcp_sup_off
    # Turn ON capture, clear buffer
    movb    $1, mcp_capture(%rip)
    movq    $0, mcp_out_len(%rip)
    # Return 0
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop
.mcp_sup_off:
    # Turn OFF capture, return 0
    movb    $0, mcp_capture(%rip)
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_file_read:
    add     %rcx, %r13
    jmp     op_file_read

.call_file_write:
    add     %rcx, %r13
    jmp     op_file_write

# __stdout_off() → saved_fd (dup stdout, redirect to /dev/null)
.call_stdout_off:
    add     %rcx, %r13
    mov     $1, %edi
    mov     $32, %eax                   # SYS_dup
    syscall
    mov     %eax, %r8d                  # saved fd
    lea     dev_null_path(%rip), %rdi
    mov     $1, %esi                    # O_WRONLY
    xor     %edx, %edx
    mov     $2, %eax                    # SYS_open
    syscall
    mov     %eax, %r9d                  # null fd
    mov     %r9d, %edi
    mov     $1, %esi
    mov     $33, %eax                   # SYS_dup2
    syscall
    mov     %r9d, %edi
    mov     $3, %eax                    # SYS_close
    syscall
    cvtsi2sd %r8d, %xmm0
    sub     $16, %r14
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# __stdout_on(saved_fd) → restore stdout
.call_stdout_on:
    add     %rcx, %r13
    movq    (%r14), %xmm0
    add     $16, %r14
    cvttsd2si %xmm0, %edi
    push    %rdi
    mov     $1, %esi
    mov     $33, %eax                   # SYS_dup2
    syscall
    pop     %rdi
    mov     $3, %eax                    # SYS_close
    syscall
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_file_append:
    add     %rcx, %r13
    jmp     op_file_append

# ── Raw byte buffer builtins ────────────────────────────────────────
# Bytes buffers: stored as [size:8][raw_bytes:N] on heap
# VM stack entry: ptr = heap_base, len = BYTES_MARKER (-5)
.equ BYTES_MARKER, -5

.call_bytes_new:
    # __bytes_new(size) → byte buffer
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax          # size
    add     $16, %r14               # pop size
    # Allocate on heap: [size:8][zeroed bytes]
    mov     %r15, %rdi              # buffer base
    mov     %rax, (%r15)            # store size
    add     $8, %r15                # past size header
    # Zero the buffer
    push    %rdi                    # save buf base
    mov     %rax, %rcx              # count = size
    push    %rcx                    # save size
    mov     %r15, %rdi              # dest = data start
    xor     %eax, %eax              # AL = 0 (rep stosb stores AL)
    rep     stosb
    pop     %rax                    # restore size
    pop     %rdi                    # restore buf base
    add     %rax, %r15              # bump heap past buffer
    # Push result
    sub     $16, %r14
    mov     %rdi, (%r14)            # ptr = buffer base
    movq    $BYTES_MARKER, 8(%r14)  # marker
    jmp     vm_loop

.call_bytes_set:
    # __bytes_set(buf, offset, value) → buf
    add     %rcx, %r13
    # Pop value (f64)
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax          # byte value
    # Pop offset (f64)
    movsd   16(%r14), %xmm1
    cvttsd2si %xmm1, %rdx          # offset
    # Pop buf
    mov     32(%r14), %rdi          # buf ptr
    add     $48, %r14               # pop 3
    # Write byte at buf + 8 + offset
    mov     %al, 8(%rdi, %rdx)
    # Push buf back
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $BYTES_MARKER, 8(%r14)
    jmp     vm_loop

.call_bytes_get:
    # __bytes_get(buf, offset) → f64 byte value
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rdx          # offset
    mov     16(%r14), %rdi          # buf ptr
    add     $16, %r14               # pop offset (keep buf slot for result)
    movzbl  8(%rdi, %rdx), %eax    # read byte
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)          # overwrite buf with result
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bytes_write_raw:
    # __bytes_write(path, buf, size) → 0
    add     %rcx, %r13
    # Pop size
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %r9           # size
    # Pop buf
    mov     16(%r14), %r8           # buf ptr
    # Pop path (string)
    mov     32(%r14), %rsi          # path ptr (u16 mols)
    mov     40(%r14), %rdx          # path len
    add     $48, %r14               # pop 3
    # Decode path → ASCII
    lea     sha_input(%rip), %rdi
    xor     %rcx, %rcx
.bw_decode_path:
    cmp     %rdx, %rcx
    jge     .bw_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .bw_decode_path
.bw_open:
    movb    $0, (%rdi, %rcx)
    lea     sha_input(%rip), %rdi
    mov     $0x241, %esi            # O_WRONLY|O_CREAT|O_TRUNC
    mov     $0755, %edx             # permissions (executable)
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      .bw_fail
    # Write raw bytes from buf+8
    mov     %rax, %rdi              # fd
    lea     8(%r8), %rsi            # data = buf + 8 (skip size header)
    mov     %r9, %rdx              # size
    mov     $SYS_WRITE, %eax
    syscall
    # Close
    mov     $SYS_CLOSE, %eax
    syscall
.bw_fail:
    # Push 0 result
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_file_read_bytes:
    # __file_read_bytes(path) → bytes buffer (raw, not u16 encoded)
    add     %rcx, %r13
    mov     (%r14), %rsi            # path ptr (u16 mols)
    mov     8(%r14), %rdx           # path len
    add     $16, %r14               # pop
    # Decode path
    lea     sha_input(%rip), %rdi
    xor     %rcx, %rcx
.frb_decode:
    cmp     %rdx, %rcx
    jge     .frb_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .frb_decode
.frb_open:
    movb    $0, (%rdi, %rcx)
    lea     sha_input(%rip), %rdi
    xor     %esi, %esi              # O_RDONLY
    xor     %edx, %edx
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      .frb_empty
    # Read into heap as bytes buffer [size:8][data:N] — loop for large files
    mov     %rax, %r8               # fd
    mov     %r15, %rdi              # buf base
    add     $8, %r15                # skip size header
    xor     %r9d, %r9d              # r9 = total bytes read
.frb_read_loop:
    mov     %r8, %rdi               # fd
    lea     (%r15, %r9), %rsi       # buf = data_start + total_read
    mov     $0x100000, %edx         # read 1MB chunks
    mov     $SYS_READ, %eax
    syscall
    test    %rax, %rax
    jle     .frb_read_done
    add     %rax, %r9
    cmp     $0x100000, %rax
    je      .frb_read_loop
.frb_read_done:
    mov     %r8, %rdi
    mov     $SYS_CLOSE, %eax
    syscall
    mov     %r9, %rcx               # total bytes_read
    test    %rcx, %rcx
    jle     .frb_empty
    # Set size header
    mov     %r15, %rdi
    sub     $8, %rdi                # buf base
    mov     %rcx, (%rdi)            # size
    add     %rcx, %r15              # bump heap past data
    # Push
    sub     $16, %r14
    mov     %rdi, (%r14)
    movq    $BYTES_MARKER, 8(%r14)
    jmp     vm_loop
.frb_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

.call_heap_save:
    # __heap_save() → stores r15 in BSS, returns index as f64
    add     %rcx, %r13
    lea     heap_checkpoints(%rip), %rax
    lea     heap_cp_count(%rip), %rdx
    mov     (%rdx), %rcx               # current count
    mov     %r15, (%rax, %rcx, 8)      # store r15 at checkpoints[count]
    inc     %rcx
    mov     %rcx, (%rdx)               # count++
    dec     %rcx                        # return index (before increment)
    sub     $16, %r14
    cvtsi2sd %rcx, %xmm0              # index as f64 (small number, safe)
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_heap_restore:
    # __heap_restore(index) → resets r15 from saved checkpoint
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %rax              # index
    add     $16, %r14                   # pop
    lea     heap_checkpoints(%rip), %rdx
    mov     (%rdx, %rax, 8), %r15      # restore r15
    # Push dummy result
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_bytes_len:
    # __bytes_len(buf) → f64 size
    add     %rcx, %r13
    mov     (%r14), %rdi            # buf ptr
    mov     (%rdi), %rax            # size from header
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_floor:
    # __floor(x) → floor(x)
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    roundsd $1, %xmm0, %xmm0          # round toward -inf
    movsd   %xmm0, (%r14)
    jmp     vm_loop

.call_ceil:
    # __ceil(x) → ceil(x)
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    roundsd $2, %xmm0, %xmm0          # round toward +inf
    movsd   %xmm0, (%r14)
    jmp     vm_loop

# ── Molecule extract builtins (T5 ND.2) ────────────────────────────────
# P_weight = [S:4][R:4][V:3][A:3][T:2] = 16 bits packed as f64
# __mol_s(mol) → (mol / 4096) % 16     (bits 12-15)
# __mol_r(mol) → (mol / 256) % 16      (bits 8-11)
# __mol_v(mol) → (mol / 32) % 8        (bits 5-7)
# __mol_a(mol) → (mol / 4) % 8         (bits 2-4)
# __mol_t(mol) → mol % 4               (bits 0-1)
# __mol_pack(s,r,v,a,t) → s*4096 + r*256 + v*32 + a*4 + t

.call_mol_s:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    shr     $12, %eax
    and     $0x0F, %eax
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mol_r:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    shr     $8, %eax
    and     $0x0F, %eax
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mol_v:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    shr     $5, %eax
    and     $0x07, %eax
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mol_a:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    shr     $2, %eax
    and     $0x07, %eax
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mol_t:
    add     %rcx, %r13
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax
    and     $0x03, %eax
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_mol_pack:
    # Stack: [s, r, v, a, t] → s*4096 + r*256 + v*32 + a*4 + t
    add     %rcx, %r13
    # Pop t
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %eax              # t
    add     $16, %r14
    # Pop a
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # a
    shl     $2, %edx                    # a*4
    add     %edx, %eax
    add     $16, %r14
    # Pop v
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # v
    shl     $5, %edx                    # v*32
    add     %edx, %eax
    add     $16, %r14
    # Pop r
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # r
    shl     $8, %edx                    # r*256
    add     %edx, %eax
    add     $16, %r14
    # Pop s
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # s
    shl     $12, %edx                   # s*4096
    add     %edx, %eax
    # Push result as f64
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── UTF-8 decoding builtins ──────────────────────────────────────────────
# __utf8_cp(str, pos) → Unicode codepoint at byte position pos
# __utf8_len(str, pos) → number of bytes in UTF-8 sequence (1-4)
# Strings are u16 molecules: each byte = 0x2100 | byte_value

.call_utf8_cp:
    add     %rcx, %r13
    # Pop pos (f64)
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # pos
    add     $16, %r14
    # Pop str (chain ptr + mol_count)
    mov     (%r14), %rsi               # str ptr (u16 molecules)
    # Read byte at pos: molecule = 0x2100 | byte, so byte = mol & 0xFF
    movzwl  (%rsi, %rdx, 2), %eax     # read u16 molecule at pos
    and     $0xFF, %eax                # extract byte value
    # Check UTF-8 lead byte
    cmp     $0x80, %eax
    jb      .utf8_cp_1byte             # 0xxxxxxx → 1 byte (ASCII)
    cmp     $0xE0, %eax
    jb      .utf8_cp_2byte             # 110xxxxx → 2 bytes
    cmp     $0xF0, %eax
    jb      .utf8_cp_3byte             # 1110xxxx → 3 bytes
    # 4 byte: 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx
    and     $0x07, %eax                # keep low 3 bits
    shl     $18, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    shl     $12, %ecx
    or      %ecx, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    shl     $6, %ecx
    or      %ecx, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    or      %ecx, %eax
    jmp     .utf8_cp_done
.utf8_cp_3byte:
    and     $0x0F, %eax                # keep low 4 bits
    shl     $12, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    shl     $6, %ecx
    or      %ecx, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    or      %ecx, %eax
    jmp     .utf8_cp_done
.utf8_cp_2byte:
    and     $0x1F, %eax                # keep low 5 bits
    shl     $6, %eax
    inc     %edx
    movzwl  (%rsi, %rdx, 2), %ecx
    and     $0x3F, %ecx
    or      %ecx, %eax
    jmp     .utf8_cp_done
.utf8_cp_1byte:
    # eax already has the byte value (ASCII)
.utf8_cp_done:
    # Push codepoint as f64
    cvtsi2sd %eax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_utf8_len:
    add     %rcx, %r13
    # Pop pos (f64)
    movsd   (%r14), %xmm0
    cvttsd2si %xmm0, %edx              # pos
    add     $16, %r14
    # Pop str
    mov     (%r14), %rsi               # str ptr
    movzwl  (%rsi, %rdx, 2), %eax     # read u16 molecule at pos
    and     $0xFF, %eax                # byte value
    # Return byte count based on lead byte
    mov     $1, %ecx                    # default 1
    cmp     $0x80, %eax
    jb      .utf8_len_done
    mov     $2, %ecx
    cmp     $0xE0, %eax
    jb      .utf8_len_done
    mov     $3, %ecx
    cmp     $0xF0, %eax
    jb      .utf8_len_done
    mov     $4, %ecx
.utf8_len_done:
    cvtsi2sd %ecx, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── __sleep(ms) — nanosleep syscall ──────────────────────────────────────
.call_sleep:
    add     %rcx, %r13                  # skip name_len
    # Pop ms (f64) from VM stack
    movsd   (%r14), %xmm0
    add     $16, %r14                   # pop
    # Convert ms → seconds + nanoseconds
    cvttsd2si %xmm0, %rax              # ms as integer
    # rax = total ms, compute sec = ms / 1000, nsec = (ms % 1000) * 1_000_000
    xor     %edx, %edx
    mov     $1000, %rcx
    div     %rcx                        # rax=sec, rdx=remainder_ms
    imul    $1000000, %rdx, %rdx        # remainder → nanoseconds
    # Build timespec on CPU stack (16 bytes aligned)
    sub     $16, %rsp
    mov     %rax, (%rsp)               # tv_sec
    mov     %rdx, 8(%rsp)             # tv_nsec
    # syscall nanosleep(timespec*, NULL)
    mov     %rsp, %rdi                  # rdi = &timespec
    xor     %esi, %esi                  # rsi = NULL (remaining)
    mov     $SYS_NANOSLEEP, %eax
    syscall
    add     $16, %rsp                   # restore stack
    # Push 0.0 (success)
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── __time() — clock_gettime CLOCK_MONOTONIC → ms ──────────────────────
.call_time:
    add     %rcx, %r13                  # skip name_len
    # Build timespec on CPU stack
    sub     $16, %rsp
    mov     $CLOCK_MONOTONIC, %edi      # clock_id
    mov     %rsp, %rsi                  # &timespec
    mov     $SYS_CLOCK_GETTIME, %eax
    syscall
    # Convert to milliseconds: sec*1000 + nsec/1_000_000
    mov     (%rsp), %rax               # tv_sec
    imul    $1000, %rax, %rax           # sec → ms
    mov     8(%rsp), %rdx             # tv_nsec
    mov     $1000000, %rcx
    xor     %r8d, %r8d                  # clear for div
    push    %rax                        # save sec_ms
    mov     %rdx, %rax
    xor     %edx, %edx
    div     %rcx                        # rax = nsec/1000000
    pop     %rdx                        # rdx = sec_ms
    add     %rdx, %rax                  # total ms
    add     $16, %rsp                   # restore stack
    # Push result as f64
    sub     $16, %r14
    cvtsi2sd %rax, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

# ── __write_raw(str) — write raw bytes to stdout (for ANSI escapes) ────
.call_write_raw:
    add     %rcx, %r13                  # skip name_len
    # Pop string from VM stack
    mov     (%r14), %rsi               # ptr (u16 molecules)
    mov     8(%r14), %rdx              # len (mol count)
    add     $16, %r14                   # pop
    # Decode u16 molecules → raw bytes in temp buffer on CPU stack
    # Each molecule = 0x2100 | byte, stride 2 bytes
    # Max reasonable: 256 bytes for ANSI sequences
    mov     %rdx, %rcx                 # mol_count
    sub     $256, %rsp                  # temp buffer
    mov     %rsp, %rdi                  # dest ptr
    xor     %r8d, %r8d                  # byte count
.wr_decode:
    test    %rcx, %rcx
    jz      .wr_write
    movzwl  (%rsi), %eax               # read u16 molecule
    and     $0xFF, %eax                 # extract byte (mask off 0x2100)
    movb    %al, (%rdi, %r8)
    inc     %r8
    add     $2, %rsi                    # next molecule
    dec     %rcx
    jmp     .wr_decode
.wr_write:
    # SYS_WRITE(fd=1, buf=rsp, len=r8)
    mov     $1, %edi                    # stdout
    mov     %rsp, %rsi                  # buffer
    mov     %r8, %rdx                   # byte count
    mov     $SYS_WRITE, %eax
    syscall
    add     $256, %rsp                  # restore stack
    # Push 0.0
    sub     $16, %r14
    xorpd   %xmm0, %xmm0
    movsd   %xmm0, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.call_throw:
    # __throw(msg) — unwind to nearest try/catch
    # Stack: [msg_value] → store in __error var, jump to catch_PC
    add     %rcx, %r13                  # skip name_len
    # Pop error message (keep on stack for catch block to access)
    mov     (%r14), %r8                # msg ptr
    mov     8(%r14), %r9               # msg len
    add     $16, %r14                   # pop

    # Store error message in __error variable (hash of "__error")
    movabs  $0x07515EC1BC3E1099, %rdx   # temp: store as "__error" var
    # Actually, use the var_table to store error. For now, just jump to catch.

    # Check try_depth
    lea     try_depth(%rip), %rsi
    cmpq    $0, (%rsi)
    je      .throw_no_handler

    # Pop try frame
    decq    (%rsi)                      # depth--
    mov     (%rsi), %rdx               # new depth
    lea     try_stack(%rip), %rdi
    shl     $4, %rdx                    # depth * 16
    mov     (%rdi, %rdx), %rax         # catch_PC
    mov     8(%rdi, %rdx), %r14        # restore stack pointer

    # Push error message onto restored stack (catch block can access it)
    sub     $16, %r14
    mov     %r8, (%r14)
    mov     %r9, 8(%r14)

    # Jump to catch_PC
    mov     %rax, %r13                  # set PC = catch_PC
    jmp     vm_loop

.throw_no_handler:
    # No try block — print error and halt
    # Print "Error: " prefix
    lea     .err_prefix(%rip), %rsi
    mov     $7, %rdx
    mov     $1, %rdi
    mov     $1, %rax
    syscall
    # Print error message
    mov     %r8, %rsi
    mov     %r9, %rdx
    cmp     $F64_MARKER, %rdx
    je      .throw_halt                 # skip if f64 (can't print number easily)
    test    %rdx, %rdx
    jz      .throw_halt
    # Check if string (0x21xx pattern)
    movzwl  (%rsi), %eax
    and     $0xFF00, %eax
    cmp     $0x2100, %eax
    jne     .throw_halt
    # Decode u16 string
    mov     %r9, %rcx                   # mol count
    lea     throw_buf(%rip), %rdi
    xor     %rdx, %rdx
.throw_decode:
    cmp     %rcx, %rdx
    jge     .throw_write
    movzbl  (%rsi, %rdx, 2), %eax
    mov     %al, (%rdi, %rdx)
    inc     %rdx
    jmp     .throw_decode
.throw_write:
    lea     throw_buf(%rip), %rsi
    # rdx = length
    mov     $1, %rdi
    mov     $1, %rax
    syscall
.throw_halt:
    # Print newline and halt
    lea     str_newline(%rip), %rsi
    mov     $1, %rdx
    mov     $1, %rdi
    mov     $1, %rax
    syscall
    mov     $60, %rax
    mov     $1, %rdi
    syscall

.section .rodata
.err_prefix:
    .ascii  "Error: "
.align 4
sha_k:
    .long 0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5
    .long 0x3956c25b, 0x59f111f1, 0x923f82a4, 0xab1c5ed5
    .long 0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3
    .long 0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174
    .long 0xe49b69c1, 0xefbe4786, 0x0fc19dc6, 0x240ca1cc
    .long 0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da
    .long 0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7
    .long 0xc6e00bf3, 0xd5a79147, 0x06ca6351, 0x14292967
    .long 0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13
    .long 0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85
    .long 0xa2bfe8a1, 0xa81a664b, 0xc24b8b70, 0xc76c51a3
    .long 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070
    .long 0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5
    .long 0x391c0cb3, 0x4ed8aa4a, 0x5b9cca4f, 0x682e6ff3
    .long 0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208
    .long 0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2
.section .text

op_try_begin:
    # [0x1A, catch_offset_u32_le]
    # Push (catch_PC, saved_r14) onto try_stack
    # r13 already points past opcode byte
    movzbl  (%r12, %r13), %eax         # read catch_offset byte 0
    movzbl  1(%r12, %r13), %edx
    shl     $8, %edx
    or      %edx, %eax
    movzbl  2(%r12, %r13), %edx
    shl     $16, %edx
    or      %edx, %eax
    movzbl  3(%r12, %r13), %edx
    shl     $24, %edx
    or      %edx, %eax                  # eax = catch_offset (absolute PC)
    add     $4, %r13

    # Push onto try_stack
    lea     try_depth(%rip), %rsi
    mov     (%rsi), %rdx               # depth
    lea     try_stack(%rip), %rdi
    shl     $4, %rdx                    # depth * 16
    lea     (%rdi, %rdx), %rdi         # try_stack[depth]
    mov     %rax, (%rdi)               # catch_PC
    mov     %r14, 8(%rdi)              # saved stack pointer
    lea     try_depth(%rip), %rsi
    incq    (%rsi)                      # depth++
    jmp     vm_loop

op_catch_end:
    # Pop try_stack (catch block finished)
    lea     try_depth(%rip), %rsi
    cmpq    $0, (%rsi)
    je      vm_loop                     # no try frame → skip
    decq    (%rsi)                      # depth--
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Task 5: Syscall bridge + Emit
# ═══════════════════════════════════════════════════════════════════════════

op_emit:
    # Pop top → write to stdout
    mov     (%r14), %rsi               # buf = chain ptr
    mov     8(%r14), %rdx              # len
    add     $16, %r14                  # pop

    # MCP capture: redirect emit to buffer instead of stdout
    cmpb    $1, mcp_capture(%rip)
    jne     .emit_normal
    # Capture emit to mcp_out_buf
    jmp     .emit_capture
.emit_normal:

    # If f64 marker, format number first
    cmp     $F64_MARKER, %rdx
    je      .emit_f64

    # Array/dict: print type indicator
    cmp     $ARRAY_MARKER, %rdx
    je      .emit_array
    cmp     $DICT_MARKER, %rdx
    je      .emit_dict

    # If len == 0, skip
    test    %rdx, %rdx
    jz      vm_loop

    # Check if this is a string chain (molecules with 0x21xx pattern)
    # First molecule top byte = 0x21 → string encoded
    movzwl  (%rsi), %eax
    and     $0xFF00, %eax
    cmp     $0x2100, %eax
    jne     .emit_raw

    # String chain: decode molecules to ASCII bytes on heap, then print
    push    %rsi
    push    %rdx
    mov     %r15, %rdi                  # decode buffer = heap
    mov     %rdx, %rcx                  # mol_count
.emit_decode_loop:
    test    %rcx, %rcx
    jz      .emit_decode_done
    movzwl  (%rsi), %eax               # read u16 molecule
    movb    %al, (%rdi)                 # store lower byte (ASCII value)
    add     $2, %rsi                    # next molecule
    inc     %rdi                        # next output byte
    dec     %rcx
    jmp     .emit_decode_loop
.emit_decode_done:
    pop     %rdx                        # mol_count = string length
    pop     %rsi
    mov     %r15, %rsi                  # buf = decoded bytes
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .emit_newline

.emit_raw:
    # Raw chain bytes — write directly
    mov     $1, %edi                   # fd = stdout
    mov     $SYS_WRITE, %eax
    syscall

.emit_newline:
    # Print newline after emit
    lea     str_newline(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     vm_loop

.emit_f64:
    # Convert f64 to decimal string and print
    movq    %rsi, %xmm0                # rsi has the f64 bits from stack
    call    f64_to_ascii                # → eax = length, output at r15
    mov     %r15, %rsi                  # buf
    mov     %eax, %edx                  # len
    mov     $1, %edi                    # stdout
    push    %rax
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rax
    add     %rax, %r15                  # bump heap past formatted string
    # Print newline
    lea     str_newline(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     vm_loop

.emit_array:
    # Print "[elem0, elem1, ...]" with actual contents
    # rsi = array base ptr
    test    %rsi, %rsi
    jz      .emit_array_empty
    # Follow forward pointer chain
.emit_array_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rsi)
    jne     .emit_array_start
    mov     8(%rsi), %rsi
    jmp     .emit_array_fwd
.emit_array_start:
    push    %rbp                        # save rbp (callee-saved, used as elem ptr)
    push    %rsi                        # save array base
    # Print "["
    lea     str_lbracket(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi                        # restore array base
    mov     (%rsi), %rcx               # count
    test    %rcx, %rcx
    jz      .emit_array_close
    lea     8(%rsi), %rbp              # first element ptr
    xor     %eax, %eax                  # index = 0
    push    %rcx                        # save count
.emit_array_elem:
    push    %rax                        # save index
    push    %rbp                        # save element ptr
    # Check element type
    mov     8(%rbp), %rdx              # elem len/marker
    cmp     $F64_MARKER, %rdx
    je      .emit_array_f64
    # String element: decode u16 molecules
    mov     (%rbp), %rsi               # string ptr
    test    %rdx, %rdx
    jz      .emit_array_elem_done
    # Decode molecules to ASCII on heap
    push    %rdx
    mov     %r15, %rdi
    mov     %rdx, %rcx
.emit_arr_str_decode:
    test    %rcx, %rcx
    jz      .emit_arr_str_write
    movzwl  (%rsi), %eax
    movb    %al, (%rdi)
    add     $2, %rsi
    inc     %rdi
    dec     %rcx
    jmp     .emit_arr_str_decode
.emit_arr_str_write:
    pop     %rdx                        # mol_count = char count
    mov     %r15, %rsi
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .emit_array_elem_done
.emit_array_f64:
    movq    (%rbp), %xmm0             # f64 bits
    call    f64_to_ascii               # → eax = len, output at r15
    mov     %r15, %rsi
    mov     %eax, %edx
    mov     $1, %edi
    push    %rax
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rax
    add     %rax, %r15
.emit_array_elem_done:
    pop     %rbp                        # restore element ptr
    pop     %rax                        # restore index
    inc     %rax
    add     $16, %rbp                   # next element
    # Check if more elements
    mov     (%rsp), %rcx              # peek count from stack
    cmp     %rcx, %rax
    jae     .emit_array_close_pop
    # Print ", "
    push    %rax
    push    %rbp
    lea     str_comma_space(%rip), %rsi
    mov     $2, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rbp
    pop     %rax
    jmp     .emit_array_elem
.emit_array_close_pop:
    pop     %rcx                        # pop saved count
.emit_array_close:
    lea     str_rbracket_nl(%rip), %rsi
    mov     $2, %edx                    # "]\n"
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rbp                        # restore rbp
    jmp     vm_loop
.emit_array_empty:
    lea     str_empty_array(%rip), %rsi
    mov     $4, %edx                    # "[]\n"
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     vm_loop

.emit_dict:
    # Pretty-print: {key1: val1, key2: val2}
    test    %rsi, %rsi
    jz      .emit_dict_empty
    push    %rbp
    mov     %rsi, %rbp                  # rbp = dict base
    # Print "{"
    lea     str_lbrace(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    mov     (%rbp), %rcx               # count
    test    %rcx, %rcx
    jz      .emit_dict_close
    lea     8(%rbp), %rbp              # first entry
    xor     %eax, %eax                  # index = 0
    push    %rcx                        # save count
.emit_dict_entry:
    push    %rax                        # save index
    push    %rbp                        # save entry ptr
    # Comma separator (if not first)
    test    %eax, %eax
    jz      .emit_dict_nocomma
    lea     str_comma_sp(%rip), %rsi
    mov     $2, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
.emit_dict_nocomma:
    pop     %rbp                        # restore entry ptr
    pop     %rax                        # restore index
    push    %rax
    push    %rbp
    # Print key (string at entry+0, len at entry+8)
    mov     (%rbp), %rsi               # key ptr
    mov     8(%rbp), %rdx              # key len
    test    %rdx, %rdx
    jz      .emit_dict_key_done
    # Decode u16 molecules to ASCII
    push    %rdx
    mov     %r15, %rdi
    mov     %rdx, %rcx
.emit_dk_loop:
    test    %rcx, %rcx
    jz      .emit_dk_done
    movzwl  (%rsi), %eax
    movb    %al, (%rdi)
    add     $2, %rsi
    inc     %rdi
    dec     %rcx
    jmp     .emit_dk_loop
.emit_dk_done:
    pop     %rdx
    mov     %r15, %rsi
    mov     $1, %edi
    push    %rdx
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdx
.emit_dict_key_done:
    # Print ": "
    lea     str_colon_sp(%rip), %rsi
    mov     $2, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    # Print value (entry+16=val_ptr, entry+24=val_len)
    pop     %rbp
    pop     %rax
    push    %rax
    push    %rbp
    mov     24(%rbp), %rdx             # val len/marker
    cmp     $F64_MARKER, %rdx
    je      .emit_dict_val_f64
    cmp     $ARRAY_MARKER, %rdx
    je      .emit_dict_val_array
    cmp     $DICT_MARKER, %rdx
    je      .emit_dict_val_dict
    cmp     $CLOSURE_MARKER, %rdx
    je      .emit_dict_val_fn
    # String value: decode u16
    mov     16(%rbp), %rsi             # val ptr
    test    %rdx, %rdx
    jz      .emit_dict_val_done
    push    %rdx
    mov     %r15, %rdi
    mov     %rdx, %rcx
.emit_dv_loop:
    test    %rcx, %rcx
    jz      .emit_dv_done
    movzwl  (%rsi), %eax
    movb    %al, (%rdi)
    add     $2, %rsi
    inc     %rdi
    dec     %rcx
    jmp     .emit_dv_loop
.emit_dv_done:
    pop     %rdx
    mov     %r15, %rsi
    mov     $1, %edi
    push    %rdx
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdx
    jmp     .emit_dict_val_done
.emit_dict_val_f64:
    mov     16(%rbp), %rax
    movq    %rax, %xmm0
    call    f64_to_ascii
    mov     %r15, %rsi
    mov     %eax, %edx
    mov     $1, %edi
    push    %rax
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rax
    jmp     .emit_dict_val_done
.emit_dict_val_array:
    # Print array contents inline: [e0, e1, ...]
    mov     16(%rbp), %rsi             # array ptr
    test    %rsi, %rsi
    jz      .emit_dict_val_done
    # Follow forward ptrs
._edva_fwd:
    cmpq    $ARRAY_FORWARD_MARKER, (%rsi)
    jne     ._edva_start
    mov     8(%rsi), %rsi
    jmp     ._edva_fwd
._edva_start:
    push    %rsi
    lea     str_lbracket(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi
    mov     (%rsi), %rcx               # count
    test    %rcx, %rcx
    jz      ._edva_close
    lea     8(%rsi), %rdi              # first elem ptr
    xor     %r8d, %r8d                 # index
._edva_elem:
    push    %rcx
    push    %rdi
    push    %r8
    # Comma if not first
    test    %r8d, %r8d
    jz      ._edva_nocomma
    push    %rdi
    lea     str_comma_space(%rip), %rsi
    mov     $2, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdi
._edva_nocomma:
    mov     8(%rdi), %rdx
    cmp     $F64_MARKER, %rdx
    je      ._edva_f64
    # String elem: skip (print "...")
    jmp     ._edva_next
._edva_f64:
    movq    (%rdi), %xmm0
    call    f64_to_ascii
    mov     %r15, %rsi
    mov     %eax, %edx
    mov     $1, %edi
    push    %rax
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rax
._edva_next:
    pop     %r8
    pop     %rdi
    pop     %rcx
    inc     %r8d
    add     $16, %rdi
    cmp     %ecx, %r8d
    jb      ._edva_elem
._edva_close:
    push    %rdi
    lea     str_rbracket(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdi
    jmp     .emit_dict_val_done
.emit_dict_val_dict:
    lea     str_dict_placeholder(%rip), %rsi
    mov     $6, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .emit_dict_val_done
.emit_dict_val_fn:
    lea     str_fn(%rip), %rsi
    mov     $8, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
.emit_dict_val_done:
    pop     %rbp                        # restore entry ptr
    pop     %rax                        # restore index
    add     $32, %rbp                   # next entry (32 bytes each)
    inc     %eax
    mov     (%rsp), %rcx              # peek count
    cmp     %ecx, %eax
    jb      .emit_dict_entry
    add     $8, %rsp                    # pop count
.emit_dict_close:
    pop     %rbp
    lea     str_brace_nl(%rip), %rsi
    mov     $2, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     vm_loop
.emit_dict_empty:
    lea     str_empty_dict(%rip), %rsi
    mov     $4, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     vm_loop

# ── Syscall wrappers ─────────────────────────────────────────────────────

sys_write:
    # rdi = fd, rsi = buf, rdx = len
    mov     $SYS_WRITE, %eax
    syscall
    ret

sys_read:
    # rdi = fd, rsi = buf, rdx = len
    mov     $SYS_READ, %eax
    syscall
    ret

sys_open:
    # rdi = path, rsi = flags, rdx = mode
    mov     $SYS_OPEN, %eax
    syscall
    ret

sys_close:
    mov     $SYS_CLOSE, %eax
    syscall
    ret

# ═══════════════════════════════════════════════════════════════════════════
# Task 6: LCA (5D weighted average)
# ═══════════════════════════════════════════════════════════════════════════

op_lca:
    # Pop 2 chains, compute per-dimension average of first molecule
    # Chain A: (%r14), Chain B: 16(%r14)
    mov     (%r14), %rsi               # chain A ptr
    mov     8(%r14), %rax              # chain A len
    mov     16(%r14), %rdi             # chain B ptr
    mov     24(%r14), %rcx             # chain B len
    add     $32, %r14                  # pop 2

    # Need at least 5 bytes each (one molecule)
    cmp     $5, %rax
    jb      .lca_empty
    cmp     $5, %rcx
    jb      .lca_empty

    # Allocate 5 bytes on heap for result
    mov     %r15, %rax                  # result ptr

    # Dim 0: Shape — (A[0] + B[0]) / 2
    movzbl  (%rsi), %ecx
    movzbl  (%rdi), %edx
    add     %edx, %ecx
    shr     $1, %ecx
    movb    %cl, (%r15)

    # Dim 1: Relation
    movzbl  1(%rsi), %ecx
    movzbl  1(%rdi), %edx
    add     %edx, %ecx
    shr     $1, %ecx
    movb    %cl, 1(%r15)

    # Dim 2: Valence
    movzbl  2(%rsi), %ecx
    movzbl  2(%rdi), %edx
    add     %edx, %ecx
    shr     $1, %ecx
    movb    %cl, 2(%r15)

    # Dim 3: Arousal
    movzbl  3(%rsi), %ecx
    movzbl  3(%rdi), %edx
    add     %edx, %ecx
    shr     $1, %ecx
    movb    %cl, 3(%r15)

    # Dim 4: Time
    movzbl  4(%rsi), %ecx
    movzbl  4(%rdi), %edx
    add     %edx, %ecx
    shr     $1, %ecx
    movb    %cl, 4(%r15)

    # Push result
    sub     $16, %r14
    mov     %rax, (%r14)                # ptr
    movq    $5, 8(%r14)                 # len = 5
    add     $5, %r15                    # bump heap
    jmp     vm_loop

.lca_empty:
    # Push empty
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Task 7: Edge + Query (relation ops)
# ═══════════════════════════════════════════════════════════════════════════

op_edge:
    # [0x10, relation_byte] — create edge between top 2 stack items
    movzbl  (%r12, %r13), %eax         # relation byte
    inc     %r13
    # Stub: pop 2, push empty (no graph in standalone VM)
    add     $32, %r14
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

op_query:
    # [0x11, relation_byte]
    movzbl  (%r12, %r13), %eax
    inc     %r13
    # Stub: pop 1, push empty
    add     $16, %r14
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# File I/O operations (standalone)
# ═══════════════════════════════════════════════════════════════════════════

op_file_read:
    # Pop filename string, read file contents, push as string
    # Stack: [filename_ptr, filename_len] → [content_ptr, content_len]
    mov     (%r14), %rsi            # filename ptr (u16 molecules)
    mov     8(%r14), %rdx           # filename len (mol count)
    add     $16, %r14               # pop

    # Decode u16 filename → ASCII bytes in temp buffer
    lea     sha_input(%rip), %rdi   # reuse sha_input as temp (1024 bytes)
    xor     %rcx, %rcx
    test    %rdx, %rdx
    jz      .fread_empty
.fread_decode:
    cmp     %rdx, %rcx
    jge     .fread_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .fread_decode

.fread_open:
    movb    $0, (%rdi, %rcx)        # null-terminate
    # Open file
    lea     sha_input(%rip), %rdi   # filename
    xor     %esi, %esi              # O_RDONLY
    xor     %edx, %edx              # mode 0
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      .fread_empty             # open failed → push empty

    # Read file into heap — loop until EOF (supports files > 1MB)
    mov     %rax, %r8               # save fd
    xor     %r9d, %r9d              # r9 = total bytes read
.fread_loop:
    mov     %r8, %rdi               # fd
    lea     (%r15, %r9), %rsi       # buf = heap + total_read
    mov     $0x100000, %edx         # read 1MB chunks
    mov     $SYS_READ, %eax
    syscall
    test    %rax, %rax
    jle     .fread_done             # EOF or error
    add     %rax, %r9               # total += bytes_read
    cmp     $0x100000, %rax         # got full chunk?
    je      .fread_loop             # yes → more data
.fread_done:
    # Close file
    mov     %r8, %rdi
    mov     $SYS_CLOSE, %eax
    syscall

    mov     %r9, %rcx               # total bytes_read
    test    %rcx, %rcx
    jle     .fread_empty

    # Encode bytes → u16 molecules on heap (after raw bytes)
    mov     %r15, %r8               # raw bytes base
    add     %rcx, %r15              # bump past raw bytes
    mov     %r15, %rdi              # u16 output base
    xor     %rdx, %rdx
.fread_encode:
    cmp     %rcx, %rdx
    jge     .fread_push
    movzbl  (%r8, %rdx), %eax
    or      $0x2100, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    inc     %rdx
    jmp     .fread_encode

.fread_push:
    # Push string (ptr=u16 base, len=mol count)
    sub     $16, %r14
    mov     %rdi, (%r14)            # u16 base
    mov     %rcx, 8(%r14)           # mol count = byte count
    jmp     vm_loop

.fread_empty:
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

op_file_write:
    movl    $0x241, fwrite_flags(%rip)  # O_WRONLY|O_CREAT|O_TRUNC
    jmp     .fwrite_common
op_file_append:
    movl    $0x441, fwrite_flags(%rip)  # O_WRONLY|O_CREAT|O_APPEND
.fwrite_common:
    # Pop content, pop filename, write to file
    # Stack: [filename, content]
    mov     (%r14), %r8             # content ptr
    mov     8(%r14), %r9            # content len
    mov     16(%r14), %rsi          # filename ptr
    mov     24(%r14), %rdx          # filename len
    add     $32, %r14               # pop both

    # Decode filename → ASCII
    lea     sha_input(%rip), %rdi
    xor     %rcx, %rcx
.fwrite_fname:
    cmp     %rdx, %rcx
    jge     .fwrite_open
    movzbl  (%rsi, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .fwrite_fname
.fwrite_open:
    movb    $0, (%rdi, %rcx)
    # Open/create file
    lea     sha_input(%rip), %rdi
    mov     fwrite_flags(%rip), %esi  # O_WRONLY|O_CREAT|O_TRUNC or O_APPEND
    mov     $0644, %edx             # permissions
    mov     $SYS_OPEN, %eax
    syscall
    test    %rax, %rax
    js      vm_loop                  # open failed

    # Decode content u16 → bytes, write
    push    %rax                    # save fd
    lea     sha_input(%rip), %rdi   # reuse buffer for decoded content
    xor     %rcx, %rcx
.fwrite_decode:
    cmp     %r9, %rcx
    jge     .fwrite_do
    movzbl  (%r8, %rcx, 2), %eax
    mov     %al, (%rdi, %rcx)
    inc     %rcx
    jmp     .fwrite_decode
.fwrite_do:
    pop     %rdi                    # fd
    push    %rcx                    # save len
    lea     sha_input(%rip), %rsi   # buf
    pop     %rdx                    # len
    push    %rdi                    # save fd
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdi                    # fd
    mov     $SYS_CLOSE, %eax
    syscall
    # Push dummy result (builtin must leave value on stack for ExprStmt Pop)
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop

# ═══════════════════════════════════════════════════════════════════════════
# Task 7: Math ops (SSE2 — all x86_64 CPUs have this)
# ═══════════════════════════════════════════════════════════════════════════

# Binary math ops: pop 2 f64, compute, push result
# Stack layout for f64: [f64_bits:8][F64_MARKER:8]

op_add:
    movsd   16(%r14), %xmm0            # B (deeper in stack)
    movsd   (%r14), %xmm1              # A (top)
    add     $16, %r14                   # pop 1
    addsd   %xmm1, %xmm0
    movsd   %xmm0, (%r14)
    jmp     vm_loop

op_sub:
    movsd   16(%r14), %xmm0            # B
    movsd   (%r14), %xmm1              # A
    add     $16, %r14
    subsd   %xmm1, %xmm0              # B - A
    movsd   %xmm0, (%r14)
    jmp     vm_loop

op_mul:
    movsd   16(%r14), %xmm0
    movsd   (%r14), %xmm1
    add     $16, %r14
    mulsd   %xmm1, %xmm0
    movsd   %xmm0, (%r14)
    jmp     vm_loop

op_div:
    movsd   (%r14), %xmm1              # A (divisor)
    # Check zero
    xorpd   %xmm2, %xmm2
    ucomisd %xmm2, %xmm1
    je      vm_error_div_zero
    movsd   16(%r14), %xmm0            # B (dividend)
    add     $16, %r14
    divsd   %xmm1, %xmm0              # B / A
    movsd   %xmm0, (%r14)
    jmp     vm_loop

op_mod:
    # f64 modulo: a - floor(a/b) * b
    movsd   (%r14), %xmm1              # A (divisor)
    xorpd   %xmm2, %xmm2
    ucomisd %xmm2, %xmm1
    je      vm_error_div_zero
    movsd   16(%r14), %xmm0            # B (dividend)
    add     $16, %r14
    movapd  %xmm0, %xmm3              # save B
    divsd   %xmm1, %xmm0              # B/A
    roundsd $1, %xmm0, %xmm0          # floor(B/A)  (SSE4.1)
    mulsd   %xmm1, %xmm0              # floor(B/A)*A
    subsd   %xmm0, %xmm3              # B - floor(B/A)*A
    movsd   %xmm3, (%r14)
    jmp     vm_loop

op_neg:
    # Negate top f64
    movsd   (%r14), %xmm0
    mov     $0x8000000000000000, %rax   # sign bit mask
    movq    %rax, %xmm1
    xorpd   %xmm1, %xmm0
    movsd   %xmm0, (%r14)
    jmp     vm_loop

# ── Comparison ops (return f64: 1.0 = true, 0.0 = false) ────────────────

op_cmp_eq:
    movsd   16(%r14), %xmm0            # B (deeper)
    movsd   (%r14), %xmm1              # A (top)
    add     $16, %r14                   # pop 1, result overwrites remaining
    ucomisd %xmm1, %xmm0
    je      .cmp_true
    jmp     .cmp_false

op_cmp_lt:
    movb    $0, %al                     # op_id = 0 (LT)
    jmp     .cmp_dispatch
op_cmp_gt:
    movb    $1, %al                     # op_id = 1 (GT)
    jmp     .cmp_dispatch
op_cmp_le:
    movb    $2, %al                     # op_id = 2 (LE)
    jmp     .cmp_dispatch
op_cmp_ge:
    movb    $3, %al                     # op_id = 3 (GE)

.cmp_dispatch:
    # Check if both are f64
    push    %rax                        # save op_id
    mov     8(%r14), %rax              # b len
    mov     24(%r14), %rcx             # a len
    cmp     $F64_MARKER, %rax
    jne     .cmp_chain
    cmp     $F64_MARKER, %rcx
    jne     .cmp_chain

    # Both f64 — compare as floating point
    movsd   16(%r14), %xmm0           # a
    movsd   (%r14), %xmm1             # b
    add     $16, %r14                  # pop 1
    pop     %rax                       # restore op_id
    # Dispatch op_id FIRST, then ucomisd (to avoid clobbering flags)
    test    %al, %al
    jz      .cmp_f64_lt
    cmp     $1, %al
    je      .cmp_f64_gt
    cmp     $2, %al
    je      .cmp_f64_le
    # op_id == 3: GE
    ucomisd %xmm1, %xmm0
    jae     .cmp_true
    jmp     .cmp_false
.cmp_f64_lt:
    ucomisd %xmm1, %xmm0
    jb      .cmp_true
    jmp     .cmp_false
.cmp_f64_gt:
    ucomisd %xmm1, %xmm0
    ja      .cmp_true
    jmp     .cmp_false
.cmp_f64_le:
    ucomisd %xmm1, %xmm0
    jbe     .cmp_true
    jmp     .cmp_false

.cmp_chain:
    # At least one is a chain — compare by low bytes of molecules
    # a: 16(%r14)=ptr, 24(%r14)=mol_count
    # b: (%r14)=ptr, 8(%r14)=mol_count
    mov     (%r14), %rsi               # b ptr
    mov     8(%r14), %rdx              # b mol_count (might be F64_MARKER)
    mov     16(%r14), %rdi             # a ptr
    mov     24(%r14), %rcx             # a mol_count (might be F64_MARKER)
    add     $16, %r14                  # pop 1

    # If either has F64_MARKER, treat as len=0 for comparison (type mismatch)
    cmp     $F64_MARKER, %rdx
    jne     .cmp_chain_b_ok
    xor     %edx, %edx                 # b len = 0
.cmp_chain_b_ok:
    cmp     $F64_MARKER, %rcx
    jne     .cmp_chain_a_ok
    xor     %ecx, %ecx                 # a len = 0
.cmp_chain_a_ok:
    # Compare molecule by molecule (low byte = char value)
    # Use min(a_len, b_len) for loop count
    mov     %rcx, %r8                  # save a_len
    mov     %rdx, %r9                  # save b_len
    cmp     %rdx, %rcx
    jbe     .cmp_chain_use_a
    mov     %rdx, %rcx                 # min_len = b_len
.cmp_chain_use_a:
    # rcx = min_len, rdi = a_ptr, rsi = b_ptr
    test    %rcx, %rcx
    jz      .cmp_chain_len             # both empty prefix → compare lengths
.cmp_chain_loop:
    movzbl  (%rdi), %eax               # a low byte (char value)
    movzbl  (%rsi), %edx               # b low byte (char value)
    cmp     %edx, %eax
    jb      .cmp_chain_a_less
    ja      .cmp_chain_a_greater
    add     $2, %rdi                   # next molecule (u16)
    add     $2, %rsi
    dec     %rcx
    jnz     .cmp_chain_loop

.cmp_chain_len:
    # All compared bytes equal — compare lengths
    cmp     %r9, %r8                   # a_len vs b_len
    jb      .cmp_chain_a_less
    ja      .cmp_chain_a_greater
    # Equal: a_len == b_len and all bytes match
    pop     %rax                       # op_id
    cmp     $2, %al                    # LE or GE → true for equal
    jae     .cmp_true
    jmp     .cmp_false                 # LT or GT → false for equal

.cmp_chain_a_less:
    pop     %rax                       # op_id
    test    %al, %al                   # LT (0)?
    jz      .cmp_true
    cmp     $2, %al                    # LE (2)?
    je      .cmp_true
    jmp     .cmp_false                 # GT (1) or GE (3) → false

.cmp_chain_a_greater:
    pop     %rax                       # op_id
    cmp     $1, %al                    # GT (1)?
    je      .cmp_true
    cmp     $3, %al                    # GE (3)?
    je      .cmp_true
    jmp     .cmp_false                 # LT (0) or LE (2) → false

.cmp_true:
    # Rust VM returns non-empty chain (1.0) for true
    mov     $0x3FF0000000000000, %rax   # 1.0 in f64
    mov     %rax, (%r14)
    movq    $F64_MARKER, 8(%r14)
    jmp     vm_loop

.cmp_false:
    # Return f64 0.0 for false (NOT empty chain — breaks &&/|| logic)
    xor     %eax, %eax
    mov     %rax, (%r14)               # f64 bits = 0 (0.0)
    movq    $F64_MARKER, 8(%r14)       # mark as f64
    jmp     vm_loop

vm_error_div_zero:
    lea     msg_divzero(%rip), %rsi
    mov     $16, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

# ═══════════════════════════════════════════════════════════════════════════
# f64 → ASCII decimal conversion
# ═══════════════════════════════════════════════════════════════════════════
# Input:  xmm0 = f64 value
# Output: writes ASCII to heap at r15, returns length in eax
# Clobbers: rax, rcx, rdx, rdi, xmm0-xmm3

f64_to_ascii:
    mov     %r15, %rdi                  # output buffer = heap

    # Handle negative
    xorpd   %xmm1, %xmm1
    ucomisd %xmm1, %xmm0
    jae     .f2a_positive
    movb    $'-', (%rdi)
    inc     %rdi
    # Negate
    mov     $0x8000000000000000, %rax
    movq    %rax, %xmm1
    xorpd   %xmm1, %xmm0

.f2a_positive:
    # Handle zero
    xorpd   %xmm1, %xmm1
    ucomisd %xmm1, %xmm0
    jne     .f2a_nonzero
    movb    $'0', (%rdi)
    inc     %rdi
    jmp     .f2a_done

.f2a_nonzero:
    # Split integer and fractional parts
    # integer = (u64)f64
    cvttsd2si %xmm0, %rax              # rax = integer part
    test    %rax, %rax
    js      .f2a_large                  # overflow → print "inf"

    # Convert integer part via stack (reversed digits)
    cvtsi2sd %rax, %xmm1               # xmm1 = (f64)integer
    subsd   %xmm1, %xmm0              # xmm0 = fractional part
    push    %rax                        # save for later

    # Integer digits
    mov     %rdi, %rcx                  # save start pos
    mov     %rsp, %rdx                  # save rsp
    # Push digits in reverse
    mov     (%rdx), %rax                # restore integer
    add     $8, %rsp                    # balance the push above
    test    %rax, %rax
    jnz     .f2a_int_digits
    movb    $'0', (%rdi)
    inc     %rdi
    jmp     .f2a_frac

.f2a_int_digits:
    # We need to output digits. Use a temp buffer on stack.
    sub     $24, %rsp                   # temp buffer for digits
    mov     %rsp, %rcx                  # digit buffer
    xor     %edx, %edx                  # digit count

.f2a_int_loop:
    test    %rax, %rax
    jz      .f2a_int_copy
    mov     $10, %r8
    xor     %edx, %edx
    push    %rdx
    cqo
    idiv    %r8                         # rax = quotient, rdx = remainder
    pop     %r8                         # just balance (we don't use r8 here)
    add     $'0', %dl
    movb    %dl, (%rcx)
    inc     %rcx
    jmp     .f2a_int_loop

.f2a_int_copy:
    # Copy digits in reverse (they're backwards in buffer)
.f2a_int_rev:
    dec     %rcx
    cmp     %rsp, %rcx
    jb      .f2a_int_rev_done
    movb    (%rcx), %al
    movb    %al, (%rdi)
    inc     %rdi
    jmp     .f2a_int_rev

.f2a_int_rev_done:
    add     $24, %rsp

.f2a_frac:
    # Fractional part: multiply by 10, take integer, repeat (6 digits)
    xorpd   %xmm1, %xmm1
    ucomisd %xmm1, %xmm0
    je      .f2a_done                   # no fractional part

    movb    $'.', (%rdi)
    inc     %rdi
    mov     $6, %ecx                    # 6 decimal places

    # Load 10.0
    mov     $0x4024000000000000, %rax   # 10.0 in f64
    movq    %rax, %xmm2

.f2a_frac_loop:
    mulsd   %xmm2, %xmm0              # frac *= 10
    cvttsd2si %xmm0, %rax             # digit = (int)frac
    cmp     $9, %rax
    ja      .f2a_frac_clamp
    jmp     .f2a_frac_store
.f2a_frac_clamp:
    mov     $9, %eax
.f2a_frac_store:
    add     $'0', %al
    movb    %al, (%rdi)
    inc     %rdi
    # frac -= digit
    sub     $'0', %al
    movzbl  %al, %eax
    cvtsi2sd %eax, %xmm1
    subsd   %xmm1, %xmm0
    dec     %ecx
    jnz     .f2a_frac_loop

    # Strip trailing zeros
.f2a_strip:
    dec     %rdi
    cmpb    $'0', (%rdi)
    je      .f2a_strip
    cmpb    $'.', (%rdi)
    je      .f2a_done                   # don't keep trailing dot
    inc     %rdi                        # keep last non-zero

.f2a_done:
    # Calculate length
    mov     %rdi, %rax
    sub     %r15, %rax                  # length
    ret

.f2a_large:
    # Number too large for i64 → print "inf"
    movb    $'i', (%rdi)
    movb    $'n', 1(%rdi)
    movb    $'f', 2(%rdi)
    add     $3, %rdi
    jmp     .f2a_done

# ═══════════════════════════════════════════════════════════════════════════
# Variable name table (simple linear search)
# ═══════════════════════════════════════════════════════════════════════════
#
# Layout on heap:
#   var_table_ptr → [count:8][entries...]
#   Each entry: [name_len:1][name:N][value_ptr:8][value_len:8]
#
# For standalone VM, we keep it simple: max 256 variables, linear scan.

.equ VAR_TABLE_MAX, 1024

# Initialize variable table — called once from _start
# Uses r15 (heap bump), stores table base in var_table_base
var_table_init:
    mov     %r15, %rax
    lea     var_table_base(%rip), %rcx
    mov     %rax, (%rcx)               # save table base address
    movq    $0, (%r15)                  # count = 0
    # Reserve 4096 entries × 24 bytes + 8 header = 98312 bytes
    add     $98312, %r15
    # Initialize var shadow stack (for closure scoping)
    mov     %r15, %rax
    lea     var_shadow_base(%rip), %rcx
    mov     %rax, (%rcx)
    lea     var_shadow_top(%rip), %rcx
    mov     %rax, (%rcx)
    lea     closure_depth(%rip), %rcx
    movq    $0, (%rcx)
    # Reserve 4MB for shadow stack (deep recursion snapshots)
    add     $0x400000, %r15
    # Initialize register frame stack (4MB on heap)
    mov     %r15, %rax
    lea     reg_frame_base(%rip), %rcx
    mov     %rax, (%rcx)
    lea     reg_frame_ptr(%rip), %rcx
    mov     %rax, (%rcx)                # frame_ptr = base (empty)
    lea     reg_cur_frame(%rip), %rcx
    mov     %rax, (%rcx)                # cur_frame = base
    add     $REG_STACK_SIZE, %r15       # reserve 4MB
    ret

# ── Function Registry: register + lookup ──
# fn_reg_store: rax=hash, r8=body_pc (as ptr), r9=param_count (as len/marker)
# Registers function in fn_registry for O(1) lookup by Call opcode.
fn_reg_store:
    push    %rbx
    lea     fn_registry(%rip), %rbx
    # Slot = hash & 0x3FF (1024 slots)
    mov     %rax, %rcx
    and     $0x3FF, %ecx
    imul    $24, %ecx
    # Check if slot is empty (hash == 0)
    cmpq    $0, (%rbx, %rcx)
    jne     .frs_collision
    # Empty slot: write entry
    mov     %rax, (%rbx, %rcx)         # hash
    mov     %r8, 8(%rbx, %rcx)        # body_pc (closure ptr)
    mov     %r9, 16(%rbx, %rcx)       # param_count (closure len/marker)
    pop     %rbx
    ret
.frs_collision:
    # Slot occupied: check if same hash (update)
    cmp     %rax, (%rbx, %rcx)
    jne     .frs_chain
    # Same hash: update
    mov     %r8, 8(%rbx, %rcx)
    mov     %r9, 16(%rbx, %rcx)
    pop     %rbx
    ret
.frs_chain:
    # Different hash: linear probe (find next empty)
    add     $24, %ecx
    and     $0x5FF8, %ecx              # wrap within 1024 slots × 24
    cmpq    $0, (%rbx, %rcx)
    jne     .frs_chain
    mov     %rax, (%rbx, %rcx)
    mov     %r8, 8(%rbx, %rcx)
    mov     %r9, 16(%rbx, %rcx)
    pop     %rbx
    ret

# fn_reg_lookup: rax=hash → r8=body_pc, r9=param_count. CF=1 if not found.
fn_reg_lookup:
    push    %rbx
    lea     fn_registry(%rip), %rbx
    mov     %rax, %rcx
    and     $0x3FF, %ecx
    imul    $24, %ecx
    # Check slot
    cmpq    $0, (%rbx, %rcx)
    je      .frl_not_found
    cmp     %rax, (%rbx, %rcx)
    jne     .frl_probe
    # Found!
    mov     8(%rbx, %rcx), %r8        # body_pc
    mov     16(%rbx, %rcx), %r9       # param_count
    pop     %rbx
    clc                                # CF=0 = found
    ret
.frl_probe:
    add     $24, %ecx
    and     $0x5FF8, %ecx
    cmpq    $0, (%rbx, %rcx)
    je      .frl_not_found
    cmp     %rax, (%rbx, %rcx)
    jne     .frl_probe
    mov     8(%rbx, %rcx), %r8
    mov     16(%rbx, %rcx), %r9
    pop     %rbx
    clc
    ret
.frl_not_found:
    pop     %rbx
    stc                                # CF=1 = not found
    ret

# var_store_global: rax=hash, r8=val_ptr, r9=val_len
# Like var_store_hash but searches ENTIRE table (frame_start=0).
# Used by StoreUpdate (x = val) — Julia-style global variable update.
var_store_global:
    push    %rbx
    lea     var_table_base(%rip), %rbx
    mov     (%rbx), %rbx               # table base
    mov     (%rbx), %rcx               # total count
    test    %rcx, %rcx
    jle     .vsg_new
    # Reverse search from LAST entry to 0 (full table, no frame limit)
    lea     -1(%rcx), %rdx
    imul    $24, %rdx
    lea     8(%rbx, %rdx), %rdi
    mov     %rcx, %rdx                  # loop count = all entries
.vsg_search:
    cmp     (%rdi), %rax
    jne     .vsg_next
    # Found — update in place
    mov     %r8, 8(%rdi)
    mov     %r9, 16(%rdi)
    pop     %rbx
    ret
.vsg_next:
    sub     $24, %rdi
    dec     %rdx
    jnz     .vsg_search
.vsg_new:
    # Not found anywhere — append as new entry
    lea     var_table_base(%rip), %rbx
    mov     (%rbx), %rbx
    mov     (%rbx), %rcx
    imul    $24, %rcx, %rdx
    lea     8(%rbx, %rdx), %rdi
    mov     %rax, (%rdi)
    mov     %r8, 8(%rdi)
    mov     %r9, 16(%rdi)
    incq    (%rbx)
    pop     %rbx
    ret

# ── Hash-based variable table ──
# Table layout: [count:8][entries...]
# Entry: [name_hash:8][val_ptr:8][val_len:8] = 24 bytes
# Simple, no string comparison needed.

# hash_name: rdi=name ptr, edx=name len → rax=hash
# DIRECT-MAPPED CACHE: 256 entries indexed by (ptr & 0xFF)
# Each entry: [ptr:8][hash:8] = 16 bytes. Total: 4KB.
# Hit = 1 cmp + 1 mov. Miss = FNV + 1 store.
hash_name:
    lea     hn_cache(%rip), %rax
    mov     %rdi, %rsi
    and     $0xFF, %esi                 # index = ptr & 255
    shl     $4, %esi                    # × 16 bytes per entry
    cmp     %rdi, (%rax, %rsi)        # same ptr?
    jne     .hn_compute
    mov     8(%rax, %rsi), %rax       # cache hit!
    ret
.hn_compute:
    push    %rdi
    push    %rdx
    push    %rsi                        # save cache index
    movabs  $0xcbf29ce484222325, %rax
    movabs  $0x100000001b3, %rcx
    add     %rdi, %rdx
.hn_loop:
    cmp     %rdx, %rdi
    jae     .hn_done
    movzbl  (%rdi), %esi
    xor     %rsi, %rax
    imul    %rcx, %rax
    inc     %rdi
    jmp     .hn_loop
.hn_done:
    pop     %rsi                        # restore cache index
    pop     %rdx
    pop     %rdi
    # Store in cache
    lea     hn_cache(%rip), %rcx
    mov     %rdi, (%rcx, %rsi)        # ptr
    mov     %rax, 8(%rcx, %rsi)       # hash
    ret

# var_store_hash: rax=hash, r8=val_ptr, r9=val_len
# FRAME-AWARE: only search within current frame (depth_stack[depth-1]..count)
# This ensures inner function "n" SHADOWS outer "n" instead of overwriting
var_store_hash:
    push    %rbx
    lea     var_table_base(%rip), %rbx
    mov     (%rbx), %rbx               # table base
    mov     (%rbx), %rcx               # total count

    # Get frame_start: if depth > 0, start from depth_stack[depth-1]
    push    %rax                        # save hash
    lea     closure_depth(%rip), %rdi
    mov     (%rdi), %rdi               # depth
    xor     %r10d, %r10d               # frame_start = 0 (default: search all)
    test    %rdi, %rdi
    jz      .vsh_have_start
    dec     %rdi
    lea     scope_frame_ptrs(%rip), %r10
    mov     (%r10, %rdi, 8), %r10d    # frame_start = depth_stack[depth-1]
.vsh_have_start:
    pop     %rax                        # restore hash

    # entries_in_frame = count - frame_start
    mov     %rcx, %rdi                 # total count
    sub     %r10, %rdi                  # entries to search
    test    %rdi, %rdi
    jle     .vsh_new                    # no entries in frame → append

    # Reverse search from LAST entry to frame_start
    lea     -1(%rcx), %rdx
    imul    $24, %rdx
    lea     8(%rbx, %rdx), %rdi        # last entry ptr
    mov     %rcx, %rdx
    sub     %r10, %rdx                  # loop count = entries in frame

.vsh_search:
    cmp     (%rdi), %rax
    jne     .vsh_next
    # Found in current frame — update
    mov     %r8, 8(%rdi)
    mov     %r9, 16(%rdi)
    pop     %rbx
    ret
.vsh_next:
    sub     $24, %rdi
    dec     %rdx
    jnz     .vsh_search
    # Not found in frame → append as shadow

.vsh_new:
    # Append new entry at end
    lea     var_table_base(%rip), %rbx
    mov     (%rbx), %rbx
    mov     (%rbx), %rcx
    imul    $24, %rcx, %rdx
    lea     8(%rbx, %rdx), %rdi
    mov     %rax, (%rdi)
    mov     %r8, 8(%rdi)
    mov     %r9, 16(%rdi)
    incq    (%rbx)                     # count++
    pop     %rbx
    ret

# var_load_hash: rax=hash → rax=val_ptr, rdx=val_len
# REVERSE search with 4-entry direct-mapped cache (count-validated)
var_load_hash:
    # Check cache: 256 entries × [hash:8, entry_ptr:8, vt_count:8] = 24 bytes each
    lea     var_cache(%rip), %rcx
    mov     %rax, %rdx
    and     $0xFF, %edx
    imul    $24, %edx                   # × 24 bytes per entry
    cmp     %rax, (%rcx, %rdx)          # hash match?
    jne     .vlh_cache_miss
    # Check var_table count hasn't changed (scope change detection)
    lea     var_table_base(%rip), %rsi
    mov     (%rsi), %rsi
    mov     (%rsi), %rsi                # current count
    cmp     %rsi, 16(%rcx, %rdx)        # count match?
    jne     .vlh_cache_miss
    mov     8(%rcx, %rdx), %rcx         # entry_ptr
    mov     8(%rcx), %rax               # val_ptr
    mov     16(%rcx), %rdx              # val_len
    ret
.vlh_cache_miss:

    push    %rbx
    lea     var_table_base(%rip), %rbx
    mov     (%rbx), %rbx
    mov     (%rbx), %rcx
    test    %rcx, %rcx
    jz      .vlh_not_found

    # Start from last entry
    lea     -1(%rcx), %rdx
    imul    $24, %rdx
    lea     8(%rbx, %rdx), %rdi

.vlh_search:
    cmp     (%rdi), %rax
    jne     .vlh_next
    # Found — update cache (256 slots)
    push    %rax
    lea     var_cache(%rip), %rcx
    mov     %rax, %rdx
    and     $0xFF, %edx
    imul    $24, %edx
    mov     %rax, (%rcx, %rdx)          # store hash
    mov     %rdi, 8(%rcx, %rdx)         # store entry pointer
    # Store current var_table count for validation
    lea     var_table_base(%rip), %rax
    mov     (%rax), %rax
    mov     (%rax), %rax
    mov     %rax, 16(%rcx, %rdx)        # store count
    pop     %rax
    mov     8(%rdi), %rax
    mov     16(%rdi), %rdx
    pop     %rbx
    ret
.vlh_next:
    sub     $24, %rdi
    dec     %rcx
    jnz     .vlh_search

.vlh_not_found:
    xor     %eax, %eax
    xor     %edx, %edx
    pop     %rbx
    ret

# ═══════════════════════════════════════════════════════════════════════════
# Utilities
# ═══════════════════════════════════════════════════════════════════════════

# memcpy_small: rdi=dest, rsi=src, edx=count (< 256)
memcpy_small:
    mov     %edx, %ecx
    rep     movsb
    ret

# ═══════════════════════════════════════════════════════════════════════════
# Error handlers
# ═══════════════════════════════════════════════════════════════════════════

op_unknown:
    # al still has the opcode byte
    push    %rax
    movzbl  %al, %eax
    # Convert to 2-char hex string on stack
    mov     %eax, %ecx
    shr     $4, %ecx
    and     $0xF, %ecx
    lea     hex_chars(%rip), %rdx
    movzbl  (%rdx, %rcx), %ecx
    and     $0xF, %eax
    movzbl  (%rdx, %rax), %eax
    # Write "0xNN\n"
    sub     $8, %rsp
    movb    $0x30, (%rsp)           # '0'
    movb    $0x78, 1(%rsp)          # 'x'
    movb    %cl, 2(%rsp)            # high nibble
    movb    %al, 3(%rsp)            # low nibble
    movb    $0x0A, 4(%rsp)          # newline
    mov     %rsp, %rsi
    mov     $5, %edx
    mov     $2, %edi                # stderr
    mov     $1, %eax                # SYS_WRITE
    syscall
    add     $8, %rsp
    pop     %rax

    lea     msg_unknown(%rip), %rsi
    mov     $18, %edx
    mov     $2, %edi                    # stderr
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

op_nop:
    jmp     vm_loop

err_open:
    lea     msg_open(%rip), %rsi
    mov     $22, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

err_header:
    lea     msg_header(%rip), %rsi
    mov     $20, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

err_mmap:
    lea     msg_mmap(%rip), %rsi
    mov     $13, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

err_read_bc:
    lea     msg_readbc(%rip), %rsi
    mov     $21, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     op_halt_err

# ═══════════════════════════════════════════════════════════════════════════
# Exit
# ═══════════════════════════════════════════════════════════════════════════

op_halt:
    # Check if this is a nested halt (from __eval_bytecode or REPL trampoline)
    # Sentinels: 0xDEAD_BEEF_CAFE_0001 = eval_bytecode, 0002 = REPL trampoline
    .equ NESTED_SENTINEL, 0xDEADBEEFCAFE0001
    .equ REPL_SENTINEL,   0xDEADBEEFCAFE0002
    .equ MCP_SENTINEL,    0xDEADBEEFCAFE0003
    mov     (%rsp), %rax               # PEEK (not pop)

    movabs  $NESTED_SENTINEL, %rdx
    cmp     %rdx, %rax
    je      .halt_nested                # eval_bytecode → restore + vm_loop

    movabs  $REPL_SENTINEL, %rdx
    cmp     %rdx, %rax
    je      .halt_repl                  # REPL trampoline → restore + repl_loop

    movabs  $MCP_SENTINEL, %rdx
    cmp     %rdx, %rax
    je      .halt_mcp                   # MCP trampoline → restore + mcp_read_line

    jmp     .halt_boot                  # not nested → boot completion

.halt_nested:
    # __eval_bytecode: pop sentinel + saved context, continue caller bytecode
    add     $8, %rsp                    # skip sentinel
    jmp     .halt_nested_restore

.halt_repl:
    # REPL trampoline: pop sentinel + saved context, return to REPL loop
    add     $8, %rsp                    # skip sentinel
    pop     %rbx                        # restore bytecode size
    pop     %r13                        # restore PC
    pop     %r12                        # restore bytecode base
    jmp     .repl_loop                  # back to REPL prompt

.halt_mcp:
    # MCP trampoline: pop sentinel + saved context
    add     $8, %rsp                    # skip sentinel
    pop     %rbx                        # restore bytecode size
    pop     %r13                        # restore PC
    pop     %r12                        # restore bytecode base
    movb    $0, mcp_capture(%rip)       # re-enable emit for response output
    # Write mcp_dispatch result from VM stack to stdout
    mov     (%r14), %rsi               # chain ptr
    mov     8(%r14), %rdx              # mol_count
    add     $16, %r14                  # pop
    test    %rdx, %rdx
    jz      .halt_mcp_done             # empty result → skip output
    # Decode string molecules → ASCII on heap, then write
    mov     %r15, %rdi                  # temp decode buffer
    mov     %rdx, %rcx
.halt_mcp_decode:
    test    %rcx, %rcx
    jz      .halt_mcp_write
    movzwl  (%rsi), %eax
    movb    %al, (%rdi)
    add     $2, %rsi
    inc     %rdi
    dec     %rcx
    jmp     .halt_mcp_decode
.halt_mcp_write:
    mov     %r15, %rsi                  # buf
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall
    # Write newline
    lea     str_newline(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
.halt_mcp_done:
    jmp     .mcp_read_line


.halt_boot:
    # Not nested — this is the boot completion.

    # Check eval_mode: if set, process stdin and exit (no REPL)
    cmpb    $1, eval_mode(%rip)
    je      .eval_stdin
    cmpb    $2, eval_mode(%rip)
    je      .mcp_stdin

    # Print greeting
    lea     msg_greeting(%rip), %rsi
    movzbl  msg_greeting_len(%rip), %edx
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall

    # Check if stdin is a terminal (for raw mode)
    # isatty(0) via ioctl(0, TCGETS, &termios)
    lea     repl_termios_old(%rip), %rdx
    mov     $0x5401, %esi               # TCGETS
    xor     %edi, %edi                  # fd = stdin
    mov     $16, %eax                   # SYS_ioctl
    syscall
    test    %rax, %rax
    jnz     .repl_no_raw                # not a terminal → skip raw mode
    # Save old termios, set raw mode
    lea     repl_termios_old(%rip), %rsi
    lea     repl_termios_raw(%rip), %rdi
    mov     $60, %ecx
    rep     movsb                        # copy old → raw
    # Clear ICANON(0x2) and ECHO(0x8) in c_lflag (offset 12)
    lea     repl_termios_raw(%rip), %rdi
    movl    12(%rdi), %eax
    and     $~0x0A, %eax                # clear ICANON|ECHO
    movl    %eax, 12(%rdi)
    # Set VMIN=1, VTIME=0 in c_cc (offset 17)
    movb    $1, 23(%rdi)                # c_cc[VMIN=6] at offset 17+6=23
    movb    $0, 22(%rdi)                # c_cc[VTIME=5] at offset 17+5=22
    # Apply raw termios
    lea     repl_termios_raw(%rip), %rdx
    mov     $0x5402, %esi               # TCSETS
    xor     %edi, %edi                  # fd = stdin
    mov     $16, %eax                   # SYS_ioctl
    syscall
    movb    $1, repl_raw_mode(%rip)     # flag: raw mode active
.repl_no_raw:

    # ── REPL loop: read line → compile → execute ─────────────────
.repl_loop:
    # Restore heap to previous checkpoint (free temp allocations from last turn)
    lea     repl_heap_checkpoint(%rip), %rcx
    mov     (%rcx), %rax
    test    %rax, %rax
    jz      .repl_skip_restore              # first turn: no checkpoint yet
    mov     %rax, %r15                       # restore heap pointer
.repl_skip_restore:
    # Save heap checkpoint for this turn
    mov     %r15, %rax
    lea     repl_heap_checkpoint(%rip), %rcx
    mov     %rax, (%rcx)

    # Print prompt: "⦿ "
    lea     msg_prompt(%rip), %rsi
    mov     $4, %edx                    # "⦿ " (⦿ is 3 bytes UTF-8 + space)
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall

    # Read line from stdin (raw mode: handle Tab, Backspace, arrows)
    mov     %r15, %rsi                  # buf = heap ptr
    xor     %ecx, %ecx                  # bytes read = 0
.repl_read_byte:
    lea     (%rsi, %rcx), %rdx         # dest = buf + offset
    push    %rcx
    push    %rsi
    mov     %rdx, %rsi                  # buf for read
    mov     $1, %edx                    # read 1 byte
    xor     %edi, %edi                  # fd = stdin
    mov     $SYS_READ, %eax
    syscall
    pop     %rsi
    pop     %rcx
    test    %rax, %rax
    jle     .repl_read_done             # EOF or error
    movzbl  (%rsi, %rcx), %eax         # read byte

    # Check raw mode keys
    cmpb    $1, repl_raw_mode(%rip)
    jne     .repl_raw_skip

    # Backspace (0x7F or 0x08)
    cmp     $0x7F, %eax
    je      .repl_backspace
    cmp     $0x08, %eax
    je      .repl_backspace

    # Tab (0x09) — autocomplete
    cmp     $0x09, %eax
    je      .repl_tab

    # Enter (0x0D in raw mode)
    cmp     $0x0D, %eax
    je      .repl_raw_enter

    # ESC (0x1B) — start of arrow key sequence
    cmp     $0x1B, %eax
    je      .repl_esc_seq

    # Ctrl-C (0x03) → exit
    cmp     $0x03, %eax
    je      .repl_read_done

    # Ctrl-D (0x04) → exit
    cmp     $0x04, %eax
    je      .repl_read_done

    # Regular printable char → echo it
    push    %rcx
    push    %rsi
    lea     (%rsi, %rcx), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi
    pop     %rcx
    inc     %ecx
    cmp     $4095, %ecx
    jl      .repl_read_byte
    jmp     .repl_read_newline

.repl_raw_enter:
    # Echo newline
    push    %rcx
    push    %rsi
    lea     str_newline(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi
    pop     %rcx
    jmp     .repl_read_newline

.repl_backspace:
    test    %ecx, %ecx
    jz      .repl_read_byte             # nothing to delete
    dec     %ecx
    # Echo: backspace + space + backspace (erase char)
    push    %rcx
    push    %rsi
    lea     repl_bs_seq(%rip), %rsi     # "\b \b"
    mov     $3, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi
    pop     %rcx
    jmp     .repl_read_byte

.repl_esc_seq:
    # Read 2 more bytes for arrow key: ESC [ A/B/C/D
    push    %rcx
    push    %rsi
    sub     $2, %rsp
    mov     %rsp, %rsi
    mov     $2, %edx
    xor     %edi, %edi
    mov     $SYS_READ, %eax
    syscall
    movzbl  (%rsp), %eax
    movzbl  1(%rsp), %edx
    add     $2, %rsp
    pop     %rsi
    pop     %rcx
    cmp     $'[', %eax
    jne     .repl_read_byte             # not arrow key
    cmp     $'A', %edx
    je      .repl_history_up
    cmp     $'B', %edx
    je      .repl_history_down
    jmp     .repl_read_byte             # left/right → ignore for now

.repl_history_up:
    # TODO: implement history navigation
    jmp     .repl_read_byte
.repl_history_down:
    jmp     .repl_read_byte

.repl_tab:
    # Tab completion: find prefix in var_table
    test    %ecx, %ecx
    jz      .repl_read_byte             # empty → skip
    # Save current buffer state
    push    %rcx
    push    %rsi
    # Find word start (scan backwards for space or start)
    mov     %ecx, %r8d                  # r8 = end of word
    dec     %r8d
.repl_tab_word_start:
    cmp     $0, %r8d
    jl      .repl_tab_found_start
    cmpb    $' ', (%rsi, %r8)
    je      .repl_tab_found_start2
    dec     %r8d
    jmp     .repl_tab_word_start
.repl_tab_found_start2:
    inc     %r8d                         # skip the space
.repl_tab_found_start:
    cmp     $0, %r8d
    jge     .repl_tab_search
    xor     %r8d, %r8d
.repl_tab_search:
    # r8 = start of current word, ecx = end
    # Word = buf[r8..ecx]
    lea     (%rsi, %r8), %rdi           # word ptr
    mov     %ecx, %r9d
    sub     %r8d, %r9d                  # word len
    # Scan var_table for prefix match
    mov     var_table_base(%rip), %rbx
    mov     (%rbx), %r10               # entry count
    lea     8(%rbx), %rbx               # skip count header
    xor     %r11d, %r11d               # match count
    # First match ptr saved in repl_tab_match
    movq    $0, repl_tab_match(%rip)
    movq    $0, repl_tab_match_len(%rip)
.repl_tab_scan:
    cmp     %r10, %r11
    jge     .repl_tab_done
    # Entry: [hash:8, ptr:8, len+flags:8] = 24 bytes
    lea     (%rbx, %r11, 8), %rax
    lea     (%rax, %r11, 8), %rax
    lea     (%rax, %r11, 8), %rax       # entry = base + i*24
    mov     8(%rax), %r12               # name ptr (molecules)
    movzwl  16(%rax), %r13d             # name len (lower 16 bits)
    and     $0xFFFF, %r13d
    # Compare prefix: word[0..r9] vs name[0..r9]
    cmp     %r9d, %r13d
    jl      .repl_tab_scan_next         # name shorter than prefix
    # Compare r9 bytes
    push    %rdi
    push    %r9
    xor     %edx, %edx
.repl_tab_cmp:
    cmp     %r9d, %edx
    jge     .repl_tab_match_found
    movzbl  (%rdi, %rdx), %eax         # prefix byte
    movzbl  (%r12, %rdx, 2), %ecx     # name molecule low byte
    cmp     %eax, %ecx
    jne     .repl_tab_cmp_fail
    inc     %edx
    jmp     .repl_tab_cmp
.repl_tab_cmp_fail:
    pop     %r9
    pop     %rdi
    jmp     .repl_tab_scan_next
.repl_tab_match_found:
    pop     %r9
    pop     %rdi
    # Save first match
    cmpq    $0, repl_tab_match(%rip)
    jne     .repl_tab_has_match
    mov     %r12, repl_tab_match(%rip)
    mov     %r13, repl_tab_match_len(%rip)
.repl_tab_has_match:
.repl_tab_scan_next:
    inc     %r11
    jmp     .repl_tab_scan
.repl_tab_done:
    # If we found a match, complete the word
    cmpq    $0, repl_tab_match(%rip)
    je      .repl_tab_no_match
    mov     repl_tab_match(%rip), %r12
    mov     repl_tab_match_len(%rip), %r13
    pop     %rsi
    pop     %rcx
    # Append remaining chars of match to buffer
    mov     %r9d, %edx                  # edx = prefix len (already typed)
.repl_tab_append:
    cmp     %r13d, %edx
    jge     .repl_tab_append_done
    movzbl  (%r12, %rdx, 2), %eax     # molecule low byte
    movb    %al, (%rsi, %rcx)          # append to buffer
    # Echo the char
    push    %rcx
    push    %rsi
    push    %rdx
    lea     (%rsi, %rcx), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rdx
    pop     %rsi
    pop     %rcx
    inc     %ecx
    inc     %edx
    jmp     .repl_tab_append
.repl_tab_append_done:
    jmp     .repl_read_byte
.repl_tab_no_match:
    pop     %rsi
    pop     %rcx
    jmp     .repl_read_byte

.repl_raw_skip:
    # Non-raw mode: original behavior
    cmp     $0x0A, %eax                 # newline?
    je      .repl_read_newline
    inc     %ecx
    cmp     $4095, %ecx
    jl      .repl_read_byte
.repl_read_newline:
    # Multiline: check bracket balance before accepting line
    # Scan buffer for {, [, ( and }, ], ) — if unbalanced, read more
    push    %rcx
    push    %rsi
    xor     %edx, %edx                  # balance counter
    xor     %r8d, %r8d                  # scan index
.repl_bracket_scan:
    cmp     %ecx, %r8d
    jge     .repl_bracket_done
    movzbl  (%rsi, %r8), %eax
    cmp     $'{', %al
    je      .repl_bracket_open
    cmp     $'[', %al
    je      .repl_bracket_open
    cmp     $'(', %al
    je      .repl_bracket_open
    cmp     $'}', %al
    je      .repl_bracket_close
    cmp     $']', %al
    je      .repl_bracket_close
    cmp     $')', %al
    je      .repl_bracket_close
    jmp     .repl_bracket_next
.repl_bracket_open:
    inc     %edx
    jmp     .repl_bracket_next
.repl_bracket_close:
    dec     %edx
.repl_bracket_next:
    inc     %r8d
    jmp     .repl_bracket_scan
.repl_bracket_done:
    pop     %rsi
    pop     %rcx
    test    %edx, %edx
    jz      .repl_line_complete         # balanced → done
    # Unbalanced: print continuation prompt "... " and read more
    push    %rcx
    push    %rsi
    lea     msg_cont_prompt(%rip), %rsi
    mov     $4, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    pop     %rsi
    pop     %rcx
    # Replace newline with space and continue reading
    movb    $' ', (%rsi, %rcx)
    inc     %rcx
    jmp     .repl_read_byte
.repl_line_complete:
    inc     %rcx                        # include newline in count
.repl_read_done:
    mov     %rcx, %rax                  # rax = bytes read (compatible with old code)

    # Check for EOF or error
    test    %rax, %rax
    jle     .repl_exit                  # EOF → exit

    # Save input length and buffer pointer
    mov     %rax, %rcx                  # input length
    mov     %r15, %rdi                  # input buffer

    # Check for "exit\n" or "quit\n"
    cmp     $5, %rcx
    jne     .repl_check_help
    cmpl    $0x74697865, (%rdi)         # "exit" little-endian
    jne     .repl_check_quit
    cmpb    $0x0A, 4(%rdi)
    je      .repl_exit
.repl_check_quit:
    cmpl    $0x74697571, (%rdi)         # "quit" little-endian
    jne     .repl_check_help
    cmpb    $0x0A, 4(%rdi)
    je      .repl_exit

.repl_check_help:
    # Check for "help\n" (5 bytes)
    cmp     $5, %rcx
    jne     .repl_try_eval
    cmpl    $0x706C6568, (%rdi)         # "help" little-endian
    jne     .repl_try_eval
    cmpb    $0x0A, 4(%rdi)
    jne     .repl_try_eval
    # Print help message
    lea     msg_help(%rip), %rsi
    mov     $msg_help_len, %edx
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .repl_loop

.repl_try_eval:
    # Construct trampoline bytecode on heap that:
    #   1. Pushes input string
    #   2. Calls repl_eval(input)
    #   3. Emits result
    #   4. Halts (nested)
    #
    # Format (codegen bytecode):
    #   [0x01][len:2 LE][input bytes]   — Push string
    #   [0x24][0x09]["repl_eval"][0x01] — CallClosure "repl_eval" arity=1
    #   [0x06]                          — Emit
    #   [0x0F]                          — Halt

    # Bump heap past input data (so trampoline doesn't overwrite input)
    add     %rcx, %r15                  # r15 += input_len
    mov     %r15, %r8                   # trampoline base (after input)
    # Check repl_eval exists first
    push    %rcx                        # save input len
    push    %rdi                        # save input ptr
    lea     bname_repl_eval(%rip), %rdi
    mov     $9, %edx
    call    hash_name
    call    var_load_hash
    mov     %rdx, %rsi
    and     $0xFFFF, %esi
    pop     %rdi                        # restore input ptr
    pop     %rcx                        # restore input len
    cmp     $CLOSURE_MARKER, %si
    jne     .repl_fallback_echo

    # Build trampoline bytecode
    # Push opcode with binary molecule encoding:
    # [0x01][mol_count:u16][str_byte_mol(b0):u16][str_byte_mol(b1):u16]...
    # str_byte_mol(b) = 0x2100 | b  (shape=2, rel=1, lower 8 bits = byte value)
    movb    $0x01, (%r15)               # Push opcode
    movw    %cx, 1(%r15)                # mol_count = string length (1 mol per byte)
    add     $3, %r15

    # Encode each input byte as string molecule (u16 LE)
    push    %rcx
    push    %rdi
.repl_encode_loop:
    test    %ecx, %ecx
    jz      .repl_encode_done
    movzbl  (%rdi), %eax                # read input byte
    or      $0x2100, %eax               # str_byte_mol: shape=2, rel=1
    movw    %ax, (%r15)                 # write u16 LE molecule
    add     $2, %r15                    # 2 bytes per molecule
    inc     %rdi
    dec     %ecx
    jmp     .repl_encode_loop
.repl_encode_done:
    pop     %rdi
    pop     %rcx

    # CallClosure "repl_eval" arity=1
    movb    $0x24, (%r15)               # CallClosure opcode
    movb    $9, 1(%r15)                 # name len
    # Copy "repl_eval"
    push    %rcx
    lea     bname_repl_eval(%rip), %rsi
    lea     2(%r15), %rdi
    mov     $9, %ecx
    rep     movsb
    pop     %rcx
    movb    $1, 11(%r15)                # arity = 1
    add     $12, %r15

    # Emit
    movb    $0x06, (%r15)
    inc     %r15

    # Halt (nested)
    movb    $0x0F, (%r15)
    inc     %r15

    # Calculate trampoline length
    mov     %r15, %rcx
    sub     %r8, %rcx                   # trampoline bytecode length

    # Execute as nested bytecode: save context + run
    push    %r12                        # save boot bytecode base
    push    %r13                        # save boot PC
    push    %rbx                        # save boot bytecode size
    movabs  $0xDEADBEEFCAFE0002, %rax
    push    %rax                        # REPL sentinel (0002 = return to repl_loop)

    # Use boot bytecode base as r12 (not trampoline)
    # Trampoline is at %r8 — save it, set r12 = boot base
    mov     %r8, %r12                   # bytecode base = trampoline (for Push/CallClosure)
    xor     %r13d, %r13d               # PC = 0
    # Set rbx to large value so op_ret accepts boot bytecode PCs
    # (trampoline CallClosure jumps into boot bytecode bodies where
    # Ret pops saved PCs that are >> trampoline size)
    mov     $0x100000, %rbx             # 1MB — larger than any bytecode
    jmp     vm_loop                     # execute trampoline!
    # Flow: Push input → CallClosure repl_eval → Halt(REPL sentinel)
    #       → restore → jmp .repl_loop

.repl_fallback_echo:
    # repl_eval not found — echo input back
    mov     %rdi, %rsi                  # input ptr
    mov     %rcx, %rdx                  # input len
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .repl_loop

.halt_nested_restore:
    # Returning from nested __eval_bytecode execution
    # CPU stack now has: [saved_rbx][saved_r13][saved_r12]
    pop     %rbx                        # restore bytecode size
    pop     %r13                        # restore PC
    pop     %r12                        # restore bytecode base
    # Push empty return value (eval_bytecode consumed its arg but needs to
    # return a value for the caller's stack balance)
    sub     $16, %r14
    xor     %eax, %eax
    mov     %rax, (%r14)
    mov     %rax, 8(%r14)
    jmp     vm_loop                     # continue outer execution

# ── --eval mode: read ALL stdin → process each line → exit ─────────
.eval_stdin:
    # Pop the value that op_halt pushed back (prevents stack leak)
    pop     %rax

    # Check if this is a re-entry (buffer already read)
    cmpq    $0, eval_buf(%rip)
    jne     .eval_next_line             # re-entry → continue from saved offset

    # First entry: allocate 64KB buffer and read all stdin
    mov     %r15, %rax
    mov     %rax, eval_buf(%rip)        # save buffer base in BSS
    add     $0x10000, %r15              # reserve 64KB

    # Read all stdin into buffer
    mov     %rax, %rsi                  # buf
    mov     $0x10000, %edx             # max 64KB
    xor     %edi, %edi                  # fd = stdin (0)
    mov     $SYS_READ, %eax
    syscall
    test    %rax, %rax
    jle     .eval_exit                  # EOF or error → exit

    mov     %rax, eval_total(%rip)      # save total length
    movq    $0, eval_offset(%rip)       # start at offset 0

.eval_next_line:
    mov     eval_offset(%rip), %rcx     # current offset
    mov     eval_total(%rip), %r9       # total length
    mov     eval_buf(%rip), %r8         # buffer base
    cmp     %r9, %rcx
    jge     .eval_exit                  # processed all input

    # Find end of current line (scan for \n or end of input)
    mov     %rcx, %rdi                  # save line start offset
.eval_scan:
    cmp     %r9, %rcx
    jge     .eval_got_line              # end of input = end of line
    cmpb    $0x0A, (%r8,%rcx)          # newline?
    je      .eval_got_line
    inc     %rcx
    jmp     .eval_scan

.eval_got_line:
    # rdi = line start offset, rcx = line end offset (at \n or end)
    # Save next offset (past \n) for after execution
    mov     %rcx, %rax
    cmp     %r9, %rcx
    jge     .eval_no_skip_nl
    inc     %rax                        # skip past \n
.eval_no_skip_nl:
    mov     %rax, eval_offset(%rip)     # save offset for next iteration

    mov     %rcx, %rax
    sub     %rdi, %rax                  # rax = line length
    test    %rax, %rax
    jz      .eval_next_line             # skip empty lines

    # Set up line pointer and length
    lea     (%r8,%rdi), %rdi            # rdi = line start ptr
    mov     %rax, %rcx                  # rcx = line length

    # Strip trailing \r if present
    cmpb    $0x0D, -1(%rdi,%rcx)
    jne     .eval_no_cr
    dec     %rcx
    test    %rcx, %rcx
    jz      .eval_next_line             # empty after CR strip
.eval_no_cr:

    # Check if repl_eval exists
    push    %rcx                        # save line len
    push    %rdi                        # save line ptr
    lea     bname_repl_eval(%rip), %rdi
    mov     $9, %edx
    call    hash_name
    call    var_load_hash
    mov     %rdx, %rsi
    and     $0xFFFF, %esi
    pop     %rdi                        # restore line ptr
    pop     %rcx                        # restore line len
    cmp     $CLOSURE_MARKER, %si
    jne     .eval_fallback_echo

    # Build trampoline: Push string → CallClosure repl_eval → Emit → Halt
    mov     %r15, %r8                   # trampoline base
    # Push with binary molecule encoding (same as REPL trampoline)
    movb    $0x01, (%r15)               # Push opcode
    movw    %cx, 1(%r15)                # mol_count = string length
    add     $3, %r15

    # Encode each byte as string molecule (0x2100 | byte)
    push    %rcx
    push    %rdi
.eval_encode_loop:
    test    %ecx, %ecx
    jz      .eval_encode_done
    movzbl  (%rdi), %eax
    or      $0x2100, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    inc     %rdi
    dec     %ecx
    jmp     .eval_encode_loop
.eval_encode_done:
    pop     %rdi
    pop     %rcx

    # CallClosure "repl_eval" arity=1
    movb    $0x24, (%r15)
    movb    $9, 1(%r15)
    push    %rcx
    lea     bname_repl_eval(%rip), %rsi
    lea     2(%r15), %rdi
    mov     $9, %ecx
    rep     movsb
    pop     %rcx
    movb    $1, 11(%r15)                # arity = 1
    add     $12, %r15

    # Emit + Halt
    movb    $0x06, (%r15)
    inc     %r15
    movb    $0x0F, (%r15)
    inc     %r15

    # Calculate trampoline size
    mov     %r15, %rcx
    sub     %r8, %rcx

    # Execute as nested bytecode
    push    %r12
    push    %r13
    push    %rbx
    movabs  $0xDEADBEEFCAFE0001, %rax
    push    %rax                        # nested flag sentinel

    mov     %r8, %r12                   # bytecode base = trampoline
    xor     %r13d, %r13d               # PC = 0
    mov     $0x100000, %rbx             # 1MB bounds — op_ret accepts boot PCs
    jmp     vm_loop
    # Flow: trampoline → Halt(nested) → .halt_nested → restore r12/r13/rbx
    #       → vm_loop → boot bytecode Halt → op_halt → not nested
    #       → eval_mode check → .eval_stdin → .eval_next_line (reads from BSS)

.eval_fallback_echo:
    # repl_eval not found — echo input
    mov     %rdi, %rsi                  # line ptr
    mov     %rcx, %rdx                  # line len
    mov     $1, %edi                    # stdout
    mov     $SYS_WRITE, %eax
    syscall
    # Write newline
    lea     str_newline(%rip), %rsi
    mov     $1, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    jmp     .eval_next_line

.eval_exit:
    # Exit silently (no greeting, no goodbye)
    mov     $SYS_EXIT, %eax
    xor     %edi, %edi                  # exit code 0
    syscall

# ── --mcp mode: read stdin line-by-line, call mcp_dispatch, loop ─────
.mcp_stdin:
    pop     %rax                        # pop halt value

    # Allocate 64KB line buffer on heap (once)
    cmpq    $0, mcp_buf(%rip)
    jne     .mcp_read_line
    mov     %r15, %rax
    mov     %rax, mcp_buf(%rip)
    add     $0x10000, %r15              # reserve 64KB

.mcp_read_line:
    # Read one line from stdin (byte by byte into buffer)
    mov     mcp_buf(%rip), %r8          # buffer base
    xor     %ecx, %ecx                  # offset = 0
.mcp_read_byte:
    lea     (%r8, %rcx), %rsi          # buf + offset
    mov     $1, %edx                    # read 1 byte
    xor     %edi, %edi                  # fd = stdin
    push    %rcx
    push    %r8
    mov     $SYS_READ, %eax
    syscall
    pop     %r8
    pop     %rcx
    test    %rax, %rax
    jle     .eval_exit                  # EOF or error → exit
    cmpb    $0x0A, (%r8, %rcx)         # newline?
    je      .mcp_got_line
    inc     %ecx
    cmp     $0xFFF0, %ecx              # safety limit
    jge     .mcp_got_line
    jmp     .mcp_read_byte

.mcp_got_line:
    # rcx = line length (not including \n)
    test    %ecx, %ecx
    jz      .mcp_read_line             # skip empty lines

    # Strip \r if present
    cmpb    $0x0D, -1(%r8, %rcx)
    jne     .mcp_no_cr
    dec     %ecx
    test    %ecx, %ecx
    jz      .mcp_read_line
.mcp_no_cr:

    # Look up mcp_dispatch function
    mov     %r8, %rdi                   # line ptr
    mov     %rcx, %r9                   # save line length
    push    %r9
    push    %r8
    lea     bname_mcp_dispatch(%rip), %rdi
    mov     $12, %edx                   # len("mcp_dispatch")
    call    hash_name
    call    var_load_hash
    mov     %rdx, %rsi
    and     $0xFFFF, %esi
    pop     %r8                         # restore line ptr
    pop     %r9                         # restore line len
    cmp     $CLOSURE_MARKER, %si
    jne     .mcp_read_line              # mcp_dispatch not found → skip

    # Build trampoline: Push(line_string) → CallClosure(mcp_dispatch, 1) → Emit → Halt
    mov     %r15, %rdi                  # trampoline base
    push    %rdi
    movb    $0x01, (%r15)               # Push opcode
    movw    %r9w, 1(%r15)               # mol_count = string length
    add     $3, %r15

    # Encode each byte as string molecule (0x2100 | byte)
    xor     %ecx, %ecx
.mcp_encode_loop:
    cmp     %r9d, %ecx
    jge     .mcp_encode_done
    movzbl  (%r8, %rcx), %eax
    or      $0x2100, %eax
    movw    %ax, (%r15)
    add     $2, %r15
    inc     %ecx
    jmp     .mcp_encode_loop
.mcp_encode_done:

    # CallClosure "mcp_dispatch" arity=1
    movb    $0x24, (%r15)               # CallClosure opcode
    movb    $12, 1(%r15)                # name length
    push    %r9
    lea     bname_mcp_dispatch(%rip), %rsi
    lea     2(%r15), %rdi
    mov     $12, %ecx
    rep     movsb
    pop     %r9
    movb    $1, 14(%r15)               # arity = 1
    add     $15, %r15

    # Halt only — mcp_dispatch result stays on VM stack
    # .halt_mcp will write it to stdout directly (bypasses mcp_capture)
    movb    $0x0F, (%r15)               # Halt
    inc     %r15

    # Calculate trampoline size
    pop     %rdi                        # trampoline base
    mov     %r15, %rcx
    sub     %rdi, %rcx

    # Execute as nested bytecode with MCP sentinel
    push    %r12
    push    %r13
    push    %rbx
    movabs  $0xDEADBEEFCAFE0003, %rax   # MCP_SENTINEL
    push    %rax

    mov     %rdi, %r12                  # bytecode base = trampoline
    xor     %r13d, %r13d               # PC = 0
    mov     $0x100000, %rbx
    jmp     vm_loop
    # Flow: trampoline → Halt → .halt_mcp → .mcp_read_line (loop)

.repl_exit:
    # Restore terminal settings if raw mode was active
    cmpb    $1, repl_raw_mode(%rip)
    jne     .repl_exit_print
    lea     repl_termios_old(%rip), %rdx
    mov     $0x5402, %esi               # TCSETS
    xor     %edi, %edi                  # fd = stdin
    mov     $16, %eax                   # SYS_ioctl
    syscall
.repl_exit_print:
    # Print goodbye
    lea     msg_bye(%rip), %rsi
    mov     $4, %edx
    mov     $1, %edi
    mov     $SYS_WRITE, %eax
    syscall
    mov     $SYS_EXIT, %eax
    xor     %edi, %edi                  # exit(0)
    syscall

op_halt_err:
    # Print crash info: PC and stack trace
    lea     msg_crash_at(%rip), %rsi
    mov     $10, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    # Print PC as hex
    mov     %r13, %rdi
    call    print_hex_u64
    # Print newline
    lea     msg_newline(%rip), %rsi
    mov     $1, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    # Exit
    mov     $SYS_EXIT, %eax
    mov     $1, %edi
    syscall

# Print 64-bit value as hex to stderr
print_hex_u64:
    lea     hex_chars(%rip), %rsi
    lea     -16(%rsp), %rcx        # temp buffer on stack
    mov     $16, %r8d
.ph_loop:
    dec     %r8d
    mov     %rdi, %rax
    and     $0xF, %eax
    movzbl  (%rsi, %rax), %edx
    movb    %dl, (%rcx, %r8)
    shr     $4, %rdi
    test    %r8d, %r8d
    jnz     .ph_loop
    mov     %rcx, %rsi
    mov     $16, %edx
    mov     $2, %edi
    mov     $SYS_WRITE, %eax
    syscall
    ret

# ═══════════════════════════════════════════════════════════════════════════
# Read-only data
# ═══════════════════════════════════════════════════════════════════════════

.section .rodata

dev_null_path: .asciz "/dev/null"
repl_bs_seq: .byte 0x08, 0x20, 0x08       # backspace + space + backspace
proc_self_exe:
    .asciz "/proc/self/exe"

hex_chars:
    .ascii "0123456789ABCDEF"
msg_pc:
    .ascii "PC= "
msg_unknown:
    .ascii "Unknown opcode!\n"
    .byte 0, 0

# Native INTEGER loop template: s += i; i += 1; while i < limit
# Uses integer registers (add/inc ~1 cycle vs addsd ~4 cycles)
# Patched at: offset 2 (counter_addr), offset 12 (accum_addr), offset 30 (limit as f64 bits)
# Total: 73 bytes
jit_loop_template:
    .byte 0x48, 0xBF                    # mov rdi, imm64 (counter_addr)
    .quad 0                             # [patched] offset 2
    .byte 0x48, 0xBE                    # mov rsi, imm64 (accum_addr)
    .quad 0                             # [patched] offset 12
    # Load f64 values → convert to int64
    .byte 0xF2, 0x0F, 0x10, 0x07       # movsd xmm0, [rdi]            (offset 20)
    .byte 0xF2, 0x48, 0x0F, 0x2C, 0xC0 # cvttsd2si rax, xmm0  (i)    (offset 24)
    .byte 0xF2, 0x0F, 0x10, 0x06       # movsd xmm0, [rsi]            (offset 29)
    .byte 0xF2, 0x48, 0x0F, 0x2C, 0xC8 # cvttsd2si rcx, xmm0  (s)    (offset 33)
    # Load limit as int64
    .byte 0x48, 0xBA                    # mov rdx, imm64 (limit f64 bits) (offset 38)
    .quad 0                             # [patched] offset 40
    .byte 0x66, 0x48, 0x0F, 0x6E, 0xC2 # movq xmm0, rdx               (offset 48)
    .byte 0xF2, 0x48, 0x0F, 0x2C, 0xD0 # cvttsd2si rdx, xmm0 (limit) (offset 53)
    # .loop (offset 58):
    .byte 0x48, 0x39, 0xD0              # cmp rax, rdx (i vs limit)    (offset 58)
    .byte 0x7D, 0x08                    # jge .done (+8 → offset 71)   (offset 61)
    .byte 0x48, 0x01, 0xC1              # add rcx, rax (s += i)        (offset 63)
    .byte 0x48, 0xFF, 0xC0              # inc rax (i++)                (offset 66)
    .byte 0xEB, 0xF3                    # jmp .loop (-13 → offset 58)  (offset 69)
    # .done (offset 71):
    # Convert back to f64 and store
    .byte 0xF2, 0x48, 0x0F, 0x2A, 0xC0 # cvtsi2sd xmm0, rax (i)     (offset 71)
    .byte 0xF2, 0x0F, 0x11, 0x07       # movsd [rdi], xmm0           (offset 76)
    .byte 0xF2, 0x48, 0x0F, 0x2A, 0xC1 # cvtsi2sd xmm0, rcx (s)     (offset 80)
    .byte 0xF2, 0x0F, 0x11, 0x06       # movsd [rsi], xmm0           (offset 85)
    .byte 0xC3                          # ret                          (offset 89)
    # Total: 90 bytes

# Native fib(rdi) → rax: 42 bytes x86-64
jit_fib_code:
    .byte 0x48, 0x83, 0xFF, 0x02       # cmp rdi, 2
    .byte 0x7D, 0x04                    # jge +4 (to .recurse)
    .byte 0x48, 0x89, 0xF8              # mov rax, rdi
    .byte 0xC3                          # ret
    # .recurse:
    .byte 0x53                          # push rbx
    .byte 0x55                          # push rbp
    .byte 0x48, 0x89, 0xFB              # mov rbx, rdi
    .byte 0x48, 0x8D, 0x7B, 0xFF       # lea rdi, [rbx-1]
    .byte 0xE8, 0xE8, 0xFF, 0xFF, 0xFF # call fib (rel=-24)
    .byte 0x48, 0x89, 0xC5              # mov rbp, rax
    .byte 0x48, 0x8D, 0x7B, 0xFE       # lea rdi, [rbx-2]
    .byte 0xE8, 0xDC, 0xFF, 0xFF, 0xFF # call fib (rel=-36)
    .byte 0x48, 0x01, 0xE8              # add rax, rbp
    .byte 0x5D                          # pop rbp
    .byte 0x5B                          # pop rbx
    .byte 0xC3                          # ret

.section .text
# ═══════════════════════════════════════════════════════════════════════════
# var_find_addr: find address of value field in var_table entry
# Input: rax = hash
# Output: rax = address of val_ptr field (8 bytes into entry), 0 if not found
# ═══════════════════════════════════════════════════════════════════════════
var_find_addr:
    lea     var_table_base(%rip), %rcx
    mov     (%rcx), %rcx
    mov     (%rcx), %rdx
    test    %rdx, %rdx
    jz      .vfa_fail
    lea     -1(%rdx), %rsi
    imul    $24, %rsi
    lea     8(%rcx, %rsi), %rdi
.vfa_search:
    cmp     (%rdi), %rax
    je      .vfa_found
    sub     $24, %rdi
    dec     %rdx
    jnz     .vfa_search
.vfa_fail:
    xor     %eax, %eax
    ret
.vfa_found:
    lea     8(%rdi), %rax               # address of val_ptr field
    ret

# ═══════════════════════════════════════════════════════════════════════════
# try_jit_loop: analyze loop bytecode and generate native code
# Input: edi = loop_start_pc, esi = loop_end_pc, r12 = bytecode base
# Output: rax = native_ptr (0 = failed)
# ═══════════════════════════════════════════════════════════════════════════
try_jit_loop:
    push    %rbx
    push    %rbp
    push    %r14
    push    %r15
    mov     %edi, %ebp                  # ebp = scan cursor

    # Guard: loop body must be >= 30 bytes for accumulator pattern
    mov     %esi, %eax
    sub     %edi, %eax                  # body size
    cmp     $30, %eax
    jl      .tjl_fail                   # too small → skip

    # ── Pattern match: Load PushNum Call Jz Load Load Call Store Load PushNum Call Store Jmp ──
    # Step 1: Load(counter) = 0x02
    movzbl  (%r12, %rbp), %eax
    cmp     $0x02, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %edx         # name_len → rdx for hash_name
    lea     2(%r12, %rbp), %rdi         # name_ptr → rdi
    push    %rdx
    call    hash_name
    pop     %rcx
    mov     %rax, %r14                  # r14 = counter_hash
    lea     2(%rbp, %rcx), %ebp

    # Step 2: PushNum(limit) = 0x15
    movzbl  (%r12, %rbp), %eax
    cmp     $0x15, %al
    jne     .tjl_fail
    mov     1(%r12, %rbp), %r15         # r15 = limit f64 bits (raw 8 bytes)
    add     $9, %ebp

    # Step 3: Call = 0x07, skip name
    movzbl  (%r12, %rbp), %eax
    cmp     $0x07, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp

    # Step 4: Jz = 0x0A, skip target
    movzbl  (%r12, %rbp), %eax
    cmp     $0x0A, %al
    jne     .tjl_fail
    add     $5, %ebp

    # Step 5: Load(accum) = 0x02
    movzbl  (%r12, %rbp), %eax
    cmp     $0x02, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %edx         # name_len → rdx
    lea     2(%r12, %rbp), %rdi         # name_ptr → rdi
    push    %r14
    push    %rdx
    call    hash_name
    pop     %rcx
    pop     %r14
    mov     %rax, %rbx                  # rbx = accum_hash
    lea     2(%rbp, %rcx), %ebp

    # Steps 6-12: skip remaining opcodes (Load Call Store Load PushNum Call Store)
    # Just verify opcodes are correct types, skip content
    # Step 6: Load = 0x02
    movzbl  (%r12, %rbp), %eax
    cmp     $0x02, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 7: Call = 0x07
    movzbl  (%r12, %rbp), %eax
    cmp     $0x07, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 8: Store/StoreUpdate
    movzbl  (%r12, %rbp), %eax
    cmp     $0x13, %al
    je      .tjl_s8
    cmp     $0x1C, %al
    jne     .tjl_fail
.tjl_s8:
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 9: Load = 0x02
    movzbl  (%r12, %rbp), %eax
    cmp     $0x02, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 10: PushNum = 0x15
    movzbl  (%r12, %rbp), %eax
    cmp     $0x15, %al
    jne     .tjl_fail
    add     $9, %ebp
    # Step 11: Call = 0x07
    movzbl  (%r12, %rbp), %eax
    cmp     $0x07, %al
    jne     .tjl_fail
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 12: Store/StoreUpdate
    movzbl  (%r12, %rbp), %eax
    cmp     $0x13, %al
    je      .tjl_s12
    cmp     $0x1C, %al
    jne     .tjl_fail
.tjl_s12:
    movzbl  1(%r12, %rbp), %ecx
    lea     2(%rbp, %rcx), %ebp
    # Step 13: Jmp = 0x09
    movzbl  (%r12, %rbp), %eax
    cmp     $0x09, %al
    jne     .tjl_fail

    # ── Pattern matched! r14=counter_hash, rbx=accum_hash, r15=limit_bits ──

    # Find var_table entry addresses
    mov     %r14, %rax
    call    var_find_addr
    test    %rax, %rax
    jz      .tjl_fail
    mov     %rax, %r14                  # r14 = counter entry addr

    mov     %rbx, %rax
    call    var_find_addr
    test    %rax, %rax
    jz      .tjl_fail
    mov     %rax, %rbx                  # rbx = accum entry addr

    # ── Generate native code ──
    # mmap executable page
    push    %r14
    push    %rbx
    push    %r15
    mov     $SYS_MMAP, %eax
    xor     %edi, %edi
    mov     $4096, %esi
    mov     $7, %edx                    # RWX
    mov     $0x22, %r10d
    mov     $-1, %r8d
    xor     %r9d, %r9d
    syscall
    mov     %rax, %rdi                  # rdi = exec page
    pop     %r15                        # limit_bits
    pop     %rbx                        # accum_addr
    pop     %r14                        # counter_addr

    # Write native loop:
    # mov rdi, counter_addr; mov rsi, accum_addr
    # movsd xmm0,[rdi]; movsd xmm1,[rsi]
    # mov rax, limit; movq xmm2, rax; mov rax, 1.0; movq xmm3, rax
    # .loop: ucomisd xmm2,xmm0; jbe .done; addsd xmm1,xmm0; addsd xmm0,xmm3; jmp .loop
    # .done: movsd [rdi],xmm0; movsd [rsi],xmm1; ret
    lea     jit_loop_template(%rip), %rsi
    mov     $90, %ecx
    push    %rdi
    rep movsb                           # copy template
    pop     %rdi
    # Patch addresses: counter_addr at offset 2, accum_addr at offset 12, limit at offset 30
    mov     %r14, 2(%rdi)               # patch counter_addr
    mov     %rbx, 12(%rdi)              # patch accum_addr
    mov     %r15, 40(%rdi)              # patch limit_bits (offset 40 in int template)

    mov     %rdi, %rax                  # return native_ptr
    pop     %r15
    pop     %r14
    pop     %rbp
    pop     %rbx
    ret

.tjl_fail:
    pop     %r15
    pop     %r14
    pop     %rbp
    pop     %rbx
    xor     %eax, %eax
    ret

msg_cont_prompt:
    .ascii "... "

msg_crash_at:
    .ascii "CRASH PC=0x"
msg_newline:
    .ascii "\n"
    .byte 0

msg_open:
    .ascii "Cannot open binary!\n"
    .byte 0, 0

msg_header:
    .ascii "Bad header read!\n"
    .byte 0, 0

msg_mmap:
    .ascii "mmap failed!\n"

msg_readbc:
    .ascii "Bytecode read fail!\n"
    .byte 0

msg_divzero:
    .ascii "Division by zero!\n"

msg_greeting:
    .ascii "\xe2\xa6\xbf HomeOS v0.05\n"
    .ascii "\xe2\x97\x8b Type code or text \xc2\xb7 exit to quit\n"
msg_greeting_len:
    .byte 55                            # exact byte count of greeting

msg_prompt:
    .ascii "\xe2\xa6\xbf "               # "⦿ " (⦿ U+29BF = 3 bytes UTF-8 + space)

msg_help:
    .ascii "Code: let fn emit if while for match lambda\n"
    .ascii "HOF:  map filter reduce pipe any all\n"
    .ascii "AI:   learn <fact>  respond <text>  memory  fns\n"
    .ascii "Sys:  test  build  save  load  compile <path>  exit\n"
.equ msg_help_len, . - msg_help

# ── Keyword hash table for __str_is_keyword ────────────────────────────
.align 4
kw_count: .long 28
.align 8
kw_hashes:
    .quad 0x127284191DCC577A  # let
    .quad 0x08985A07B541D4F5  # fn
    .quad 0x08B73007B55C3E26  # if
    .quad 0x7F2B6C605332DD30  # else
    .quad 0xCDECE4AD70D57DEB  # loop
    .quad 0xCE87A3885811296E  # while
    .quad 0xDCB27818FED9DA90  # for
    .quad 0x08B73807B55C4BBE  # in
    .quad 0xC5C7B983377CAD5F  # return
    .quad 0x93B7591DEBC7CE38  # break
    .quad 0xD2BFD5ACCD1966A4  # continue
    .quad 0x86D29860573F5FCA  # emit
    .quad 0xA79439EF7BFA9C2D  # type
    .quad 0x4B2A584E9A909034  # union
    .quad 0xEE517EC576C9B637  # impl
    .quad 0xDEE78BA6C508610B  # trait
    .quad 0xC3BFE3A4FE4C13F6  # match
    .quad 0x570AC119447423EE  # try
    .quad 0xC1B2E33B13EC076C  # catch
    .quad 0x4328F78AB20E1F98  # spawn
    .quad 0xBD76100BD58C9C0D  # select
    .quad 0x60827E549DE65488  # timeout
    .quad 0x7F845078D7A5C0B5  # from
    .quad 0x4C52B0193DCCE634  # use
    .quad 0x0808591917670EE3  # mod
    .quad 0x77F362195699B624  # pub
    .quad 0x5B5C98EF514DBFA5  # true
    .quad 0xB5FAE2C14238B978  # false

    .ascii "!"

msg_bye:
    .ascii "bye\n"

str_newline:
    .ascii "\n"

# ── Type name strings ───────────────────────────────────────────────────
str_number:     .ascii "number"
str_string:     .ascii "string"
str_array:      .ascii "array"
str_dict:       .ascii "dict"
str_fn:         .ascii "function"
str_nil:        .ascii "nil"

# u16 molecule-encoded type strings (0x21XX per char)
u16_number:     .short 0x216E, 0x2175, 0x216D, 0x2162, 0x2165, 0x2172  # "number"
u16_string:     .short 0x2173, 0x2174, 0x2172, 0x2169, 0x216E, 0x2167  # "string"
u16_array:      .short 0x2161, 0x2172, 0x2172, 0x2161, 0x2179           # "array"
u16_dict:       .short 0x2164, 0x2169, 0x2163, 0x2174                   # "dict"
u16_closure:    .short 0x2163, 0x216C, 0x216F, 0x2173, 0x2175, 0x2172, 0x2165  # "closure"
u16_nil:        .short 0x216E, 0x2169, 0x216C                           # "nil"

# ── Float constants ─────────────────────────────────────────────────────
.align 8
const_ten:      .double 10.0
const_one:      .double 1.0
const_neg_one:  .double -1.0

# ── Emit format strings ─────────────────────────────────────────────────
str_array_open:  .ascii "[array "
str_bracket_nl:  .ascii "]\n"
str_empty_array: .ascii "[]\n\0"
str_lbracket:    .ascii "["
str_rbracket:    .ascii "]"
str_rbracket_nl: .ascii "]\n"
str_comma_space: .ascii ", "
str_dict_open:   .ascii "{dict "
str_dict_placeholder: .ascii "{...}"
str_brace_nl:    .ascii "}\n"
str_empty_dict:  .ascii "{}\n\0"
str_lbrace:      .ascii "{"
str_colon_sp:    .ascii ": "
str_comma_sp:    .ascii ", "

# ── REPL function name ──────────────────────────────────────────────────
bname_repl_eval: .ascii "repl_eval"
bname_mcp_dispatch: .ascii "mcp_dispatch"


# ── BUILTIN JUMP TABLE (256 entries × 8 bytes = 2KB) ──────────────────
    .align 8
builtin_jump_table:
    .quad   .call_not_builtin
    .quad   .builtin_slot_1
    .quad   .builtin_slot_2
    .quad   .builtin_slot_3
    .quad   .call_not_builtin
    .quad   .builtin_slot_5
    .quad   .builtin_slot_6
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_10
    .quad   .call_not_builtin
    .quad   .builtin_slot_12
    .quad   .call_not_builtin
    .quad   .builtin_slot_14
    .quad   .builtin_slot_15
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_18
    .quad   .call_not_builtin
    .quad   .builtin_slot_20
    .quad   .builtin_slot_21
    .quad   .call_not_builtin
    .quad   .builtin_slot_23
    .quad   .builtin_slot_24
    .quad   .builtin_slot_25
    .quad   .builtin_slot_26
    .quad   .builtin_slot_27
    .quad   .builtin_slot_28
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_32
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_36
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_41
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_44
    .quad   .builtin_slot_45
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_51
    .quad   .builtin_slot_52
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_56
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_59
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_74
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_79
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_86
    .quad   .builtin_slot_87
    .quad   .call_not_builtin
    .quad   .builtin_slot_89
    .quad   .call_not_builtin
    .quad   .builtin_slot_91
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_94
    .quad   .builtin_slot_95
    .quad   .call_not_builtin
    .quad   .builtin_slot_97
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_101
    .quad   .builtin_slot_102
    .quad   .builtin_slot_103
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_106
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_109
    .quad   .builtin_slot_110
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_113
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_116
    .quad   .call_not_builtin
    .quad   .builtin_slot_118
    .quad   .builtin_slot_119
    .quad   .call_not_builtin
    .quad   .builtin_slot_121
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_125
    .quad   .builtin_slot_126
    .quad   .builtin_slot_127
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_130
    .quad   .builtin_slot_131
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_134
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_144
    .quad   .builtin_slot_145
    .quad   .builtin_slot_146
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_151
    .quad   .call_not_builtin
    .quad   .builtin_slot_153
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_156
    .quad   .call_not_builtin
    .quad   .builtin_slot_158
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_161
    .quad   .builtin_slot_162
    .quad   .call_not_builtin
    .quad   .builtin_slot_164
    .quad   .builtin_slot_165
    .quad   .builtin_slot_166
    .quad   .builtin_slot_167
    .quad   .builtin_slot_168
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_171
    .quad   .builtin_slot_172
    .quad   .builtin_slot_173
    .quad   .call_not_builtin
    .quad   .builtin_slot_175
    .quad   .builtin_slot_176
    .quad   .call_not_builtin
    .quad   .builtin_slot_178
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_185
    .quad   .builtin_slot_186
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_190
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_196
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_200
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_207
    .quad   .builtin_slot_208
    .quad   .call_not_builtin
    .quad   .builtin_slot_210
    .quad   .call_not_builtin
    .quad   .builtin_slot_212
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_228
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_233
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_237
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_240
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_245
    .quad   .builtin_slot_246
    .quad   .call_not_builtin
    .quad   .call_not_builtin
    .quad   .builtin_slot_249
    .quad   .builtin_slot_250
    .quad   .call_not_builtin
    .quad   .builtin_slot_252
    .quad   .builtin_slot_253
    .quad   .call_not_builtin
    .quad   .builtin_slot_255

# ── BSS (writable, zero-initialized) ────────────────────────────────────
.section .bss
var_table_base:
    .space 8                            # pointer to variable table on heap
bc_format:
    .space 1                            # 0 = ir.rs format, 1 = codegen.ol format
    .align 8
boot_bc_base:
    .space 8                            # boot bytecode base address (for nested calls)
boot_bc_size:
    .space 8                            # boot bytecode size
eval_bc_base:
    .space 8                            # eval bytecode base (for cross-boundary closure calls)
eval_mode:
    .space 1                            # 0 = REPL mode, 1 = --eval mode (stdin)
    .align 8
repl_heap_checkpoint:
    .space 8                            # heap pointer saved at REPL turn start
    .align 8
    .space 8                            # saved initial r14 for debug
jz_did_pop:
    .space 1                            # 1 = Jz already popped, next Pop is no-op
    .align 8
# Var shadow stack: saves [hash, old_ptr, old_len] per modified var in closure.
# Marker entry: hash=0 separates call frames.
var_shadow_base:
    .space 8                            # pointer to shadow stack on heap
var_shadow_top:
    .space 8                            # current top of shadow stack
closure_depth:
    .space 8                            # nesting depth (0 = not in closure)
skip_scope_restore:
    .space 8                            # >0 → skip N scope restores (for module loading)
var_cache:
    .space 6144                         # 256 × [hash:8, entry_ptr:8, vt_count:8] = 24 bytes each
loop_counters:
    .space 1024                         # 256 × 4-byte backward jump counters
loop_jit_cache:
    .space 512                          # 32 × [start_pc:8, native_ptr:8] = 16 bytes each
_save_rbx:
    .space 8                            # temp save for rbx
scope_frame_ptrs:
    .space 32768                        # 4096 frame pointers × 8 bytes (max recursion depth)
    .align 8
# ── Register frame stack ──
reg_frame_base:
    .space 8                            # register stack base on heap
reg_frame_ptr:
    .space 8                            # next free position on register stack
reg_cur_frame:
    .space 8                            # current frame base (for LoadReg/StoreReg)
# ── Function Registry (user-defined functions, separate from var_table) ──
# 1024 slots × 24 bytes = [hash:8][body_pc:8][param_count:4][flags:4]
fn_registry:
    .space 24576                        # 1024 × 24 bytes
# ── Regex NFA buffers ──
rx_pattern:
    .space 1024                         # decoded pattern bytes
rx_text:
    .space 65536                        # decoded text bytes
rx_nfa:
    .space 2048                         # NFA states: 256 × 8 bytes
rx_cur_set:
    .space 32                           # active state bitmap (256 bits)
rx_next_set:
    .space 32                           # next active state bitmap
rx_search_mode:
    .space 8                            # 1 = search mode (unanchored)
rx_alt_patch:
    .space 4                            # alternation left-branch patch index (-1 = none)
fn_registry_count:
    .space 8                            # number of registered functions
try_stack:
    .space 256                          # 16 try frames × 16 bytes (catch_PC:8 + saved_r14:8)
try_depth:
    .space 8                            # current try nesting depth (0 = no try)
throw_buf:
    .space 256                          # temp buffer for error message decoding
heap_checkpoints:
    .space 128                          # 16 checkpoint slots × 8 bytes
heap_cp_count:
    .space 8
sha_input:
    .space 1024                         # SHA-256 input buffer (max ~900 bytes after padding)
sha_state:
    .space 32                           # H[0..7] (8 × 4 bytes)
sha_w:
    .space 256                          # W[0..63] message schedule (64 × 4 bytes)
sha_abcdefgh:
    .space 32                           # working variables a..h (8 × 4 bytes)
sha_save_regs:
    .space 64                           # saved VM registers during SHA-256 (8 × 8 bytes)
sha_round_idx:
    .space 8                            # round counter (avoid clobbering r12)
eval_buf:
    .space 8                            # pointer to stdin buffer on heap
eval_total:
    .space 8                            # total bytes read from stdin
eval_offset:
    .space 8                            # current processing offset in buffer
mcp_buf:
    .space 8                            # pointer to MCP line buffer on heap
mcp_entered:
    .space 1                            # 1 = already entered MCP loop (skip pop)
fwrite_flags:
    .space 4                            # open flags for file write/append
mcp_capture:
    .space 1                            # 1 = capture emit output to mcp_out_buf
mcp_out_len:
    .space 8                            # current length of captured output
mcp_out_buf:
    .space 8192                         # captured emit output buffer (8KB)
mcp_out_mols:
    .space 16384                        # molecule encoding buffer (8K chars × 2 bytes)
mcp_fmt_tmp:
    .space 64                           # temp buffer for f64 formatting in capture mode
repl_termios_old:
    .space 60                           # saved terminal settings
repl_termios_raw:
    .space 60                           # raw mode terminal settings
repl_raw_mode:
    .space 1                            # 1 = raw mode active
repl_tab_match:
    .space 8                            # ptr to first matching name
repl_tab_match_len:
    .space 8                            # length of matching name
jit_cache:
    .space 512                          # 32 slots × [hash:8][ptr:8] = 16 bytes each
profile_counters:
    .space 1024                         # 256 × 4-byte counters
profile_hashes:
    .space 2048                         # 256 × 8-byte hashes
hn_cache:
    .space 4096                         # 256 entries × [ptr:8][hash:8] = 16 bytes each
net_sockaddr:
    .space 16
net_recv_buf:
    .space 65536
net_dents_buf:
    .space 8192
sha512_state:
    .space 64
sha512_w:
    .space 640
sha512_input:
    .space 2048
ed25519_tmp:
    .space 512
