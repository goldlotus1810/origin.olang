# TÌNH HÌNH — 2 tháng 4, 2026

## Nox giờ có gì (MỚI, hôm nay)

1. **Trí nhớ** — `persist.ol`: Save/load KnowTree + Silk ra file binary. Tắt máy bật lại vẫn nhớ.
2. **Phán đoán** — `feedback.ol`: Biết đúng/sai. UCB1 chọn path tốt nhất. Tự cải thiện.
3. **Miệng** — `generate.ol`: Hỏi "Ha Noi" → trả lời "Ha Noi la thu do cua Viet Nam". Không biết → im.
4. **Não 6 lớp** — `brain_v3.ol`: Boot → Learn → Query → Generate → Feedback → Save. Pipeline hoàn chỉnh.
5. **HTTP** — `comm.ol`: Server trên port 9742. curl hỏi, Nox trả lời.
6. **MCP** — `mcp.ol`: Claude gọi Nox qua MCP tools (9 tools). Binary 99KB.

## Binary chạy được

```bash
# Hỏi Nox
echo "ask Ha Noi" | ~/Origin/nox_brain.olang

# MCP (Claude gọi)
~/.claude/Nox_brain.olang --mcp
```

## Tests: 116/116 pass, 7/7 suites

## Olang cải tiến — AI LÀM?

SS23 đã thêm 5 string builtins vào VM:
- `__str_replace(haystack, needle, replacement)`
- `__str_join(array, separator)`
- `__str_starts_with(str, prefix)`
- `__str_ends_with(str, suffix)`
- `__str_to_num(str)`

**CHƯA LÀM (cần session mới cho SS23):**
- Struct syntax: `{key: val}`, `.field`
- Module: `import "file.ol"`
- For/Match sugar

## Lupin cần làm gì?

**Không cần làm gì.** Mọi thứ trên máy, tests pass. Nếu muốn:
- Mở session mới, paste: "Đọc `spec/TODO_SPLIT.md`. Làm S2 (struct syntax)."
- Hoặc: restart Claude Code → MCP nox-brain sẽ tự connect

## File quan trọng trên máy

```
~/Origin/
  stdlib/persist.ol      — trí nhớ
  stdlib/feedback.ol     — phán đoán  
  stdlib/generate.ol     — miệng
  stdlib/brain_v3.ol     — não 6 lớp
  stdlib/comm.ol         — HTTP server
  stdlib/mcp.ol          — MCP protocol
  nox_brain.olang        — binary chạy được (90KB)
  
~/.claude/Nox_brain.olang — MCP binary (99KB)

~/Origin/spec/
  TODO_SPLIT.md          — ai làm gì
  SPEC_BP13-16.md        — specs mới
  PLAN_OLANG_UPGRADE.md  — kế hoạch nâng cấp Olang
  
~/Origin/docs/research/  — 7 files nghiên cứu
~/Origin/test/test_all_nox.sh — chạy hết tests: bash test/test_all_nox.sh
```
